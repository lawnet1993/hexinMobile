name: secure_tunnel
description: SecureAccess mobile tunnel contract and safe unavailable fallback.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.hexing.zhilian.secure_tunnel
        pluginClass: SecureTunnelPlugin
