# 586 新建待办抽屉信息密度整改

## 当前结论

本轮完成“待办 → 新建待办”的移动端紧凑化、自动化回归、Profile 构建和真机覆盖安装。真机仍处于系统锁屏，最终触控与截图复验待用户正常解锁后继续。

- 新建待办继续使用覆盖底部 Tab 的根导航底部抽屉，不使用居中弹窗。
- 抽屉增加紧凑标题栏和关闭入口，正文不再使用默认大号表单布局。
- 待办内容输入框固定为 36dp，使用浅灰填充且移除描边。
- 校验错误独立显示在输入框下方，不挤压 36dp 输入区域。
- 优先级选择固定为 36dp；取消和创建按钮右对齐，创建按钮实际高度为 36dp、宽度小于 120dp。
- 第一次定向回归发现按钮被紧凑视觉密度二次压成 28dp，已修正并由尺寸断言锁定为 36dp。

当前判定：**实现、自动化、静态检查和覆盖安装通过；真机视觉复验待解锁**。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| 定向测试 | `todos page unifies personal todos with approval work` 通过 |
| 完整自动化 | 210/210 通过 |
| 静态检查 | `flutter analyze`，0 个问题 |
| Profile 构建 | 成功，76.0 MB |
| Profile APK SHA-256 | `8C0B64BEAB5849CB22776B47E8DC0989D42B6C85FD839F0C63F708E4392B236F` |
| 真机覆盖安装 | `adb install -r` 成功 |
| 真机状态 | `Keyguard showing=true`、`mInputRestricted=true` |

## 自动化覆盖

现有待办页面回归新增以下断言：

1. “新建”先打开移动操作抽屉，页面没有 `AlertDialog` 或悬浮菜单。
2. 选择“新建待办”后出现 `todo-create-sheet`。
3. 待办内容输入框实际高度为 36dp。
4. 创建按钮实际高度为 36dp、宽度小于 120dp。
5. 返回后抽屉完整关闭，不残留遮罩或路由。

## 真机边界

本轮没有创建待办、修改审批或写入线上业务数据。设备解锁后需要继续：

1. 打开“待办 → + → 新建待办”，记录键盘弹出前后截图。
2. 检查抽屉是否覆盖底部 Tab、输入框和优先级是否在小屏下完整可见。
3. 只输入临时文本验证焦点和错误提示，随后取消，不提交真实待办。
4. 同时补齐 585 的发起会话与 OA 成员选择器真机复验。
5. 清理应用范围日志并确认无 Flutter、ANR 或崩溃异常。
