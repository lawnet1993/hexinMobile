# 570 考勤通知跳转验收

## 结论

本轮通过。移动端通知中心的考勤异常不再点击无响应；它与审批、单聊、群聊和好友申请继续保持独立路由，进入真实补卡异常页。

## 修复内容

- 使用服务端通知 `category/type` 识别考勤，不依赖“缺少上班打卡”等中文标题。
- 考勤通知显示“考勤”标签和异常日历图标。
- 无异常 ID 时进入 `/attendance` 异常列表；携带 `targetId/requestId` 时通过查询参数传递异常 ID。
- 补卡页收到异常 ID 后将匹配项置顶并轻量高亮；ID 缺失或失效时保持原有列表顺序。
- IM 会话仍按 `im_conversation` 打开真实会话，没有与 OA 考勤通知共用目标。

## 自动化

- 路由测试覆盖考勤列表回退、带特殊字符异常 ID 的安全编码、单聊/群聊/审批/好友申请原有路由。
- 页面测试覆盖目标异常置顶和高亮。
- 午夜跨日导致固定日期与今日日期相同时，动态表单测试改为在目标字段范围内断言，避免全页同值误判。
- `flutter analyze`：0 问题。
- `flutter test`：197/197 通过。

## 真机证据

- 设备：realme RMX3366，Android 14，序列号 `dd00d66d`。
- Profile APK SHA-256：`ACC2FB86196A1885B70C6D7247B6B032A115AC6A2456759AFFD5D22B597FEDB4`。
- 通知中心真实显示 28 条通知、0 条未读；考勤项增加“考勤”标签。
- 点击 2026-08-14 考勤通知进入“补卡”，显示本月异常 19、补卡次数 0/3 和真实待处理列表。
- 返回后点击群聊通知进入 `Mobile-Group-20260825`，显示 2 位成员、1 人在线和原有视频预览。
- 证据目录：`docs/device-acceptance/notification-audit-20260901/`。
- logcat 无 `FATAL EXCEPTION`、应用 ANR 或 `Unhandled Exception`。

## 当前边界

线上考勤通知当前没有可与异常列表匹配的 `targetId/requestId`，因此只能进入异常列表，不能可靠定位 2026-08-14 的单条异常。移动端已支持精确 ID，待服务端补齐后可直接正向验证。

本轮仅打开已有通知、考勤列表和群聊，没有提交补卡、发送消息、标记通知、修改或删除业务数据。临时复制的加密通知数据库快照已删除，真机原始缓存未改动。
