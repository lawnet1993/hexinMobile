# 749 · Windows 入站缓存与日志异常证据及窗口待复验项

> 当前基线更新（2026-09-08）：用户提供的真实运行窗口已确认桌面端为 **v1.0.105**。本文中的 v1.0.93/v1.0.94 仅表示 2026-09-06 当次历史取证版本，不再作为当前移动端功能对齐基线；当前结论与未完成项以 [799](799-mobile-core-goal-completion-audit-20260906.md) 和 [801](801-windows-1105-desktop-only-site-boundary-20260908.md) 为准。

日期：2026-09-06（Asia/Shanghai）  
结论：**更新前采样存在桌面缓存缺消息；v1.0.94 真实窗口已补齐旧文件，本轮新文本双端可见。新版仍复现会话摘要至少 86 秒未更新、指定历史视频播放 404。完整实测与新提示词见 [750](750-windows-1094-real-window-regression-20260906.md)。**

版本备注：用户报告当前窗口版本为 v1.0.93；发布接口在当次查询返回 `latestVersion=1.0.93`（发布时间 2026-09-05T13:22:12.469362）。已检查路径的 `hexing-zhilian.exe` 与 `uninstall.exe` 文件版本元数据仍为 1.0.87，主程序文件时间为 2026-09-02 13:29:36。发布接口只证明可用发布版本，文件元数据也不能独立证明当前窗口加载的构建；后续以 v1.0.93 窗口为验收目标，核对构建标识与安装包哈希，不能据此判定升级失败。

### 新版本更新与复验基准

2026-09-06 用户新增的[桌面更新截图](../test/evidence/desktop-update-20260906/user-desktop-1.0.93-to-1.0.94-downloading.png)明确显示：当前 v1.0.93、最新 v1.0.94、最低支持版本 v1.0.94、安装包 240.55 MB，处于下载 3%（7.85 MB）的阶段。窗口显示的当前版本现有截图依据。

更新说明包含“桌面消息提醒与多端实时同步修复”。因此下一轮窗口验收改为升级完成后的 v1.0.94：先记录安装完成和运行版本，再新发带时间前缀的单聊/群聊消息，检查双向接收、同账号消息、已读同步、附件及媒体。此截图只能证明下载中，不能证明 v1.0.94 已安装，也不能把下文更新前的异常直接定性为 v1.0.94 的缺陷。

随后 Computer Use 已真实激活合兴智联窗口：工作台及可访问树确认当前 v1.0.94、最新 v1.0.94，登录 test01。打开 Test Terminal 03 单聊后，聊天区已显示 00:16 的 `AI-UAT-20260902-165500-leave-proof.txt`，会话列表摘要也显示该附件。因此“旧文件持续缺失”在本次新版窗口检查中未复现；是否经启动补偿或打开会话补齐尚未区分，不能代替新消息实时同步测试。

### v1.0.94 新窗口实测记录

- 00:48:31.718 通过桌面真实输入框向 Test Terminal 03 发送 `AI-UAT-20260906-004800-D94-M1-TEXT`，仅点击发送一次。
- 00:48:40.426 窗口观察：目标消息正文节点精确匹配 1 条，另有列表摘要 1 处；输入框清空，发送按钮禁用，消息显示单勾/未读。这里只确认桌面发送状态，没有将单勾当成对方已读。
- [桌面版本与真实发送截图](../test/evidence/desktop-update-20260906/02-desktop-v1094-test03-sent.png)同时显示 test01、v1.0.94、新消息及此前缺失的文件。
- 真机随后解锁，原会话底部可见桌面 00:48 文本；00:51:12.681 手机发送反向新文本，桌面原会话 00:51:25.646 自然出现，未重开或刷新且正文仅一条。双端已读、群聊和离线补偿本轮仍未覆盖。
- 新版问题：00:52:38.626 左侧会话摘要仍停在 00:48，距离手机发送已约 86 秒；点击本会话 2.0 MB/0:03 视频显示 `status=404 detail=attachment request failed`。均已有真实截图，详见 [750](750-windows-1094-real-window-regression-20260906.md)。
- 00:48:40 的只读日志采样仍出现 `transport error` 与 `event id is missing`。这是一项需关联新消息逐段核对的日志线索，不直接替代新版窗口收发结论。

## 更新前的链路采样

1. 当前 Windows 客户端与 Android 真机同时登录 Test Terminal 01。
2. Android 在与 Test Terminal 03 的真实单聊中，通过系统文件选择器发送 `AI-UAT-20260902-165500-leave-proof.txt`。
3. 移动端消息显示“已发送”，选择器缓存为空，当前测试文件已从移动端 Outbox 清理，移动端进程无 FATAL、ANR、Unhandled Exception 或 `E/flutter`。
4. 只读查询当前运行桌面端的 `collaboration-cache.sqlite3`，立即查询和等待 20 秒后查询均为 0 条匹配消息。

更新前没有通过 Windows 窗口检查会话列表、当前聊天区、附件打开、视频/音频播放、下载进度或 OA 页面。升级后的列表及聊天区结果见上方新增记录；附件打开、视频/音频播放、下载进度及 OA 交互仍需单独复验。

## 更新前的桌面缓存与日志证据

- `im_messages_cache` 总数始终为 424，最大 sequence 始终为 20840，最大 updated_at 始终为 2026-09-05 23:56:05。
- `collaboration_event_inbox` 没有本轮文件消息。
- 当前桌面日志持续记录 `realtime connection state`，IM 状态为 `retry-wait`，原因是 `transport error`，重试次数已经超过 260。
- 同一日志持续记录 `im realtime events could not be persisted: collaboration event id is missing`。
- 2026-09-06 00:36:43 再次只读采样：缓存仍为 424 条、最大 sequence 20840、事件 inbox 为 0；00:36:20 的 IM realtime 已到第 298 次重试，仍为 `retry-wait / transport error`，随后继续出现缺少事件 ID 的持久化警告。
- 日志显示连接反复失败和事件 ID 缺失两类异常；两者是否同源，以及缺少 ID 的数据来自服务端响应、事件映射还是内部调用，目前未定位。

该证据来自当前安装运行的 Windows 二进制、数据库和日志，不使用旧桌面源码代替当前状态。数据库查询全程只读，没有修改桌面数据；它只证明入站落库链路异常，不证明当前 v1.0.93 窗口中所有可见问题都已复现。

Windows 真实窗口复验原计划使用 Computer Use 插件，但本任务仅加载到技能说明，没有暴露其必需的 `node_repl/@oai/sky` 控制入口。未使用猜测坐标或其他 UI 自动化替代，窗口层项目继续保持未执行。

2026-09-06 重启后的挂载排查：Codex 00:40:43/44 日志明确记录 `node_repl` 与 `cua_repl` 启动失败，错误为“目录名称无效（os error 267）”。任务绑定的旧“副本 (2)”目录已不存在，而现有项目目录有效。已为全局 `mcp_servers.node_repl` 配置有效 `cwd`，配置读取校验通过；实际工具挂载和 Windows 窗口复验需在控制服务初始化成功后继续。此前仅将 `js_repl` 改为 `true` 并不能证明挂载成功。

00:45 已为旧任务目录建立 Junction，目标为现有工程目录，未复制项目文件；新初始化会话的 `node_repl` 与 `cua_repl` 于 00:45:41 均进入 `ready`。这证明进程初始化错误已消除，实际桌面窗口枚举和交互还需要单独验证。

随后已通过 `mcp__node_repl__js` 初始化 `@oai/sky` 并真实执行 `list_apps`、`list_windows`，成功枚举唯一合兴智联窗口。首个截图请求提示窗口最小化；控制连接已恢复，正在继续窗口观察。此握手不代表 IM/OA 功能已经验收。

## 判定

更新前：移动端显示发送成功，被查询桌面缓存及事件收件箱未检出目标消息，该采样窗口内缓存补齐验证未通过。更新后：v1.0.94 已显示旧目标文件，新文本双端可见，手机回复在桌面原会话自然出现；但会话摘要与指定视频播放未通过。新证据与修复提示词以 [750](750-windows-1094-real-window-regression-20260906.md) 为准；下方旧排查提示词保留更新前上下文，不代表全部问题仍在新版复现。

## 交给桌面端 AI 的提示词

```text
当前窗口截图显示 v1.0.93 正在强制更新至 v1.0.94，更新说明已包含消息提醒与多端同步修复。先确认更新完成并记录实际运行 v1.0.94，再复验 IM/OA realtime 同步，针对仍可真实复现的问题修复。以下已有异常证据均产生在本次更新完成前，限于移动端操作及桌面缓存/日志，不能直接定性为 v1.0.94 缺陷。以正在运行的安装包、当前日志和当前测试服为准，不要把旧桌面源码当成最新行为，不要输出密码、Token、Cookie、完整 deviceId、消息正文或附件真实地址。

先记录当前窗口 v1.0.93、实际构建标识和缓存归属。用户报告及 latestVersion=1.0.93 不构成主程序构建的独立验证；已检查文件的版本元数据为 1.0.87，同样不能据此否定更新。核对安装包 hash、主程序 hash、构建版本和更新时间，说明版本来源差异。

真实复现：Android 与 Windows 同时登录 Test Terminal 01。Android 在 Test Terminal 03 单聊中发送一个 AI-UAT 文件，移动端已显示“已发送”且 Outbox 已清理，但 Windows 等待 20 秒后 collaboration-cache.sqlite3 仍找不到该消息；im_messages_cache 保持 424 条、最大 sequence 20840、最后更新时间 2026-09-05 23:56:05。

当前 Windows 日志持续出现：
- realtime connection state: service=im, state=retry-wait, reason=transport error，重试次数超过 260；
- im realtime events could not be persisted: collaboration event id is missing；
- OA 也存在相同 transport error 与 event id missing 警告。

请完成：
1. 从登录响应、版本配置和运行时状态核对 IM/OA realtime Gateway 的 host、scheme、port、TLS 与 HTTP/2 配置；验证客户端实际连接的地址，不要只证明公网端口可连。
2. 分别定位 gRPC/realtime transport error 的底层状态码、握手错误和服务器响应，日志需脱敏但必须保留可诊断摘要；连接成功必须进入 ready，并有最近事件时间。
3. 追踪“event id is missing”的事件来源和原始 DTO 映射。服务端协议必须稳定返回 eventId/id、sequence、eventType 和 payload；桌面适配层要兼容实际字段命名，但不能用随机 ID 破坏幂等。
4. 检查是否错误过滤 senderId 为当前账号的事件。同账号移动端发送的消息必须进入 Windows 缓存，并通过 server message id 或 clientMessageId 去重，不能当成“自己发送”而丢弃。
5. 检查事件落库、游标更新、ACK 的顺序：先事务落地 inbox/message，再更新本地游标，最后 ACK；崩溃重放不得重复。
6. realtime 不可用时，确认 REST 长轮询/增量同步能继续推进独立桌面 deviceId 游标；缺少 eventId 的单条坏事件不能导致整个同步循环永久停滞。
7. 修复后重新执行：移动发文字/文件/图片/视频，Windows 实时出现；Windows 发送，移动实时出现；两端已读互清；离线积压恢复无丢失、无重复、顺序正确。

输出：根因、改动文件、协议字段、实际 Gateway 地址的脱敏摘要、失败前后日志、数据库 sequence/游标证据、自动化测试、版本一致性证据及修复后真实安装包回归结果。没有真实运行证据的项目标记未通过。
```
