# 728：OA 时间范围校验与真机草稿回归

日期：2026-09-05。局部通过，完整 OA 流转仍未验收完成。

## 真机发现的问题

test01 从工作台打开当前后台的请假审批 v1。必填空值提交能拦截，选择器、日期和时间使用底部抽屉。但当开始时间是 2026/09/06 01:21、结束时间是 2026/09/05 01:21 时，只提示“请假天数尚未计算，请检查关联字段”。用户看不到应修改哪个日期。

- 初始表单：[01](../test/evidence/oa-form-728/01-form.png)。
- 必填校验：[02](../test/evidence/oa-form-728/02-validation.png)。
- 原错误范围：[03](../test/evidence/oa-form-728/03-invalid-date.png)。

## 最终修正

1. 按后台 `durationStartFieldId`、`durationEndFieldId` 找到关联字段及实际标签，不根据应用名称或固定字段 ID 推断。
2. 结束时间早于或等于开始时间时，清掉旧时长值，在**可编辑的结束时间**上显示“结束时间必须晚于开始时间”。不再把无法手工修改的时长字段标红，也不额外显示泛化的“尚未计算”错误。
3. 日期改正后清除关联错误并重新计算。原自然日与小时计算规则不变；没有修改后台模板、流程或字段定义。
4. 首次补丁仅把文字放在时长下方；真实截图复验后继续改为定位到结束时间。中间截图 04/05 是迭代过程，最终效果以 06/07 为准。

## 自动化与构建

- 新增 6 个 Widget 用例：自然日/小时 × 早于/相等/正常；使用“出发时刻/返程时刻”和自定义字段 ID，证明提示来自配置标签。
- 四个错误范围用例同时检查：旧的 99 时长被清除；错误属于返程输入，只读时长没有错误；改成合法日期后错误消失、自动恢复 2 天或 24 小时。
- 四个错误提示用例在修正前失败；最终 OA 页面回归 92/92。
- 最终完整测试 1339/1339，01:03 完成。修改的生产文件和测试文件定向分析无问题。
- 最终 Profile APK 80.7 MB，arm64/x64，构建 55.2 秒；SHA-256：`1F2BDE4928093F55C799DA6E3B3900BF8F27FEA4A3FAE369AE031FAAE83CE9E6`。

## 最终真机复验

普通包保留数据安装到 dd00d66d，自动恢复 test01。未清除账号或数据库、未改变网络。

- 本轮本地测试草稿事由为 `AI-UAT-20260905-012200-OA-DATE-728`。
- 经待办 → 草稿箱重新打开，草稿字段和事由完整保留。
- 再次选择结束日期 09/05，错误出现在结束时间输入下方，只读天数不标红：[最终错误定位](../test/evidence/oa-form-728/06-end-field-error.png)。
- 改为 09/07 01:21 后显示 2 天、错误消失，显式保存成功：[最终修正与保存](../test/evidence/oa-form-728/07-final-corrected.png)。
- 只读 SQLite 元数据验证同一草稿 ID `e6d827f3-04bc-4591-a641-57812cf90647` 保留，模板 ID `87adcda2-a096-465b-a572-5c49eb6ab68c`，OA Outbox 前后均为空：[元数据](../test/evidence/oa-form-728/08-draft-metadata.json)。未导出草稿明文或令牌。
- 最终进程 28654 的 FATAL EXCEPTION、Unhandled Exception、RenderFlex overflowed 均 0。操作后返回工作台，保留测试草稿供检查。

## 未执行与真实边界

- 当前后台预览为公司总部 v1 的“部门负责人审批”，实际列出宋扬、Test Terminal 01、陆炳、陆炳002 四人会签。这是配置预览证据，不是四人真实审批通过证据，也没有根据姓名猜登录账号。
- 本轮只进行了无效提交拦截、表单选择/计算和本地草稿保存，没有执行合法申请提交、同意、驳回或完整四人会签。当前移动表单无单独的申请标题输入，标题自动生成；因此没有创建无法按 AI-UAT 标题命名的服务端申请，AI-UAT 标识保留在本地草稿事由中。
- 依据 Computer Use 技能检查运行通道后，当前仍缺 node_repl/sky；没有实际操控最新桌面窗口，也没有用旧源码替代最新桌面行为。桌面附件契约仍按 724 待核验。
- 模拟器保持 727 包，本轮新校验只在真机执行；不冒充模拟器已回归 728。
- 复杂金额分支、多岗位/会签/或签的完整实际流转与通知、桌面入站同步缺口、长期性能、实际到期续期仍未完成。
