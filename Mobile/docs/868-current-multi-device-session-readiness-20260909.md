# 868 · 当前多设备会话验收就绪状态

时间：2026-09-09（Asia/Shanghai）

## 当前状态

- `emulator-5556`：Test Terminal 03 会话有效，工作台、IM、OA、通讯录和个人页可用。
- `emulator-5554`：当前停在登录页。
- `emulator-5560`：当前停在登录页。
- realme `dd00d66d`：ADB 在线，但系统处于 Doze/锁屏状态。

四台设备均已覆盖安装同一 Profile APK，安装过程全部返回 `Success`。安装没有导致 `emulator-5556` 的有效会话丢失；另外两台模拟器在本轮开始多端测试时已处于登录页，不能据此判断为本次覆盖安装导致退出。

## 本轮边界

当前只有一个可交互的已登录移动端，无法制造发送端与接收端分离的真实多设备消息及时性样本。没有通过 ADB、命令行、日志或测试文档注入密码，也没有尝试绕过 realme 锁屏。

因此下列项目继续保持未通过，而不是用单设备结果代替：

- M2 替换 M1 的 `session_replaced`；
- 两台移动端同时在线的消息、已读和 OA 状态同步及时性；
- realme 锁屏/Doze 后的后台通知与点击定位。

只要另一台模拟器通过屏幕完成一次测试账号登录，即可继续上述多端验收；不需要重新构建 APK。

