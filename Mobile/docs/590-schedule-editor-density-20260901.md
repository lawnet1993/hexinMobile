# 590 日程编辑抽屉密度整改

## 当前结论

首页继续不展示空日程模块；独立“日程”应用保留桌面端同源个人待办能力。本轮将新增/编辑日程从默认大号表单收敛为移动端紧凑抽屉。

- 抽屉使用根导航、安全区、自定义拖动提示、16dp 标题和关闭入口。
- 标题输入固定 36dp，备注输入固定 64dp，均使用浅灰填充且移除描边。
- 标题校验独立显示，不改变输入框高度。
- 优先级和截止时间入口均为 36dp；日期和时间继续使用向上展开的移动选择抽屉。
- 取消/保存按钮右对齐，保存按钮为 36dp、高度可测且宽度小于 120dp。
- 新增、编辑、完成、恢复及服务端数据结构保持不变。

当前判定：**实现、自动化、静态检查、构建和覆盖安装通过；真机触控复验待正常解锁**。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| 日程定向测试 | 通过 |
| 完整自动化 | 210/210 通过 |
| 静态检查 | `flutter analyze`，0 个问题 |
| Profile APK | 构建成功，75.9 MB |
| SHA-256 | `267D2FB5D67BC69F2EAC547C6B76B861899C91D50C7362ECFBEAA670C44F80DC` |
| realme 覆盖安装 | `adb install -r` 成功 |
| 真机状态 | `Keyguard showing=true`、`mInputRestricted=true` |

## 自动化覆盖

测试实际断言：标题 36dp、备注 64dp、优先级 36dp、截止时间 36dp、保存按钮 36dp且宽度小于 120dp；页面不存在 `AlertDialog`，关闭后无残留抽屉。

## 真机边界

真机解锁后需要从全部应用进入日程，依次打开新增、优先级、日期和时间选择器，检查键盘避让和小屏布局后全部取消。本轮没有新增、编辑或完成任何线上待办。
