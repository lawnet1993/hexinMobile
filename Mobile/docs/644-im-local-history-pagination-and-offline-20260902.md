# 本地历史消息分页与真断网验收

时间：2026-09-02 17:16–17:29（Asia/Shanghai）；网络恢复复核于本轮续验。结论：本地历史分页修复通过，整体 IM 验收仍为部分通过。

## 真实对照

- M1 真机 test01、M2 模拟器 test02；桌面现有 test01 会话只用于 GET 状态核对，没有 Windows 窗口操作证据。
- 仅向既有两人测试群 `AI-UAT-20260902-162326-GROUP` 通过模拟器界面发送 81 条编号文本 `AI-UAT-20260902-172000-CACHE-001` 至 `081`，加上原有 3 条，总计 84 条。未通过 API 发送或数据库灌入消息。
- 旧包冷启动只显示最近 80 条；继续上滑调用服务端历史 GET，返回 405，本机已有的最早 4 条被挡住。
- 新包相同数据、相同操作能显示最初的 @消息、GROUP-LIVE、GROUP-PREVIEW 和 CACHE-001。
- 关闭模拟器 Wi-Fi、移动数据，确认 `Active default network: none`，强制结束进程后冷启动，再次上滑能显示以上最早消息。不是只切应用后台，也不是接口 mock。
- 完成后恢复 Wi-Fi 和移动数据；续验实测设置均为 1，活动默认网络为 101。未更改真机网络。

## 修复边界

1. SQLite 优先读取紧邻当前窗口之前的连续序号缓存，查询最多 80 行、只解密所需消息，不扫描全部历史。
2. 遇到序号缺口不跨过缺口拼接旧缓存，仍由现有历史接口补齐；删除墓碑参与连续性检查，但不重新显示已删除消息。
3. 短缓存页不再自动当作全部历史结束；只有读到序号 1 或真实空页才结束分页。
4. 保留向上滑动加载、滚动锚点和热重开窗口，不增加“加载更早”按钮。

## 证据

目录：[im-cached-history-20260902](../test/evidence/im-cached-history-20260902/)。

| 证据 | 说明 |
| --- | --- |
| 01 | 新包真机单聊缓存回归 |
| 02 | 接收端无群消息缓存，仍显示真实接口错误，没有伪装群消息到达 |
| 03 | 桌面现有会话 GET 核对：群 latestSequence 84、未读 84、lastRead 0、@序号 1；历史 GET 405，群消息创建事件仍缺失 |
| 04–06 | 旧包发送完成、冷启动和上滑失败；失败文字见 06 XML |
| 07–09 | 新包冷启动、向上分页、最初消息可见；以 09 为完整首屏证据，08 是中间滚动位置 |
| 10–12 | 真断网冷启动、向上分页、最初消息可见；以 12 为最终证据，11 是中间位置 |

## 自动化与安装

- 新增单聊/群聊各 173 条加密缓存关闭重开、三段分页且零 HTTP 请求；缓存缺口仍请求并返回真实 mock 405；账号/会话隔离；删除墓碑。页面测试覆盖短缓存页继续加载和序号 1 结束。
- 本次实现最终全量 331/331 测试通过，静态分析 0 问题；本轮续验再次核对两设备安装包散列均为下述新包。
- 旧包 SHA-256：`B97996D5BD35702484C39B3C98BE3BF1E3034576F30350838FBC0E9C5F9405D6`。
- 新包 SHA-256：`E36CA2223E2E78EC32DACA99900AF02347EA5F33FD4FCE4E4B43BB3E99D98978`，Profile arm64/x64，版本 1.0.1 / code 2。
- 性能结论仅限已有缓存翻页消除 HTTP 依赖；未据此宣称 FPS、全量压力测试或精确响应时间达标。

## 未通过 / 未执行

- 新环境 GET 历史消息仍 405，接收端群创建事件仍缺失；本地分页修复不能修复未收到的消息。不能用发送端缓存证明群聊双端同步通过。
- OA 附件上传 HTTP 500、完整高级审批流程、D2 替换及 Windows 窗口对照仍待验收，见 641–643。
- 续验发现消息双勾目前取决于回执权限，而非实际阅读；14 的真实回执为 0/1。此问题另行修复，不纳入本报告的历史分页通过范围。
