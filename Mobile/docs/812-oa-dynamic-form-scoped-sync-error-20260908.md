# OA 动态表单与局部同步异常文案复验（2026-09-08）

## 结论

Android 16 模拟器上的真实请假申请页能够从后台表单 Schema 渲染请假类型、开始/结束时间、只读时长、请假事由和附件；审批流程预览当前因服务端异常无法同步，但表单与草稿仍可继续保留。已将容易误导为手机网络或隧道故障的“网络不可用”改为“审批流程暂时无法同步”，并安装到模拟器和真机。

## 当前页面证据

- 页面：工作台 → 请假审批。
- 真实身份：Test Terminal 02 / 公司总部。
- 表单版本：v1。
- 字段来自服务端 Schema，未把 `readOnly`、`durationUnit`、公式对象或序列化配置显示成业务值。
- 请假类型、时间等选择字段继续使用移动端选择交互；附件入口为紧凑按钮。
- 底部“保存草稿/提交申请”约 36dp 高，不使用桌面大按钮尺寸。
- 流程预览失败只标记OA局部同步状态，不把它等同于设备网络、隧道或登录状态。

证据：`Mobile/test/evidence/real-device-main-pages-20260908/oa-leave-form-scoped-sync-error-current.png`。

## 回归与构建

- OA 动态字段、只读值、公式、附件、流程预览恢复、草稿、审批列表/详情、通知及窄屏选择器：**107/107 通过**。
- Profile APK 构建成功并覆盖安装到 `emulator-5554` 和真机 `dd00d66d`。
- APK：85,792,566 bytes；SHA-256 `A2DFC14F472D13C25F235842AC072B12626770578098099FB580AA00B891379C`。
- `git diff --check` 无空白错误，仅有仓库既有的 Windows LF/CRLF 提示。

## 尚未通过

- 服务端审批流程预览当前不可用，因此不能用本轮页面证明实际处理人、岗位、会签/或签和分支解析正确。
- 真机仍锁屏；新包已安装，但新文案和选择器仍需解锁后补真机实操截图。
