# 602 · OA 待办自动分页与移动端实操

时间：2026-09-01

## 结果

- 待办中的审批列表移除“加载更多”和“加载更多记录”按钮。
- 距列表底部 240dp 时自动读取下一游标；首屏不足一屏或当前筛选页为空时自动继续查找。
- 下一页按申请 ID 合并，避免重复记录；服务端游标不前进时停止请求，避免循环。
- 分页失败保留已显示内容，并暂停自动重试；下一次真实上滑可恢复请求，同时仅在失败状态提供“重新加载”。
- 切换待办分类、搜索或筛选时清理旧分页状态并回到顶部，防止不同视图的数据混入。

## 模拟器实操

- 设备：Android 模拟器，1080×2400。
- 使用隔离 Demo 配置启动，实际点击底部“待办”，检查待我处理、搜索、筛选和列表。
- UI 树确认：存在“待办”“待我处理”“草稿箱”“待同步”和“搜索事项或申请编号”，不存在“加载更多”。
- 实际打开筛选，确认为向上展开的底部抽屉，包含事项类型、审批应用、审批状态和时间范围；未出现居中对话框。
- 应用进程关键错误计数为 0；Debug 首启跳帧不作为性能通过证据。

证据：

- `docs/evidence/602-todos-auto-paging/01-todos.png`
- `docs/evidence/602-todos-auto-paging/01-todos.xml`
- `docs/evidence/602-todos-auto-paging/02-filter-sheet.png`
- `docs/evidence/602-todos-auto-paging/02-filter-sheet.xml`

## 自动化与构建

- OA 页面定向测试：45/45 通过，其中新增：
  - 长列表接近底部后只读取一次下一页；
  - 空首屏自动补齐下一页。
- 全量测试：223/223 通过。
- `flutter analyze`：0 项问题。
- Production Profile APK：79,660,326 bytes。
- SHA-256：`B5A57ED304F7C6E6CBB8CDD8B9E94985A72846FC3FA06C4BA7257F5ED14B2EAF`。
- 已覆盖安装到 realme 真机，设备端 APK 哈希一致。

## 边界与后续

- 当前 Demo 待办数据只有 4 条，模拟器可验证界面和交互，但无法靠现有数据真实触发第二页；第二页请求由可控分页自动化覆盖。
- realme 真机仍处于系统锁屏：`Keyguard showing=true / InputRestricted=true`。未绕过锁屏，待用户正常解锁后补真实账号的上滑、筛选和分页回归。
- 通讯录和群管理仍有手动分页入口，留在后续轮次继续处理。
