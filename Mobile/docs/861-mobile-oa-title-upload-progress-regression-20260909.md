# 861 · 移动端 OA 标题与附件上传进度回归

时间：2026-09-09（Asia/Shanghai）  
范围：Android Flutter 移动端

## 结论

- 新建审批恢复可编辑“申请标题”，不再只能使用客户端自动拼接标题。
- 附件仍使用 `multipart/form-data` 文件流上传；Dio 的真实 `sent/total` 已贯穿 Repository、Outbox 投递和页面提交状态。
- 多附件进度按“当前附件序号 + 当前附件字节进度”折算为整批百分比；页面仅在整数百分比变化时刷新，避免每个网络分片触发重绘。
- 上传开始但尚未获得总字节数时只显示忙碌状态，不显示虚假百分比；失败或结束后清除进度。

## 验证证据

- OA 页面与离线附件定向回归：99/99 通过。
- 草稿生命周期和表单交互回归：19/19 通过。
- 当前移动端全量 Flutter 回归：1434/1434 通过。
- `flutter analyze`：0 issue。
- Profile APK：85,858,102 字节，SHA-256 `D7B957102E82A3A220ABB1CBCC5583CB370BCE760ECD0D6BD28017C940E008E5`。
- APK 已成功覆盖安装至 3 台 Android 模拟器和 1 台 realme 真机。
- `emulator-5556` 冷启动进入已登录工作台；打开请款审批后，原生语义树确认可编辑标题以及后台表单字段、附件入口和审批流程同时存在。
- Android 模拟器通过系统文件选择器选择 19.0 MiB 文件，网络限速为 2000 Kbit/s 后提交；页面真实捕获到“上传 3%”，恢复全速后进入申请详情。
- 服务端产生测试申请 `AI-UAT-OA-UPLOAD-20260909-0100`，详情显示金额 4999、合同付款、付款日期、请款说明、19.0 MiB 附件和“部门负责人审批”待处理节点。
- 运行截图：`test/evidence/oa-upload-progress-20260909/oa-upload-progress.png` 与 `oa-upload-complete.png`。

## 尚未冒充通过的边界

- 真机当前锁屏/Doze 状态下只完成覆盖安装，未绕过锁屏执行表单交互。
- 服务端流程节点 `completionMode`、退回语义等仍以服务端修复和真实流程验收结果为准。
