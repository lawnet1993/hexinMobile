# 762 · Windows 更新路径与跨端同步检查提示词

日期：2026-09-06，Asia/Shanghai。

> 09:40 的当前运行证据与更新后的修复提示词见 [771](771-windows-mobile-event-contract-live-evidence-20260906.md)。当前 Windows IM/OA realtime 从未进入 ready，事件 ID 警告和桌面空游标已经与移动端有效事件 inbox/游标完成对照；以下内容保留较早现场上下文。

下面内容可直接交给负责 Windows 桌面端的 AI。不要在提示词、日志或报告中写入密码、Token、Cookie、设备指纹或完整附件地址。

## 可复制提示词

你是一名高级 Windows/Tauri/Rust 客户端工程师。请在当前测试机真实检查并修复合兴智联 Windows 客户端的更新安装路径和跨端同步，不要只阅读源码推断。

已知现场证据：

- 更新 UI 曾显示当前 1.0.93、目标 1.0.94，但当前实际运行进程为 `C:\Users\86137\AppData\Local\合兴智联\hexing-zhilian.exe`，文件版本和产品版本均为 1.0.87。
- HKCU 卸载注册项也显示 1.0.87，但 `InstallLocation` 指向另一个目录 `C:\Users\86137\AppData\Local\HexingZhilian`。必须查清下载、解包、覆盖、注册项、快捷方式、重启目标是否使用了不同目录。
- test01 移动端能够持续拉取 `GET /api/im/sync/events` 并接收桌面消息；此前“手机发出、桌面不实时显示”的现场运行文件仍是 1.0.87，不能据此宣称 1.0.94 已修复或仍有缺陷。
- 当前移动端使用账号 + 独立安装 ID 的设备游标；每批严格执行 SQLite 事务落地、更新游标、最后 ACK，不过滤当前账号自己从另一设备发送的事件。

请完成：

1. 读取当前进程可执行文件路径、文件版本、产品版本、父进程、快捷方式目标、卸载注册项和更新器日志；确认正在运行的二进制就是更新后的目标文件。
2. 修复更新器目录不一致：下载到临时目录，校验包完整性，退出旧进程，原子替换唯一安装目录，更新注册项/快捷方式，明确从新文件重启；失败必须回滚，不能留下两个可运行副本。
3. 更新完成后重新读取磁盘文件版本和进程路径，不允许只相信前端显示或下载进度。
4. 核对桌面端 IM 地址和网关下发值，特别是 `http://api.sfhkh.com:7273`；记录安全的连接状态、HTTP/gRPC 状态码和请求编号，不记录认证材料。
5. 为桌面设备维护独立游标，键至少包含 accountId + desktopDeviceId；拉取 `afterSequence`、`waitSeconds=25`、`take=500`，满 500 条立即追平。
6. 事件和消息必须先在 SQLite 事务中幂等落地，再更新本地游标，最后 ACK；ACK 前崩溃允许重放，重复事件和重复 ACK 不能产生重复消息。
7. 不得过滤发送者等于当前账号的 `message.created` 或 `conversation.read`，否则手机发出的同账号消息和跨端已读不会出现在桌面。
8. 启动、恢复、网络重连和周期巡检时，对比会话 `lastMessageSequence` 与本地最大序号；发现缺口就分页补拉。索引兜底不能掩盖事件流异常，应记录 eventCursor、indexSequence、补拉数量和耗时。
9. 修复后使用桌面 D1 + 移动 M1 同时登录 test01：手机发 20 条、桌面发 20 条、另一账号向双方发 20 条；核对两端无丢失、无重复、顺序正确、自己的跨端消息不增加未读、任一端阅读后另一端未读清零。
10. 在事件落库后 ACK 前强杀桌面进程，重启后验证数据不重复；断网后累计单聊、群聊和 @我，恢复后验证完整性。

输出报告必须包含：实际运行版本与路径、更新前后注册项、Gateway 实际值、事件游标时间线、每轮消息数量/序号/唯一性、跨端已读、崩溃恢复、请求编号、未执行项。只有实际运行文件确认为目标版本且双向实时同步有证据时，才能判定通过。
