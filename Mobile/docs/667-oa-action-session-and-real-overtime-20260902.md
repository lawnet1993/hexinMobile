# 667 OA 动作会话隔离与真实加班审批

时间：2026-09-02，测试环境 api.sfhkh.com。结论：本轮专项通过，整体目标仍进行中。

## 实现与回归

- 审批列表、详情、同意/驳回、其他动作、抄送已读、流程预览和全部已读固定请求会话；请求前后及缓存提交核对账号、设备与登录凭据，旧会话结果不得影响新登录。
- 通知已读队列按完整会话隔离合并请求；迟到响应不确认/污染队列，401 与 session_replaced 停止本批并保留意图。详情页忽略旧登录异常，不覆盖新登录在线状态。
- 新增 42 项 HTTP/SQLite 专项与 1 项页面回归。原实现 40 项失败；修复后的相关 96 项通过，最终全量 **573/573**，分析 0 问题。
- 中间版本引入的 Future 清理自等待与新增测试容器清理问题均已修正，失败日志保留。最终正常 profile 包 SHA256：`2783428834A566D38D7F6F03328F664FF39FD777728B6630C66582E8CBDA95FF`，仅安装独立 M3，安装后哈希一致。

## 真实操作证据

独立 M3 / emulator-5556 / test03，通过应用页面新建一条加班申请，未使用 API 代替提交或审批。未操作 M1/M2 或 Windows UI。

- 申请编号 `OA-20260902-FE9B58`，ID `fe9b58d5-42de-4632-99be-cd5c70ebe983`。
- 当前表单无标题输入，自动标题为“Test Terminal 03的加班审批”；事由采用 `AI-UAT-20260902-230500-OVERTIME-SESSION` 标记。
- 表单显示 v1，2026-09-03 16:04 至 17:04，自动计算 1.0 小时。申请人与预览解析的部门负责人均为 Test Terminal 03 / 财顺，不根据账号名称推断角色。
- 23:07:38 CN 点击提交，详情为“审批中 / 待你处理”，未提前显示通过。
- 23:08:48 CN 在“同意审批”底部抽屉确认，审批备注 `AI-UAT-20260902-230500-APPROVE`。随后详情“已通过 / 已同意”，不再显示可处理按钮。
- 实际出现提交、待审批、审批结果和考勤回写完成通知。打开审批结果后通知未读 8→7；本地对应回执 `sent`，后续 `oa.notification.read` seq355 已落库。详情有后续服务完成记录；未独立核对考勤业务数据库。
- WiFi/数据均关闭后杀进程冷启动：通知未读 7、申请通过状态及处理记录保留；离线详情明确为本机快照。恢复网络无需手动重试，快照提示消失，最后回到首页。
- 原请假草稿 `61faf8e1-6404-4c34-b2d4-257fd6931949` 与更新时间未改变，新加班草稿提交后自动移除，Outbox 为空。

证据目录：[oa-action-session-20260902](../test/evidence/oa-action-session-20260902)。关键文件：17-final-form、19-after-submit-stable、20-review-sheet、22-after-approve、24-notifications-before-read、26-notifications-after-read、29-cold-offline-detail、30-recovered-detail、31-final-home，及 metadata-final.json。应用事务的 HTTP/关联请求编号未采集，不虚构编号。

## 剩余与新发现

- 自动保存状态条插入导致字段位置跳动；日期/时间抽屉关闭后旧输入框重新唤起键盘；详情长字段标签换行不自然。下一轮处理交互。
- 未验证会签/或签、金额分支、完整公式/权限/推送/并发、自然到期和完整桌面 UI 多端矩阵。
- 其他 OA 写入口、在途写操作停止语义、全部已读与新通知到达竞争仍待审计。既有服务端群读投影、缺失群事件和附件 500 问题未因此关闭。
