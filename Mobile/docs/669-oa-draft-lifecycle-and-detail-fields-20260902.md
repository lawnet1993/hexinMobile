# 669 草稿保存生命周期与审批详情排版

时间：2026-09-02 23:24–23:40 CN。结论：草稿与详情专项通过；收尾实际发生自动续期401，已重新登录恢复，但续期问题未解决，整体目标继续进行。

## 修复内容

- 草稿写入串行化。保存中的新编辑按修订号合并到下一份快照，旧保存结果不再把新输入误标成已保存；保存与失败重试使用稳定草稿 ID。
- 分别显示未保存、保存中、保存失败、已保存/已恢复，继续使用申请人栏内的小状态，不重新插入表单上方状态条。底层保存依赖初始化错误也转为失败状态。
- 返回等待最新草稿写完；重复返回不启动重复保存，失败留在表单并提示重试。提交等待已在途的保存结束，防止提交后旧写入重新产生草稿；重复提交、处理中返回和文件选择期间提交/返回被限制。
- 返回/提交收起输入焦点，进行中的输入区域不接受触摸。页面关闭或账号改变后不继续提交后续本地草稿快照。值采用独立 JSON 快照，附件列表独立拷贝。
- 详情字段按实际文字宽度与字体缩放统一标签列。普通字段继续左右排布，过长标签在窄屏/大字体下使用整行放到数值上方，不在固定84dp窄列中挤断。
- 实际截图发现多行事由误用编码字段缩放规则，补充读取后台 textarea/multiline/richtext/rows 配置，多行内容正常字号换行；原地址类单行展示回归仍通过。

## 自动回归与构建

- 新增6项草稿生命周期测试、4项详情布局矩阵（320/390dp × 1.0/1.6字体缩放，含多行事由）。既有表单位置稳定、键盘、地址显示和业务状态测试一起执行。
- 草稿旧实现首轮4项失败/1项通过，之后加入切号用例；标签布局旧实现4项失败。真实运行新发现的多行事由问题也先补测试复现4项失败，再修复。
- 最终全量 **596/596**，`flutter analyze` **0问题**，目标源文件 `git diff --check` 通过，无黄金图更新。
- 中途测试的动态 provider override 数量用法错误已修正；首次实现将依赖读取放到异常捕获外，已有测试发现后已修正。保留失败日志，未将测试搭建问题算作产品问题。
- 最终正常 profile APK / M3 已安装包 SHA256：`01284D68D3A42A3F80160F9330B88423639075112F25C027181123DB53DFE4D0`。中间 E861…包完成快速返回实测后，仅补多行详情渲染并重新全量回归、构建、安装与截图；早期其他构建不作为最终交付。

## 真实运行

仅操作独立 M3（emulator-5556 / test03 / Android16），未操作M1真机、M2或Windows客户端。当前工具集再次查询仍无可调用的Windows电脑控制工具，本轮不声称桌面UI验收。

1. 打开上一轮本地加班草稿 `2672bb18-7c66-4e25-8e89-17c13a08b7fd`，追加 `_LATEST-232400`。
2. 输入命令结束后紧接着点击页面返回，宿主计时两条命令完成相差约 **91ms**。它不是端到端性能指标，也不是强制慢磁盘并发证据；精确在途竞争由带闸门的widget测试覆盖。
3. 再打开草稿，实际显示完整 `AI-UAT-20260902-232100-FORM-INTERACTION_LATEST-232400`，原开始时间 `2026/09/03 15:22` 保留。SQLite只有同一加班草稿，没有重复ID。
4. 原请假草稿 `61faf8e1-6404-4c34-b2d4-257fd6931949` 更新时间仍为 `2026-09-02T13:53:30.441508Z`，未修改。Outbox为空，原两个通知已读回执仍sent。
5. 打开已读的加班审批结果 `OA-20260902-FE9B58`：状态已通过、开始/结束时间、1.0小时、事由和处理记录均不变。最终截图中“加班时长（小时）”完整单行，事由正常字号分两行；未产生新的审批提交或处理动作。
6. 最后回到首页，通知未读仍7，WiFi=1、数据=1、默认网络130。本轮未关闭网络；上一轮断网草稿恢复证据仍保留，未冒称本轮又做了一次断网验收。

证据：[oa-draft-lifecycle-20260902](../test/evidence/oa-draft-lifecycle-20260902)。关键文件：before.log、layout-before.log、multiline-before.log、full-tests-delivery.log、analyze-delivery.log、build-delivery.log；03-detail-before.png、07-reopened-latest.png、13-detail-delivery.png及对应XML、rapid-back-timing.json、metadata-final.json、15-final-home.xml。

## 仍未完成

### 收尾新增证据：自然时间推进后的续期拒绝

- 23:38:25 CN，最终正常包的真实日志出现 `MOBILE_SESSION_REFRESH {status:401,errorCode:null,action:rejected}`，随后 `MOBILE_SESSION_AUTH {source:heartbeat,status:401,action:terminated}`。页面实际回到登录，提示“登录已失效或已到期，请重新登录”。没有人工触发刷新探针、改动时钟或注销。
- 660轮到期前真实刷新响应为200，更新后令牌未验证时间提示 exp 为15:38:19Z；本轮事件约在该时间6秒后，时间相符但不是服务端拒绝原因的证明。
- 当前刷新客户端是独立Dio，没有旧Authorization拦截器，仍发送 `/connect/token` 的 refresh_token 授权。不能直接把401归因于客户端携带旧Bearer，也不能证明是刷新令牌过期或服务端策略；响应仅有白名单摘要，无具体错误码。
- 安全日志和登录页证据：16-session-events.log、16-session-ended.xml/png。**自动无感续期不能判通过**，下一轮应优先核对当前服务端/桌面续期协议。
- 23:40通过真实登录页点击一次登录，使用应用安全保存的test03凭据，未导出/重新写入密码。18-reauth-ready.xml确认回到Test Terminal 03首页、未读7；metadata-after-reauth.json确认两条原草稿ID与时间、已读回执、空Outbox仍保持。本轮最终停留已登录首页，不是16截图的失效页。

- 真机最终包更新、完整Windows UI对照、多台同平台替换矩阵、系统推送、自然到期续期、完整性能与高级OA验收尚未全部完成。
- 草稿存储层所有跨登录写路径、模板热更新时的草稿兼容、审批成功后的其他本地清理失败恢复，仍需进一步独立审计。本轮测试不证明这些路径。
- 既有服务端群已读投影、缺失群事件与附件500仍待解决，不因本轮通过而关闭。
