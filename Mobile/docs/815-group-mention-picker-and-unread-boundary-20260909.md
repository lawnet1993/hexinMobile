# 815 · 群提及成员与未读边界（2026-09-09）

## 本轮结论

- 群聊“提及成员”底部面板此前仍在姓名和部门后显示终端账号，与移动端隐藏账号的既定规则不一致；已修复为只显示姓名和部门。
- 搜索仍可在数据层匹配账号，方便精确查找，但搜索提示和成员行不再向普通用户暴露账号。
- Test Terminal 03 向 Test Terminal 01 发送的结构化 `@` 消息已进入真机群摘要，本地消息记录也包含非空 mention 元数据；但本次 `@我` 未读验收被同账号的另一端并发活动污染，不能判定通过或失败。

## 代码改造

- `chat_page.dart`
  - 搜索提示从“搜索姓名、部门或账号”改为“搜索姓名或部门”。
  - 大群分页提示从“请输入姓名或账号继续查找”改为“请输入姓名或部门继续查找”。
  - 成员副标题只显示部门；没有部门时显示“群成员”，不显示 `username`。
- `chat_page_type_test.dart`
  - 增加群提及抽屉不出现当前成员账号的回归断言。

## 自动回归与构建

- `flutter test test/chat_page_type_test.dart`：97/97 通过。
- Android Profile APK 构建通过。
- APK：85,792,566 bytes。
- SHA-256：`C6DA17C09675CBEDAFC20710D5602AEC42FE1C6AFF6CF1AB5F5CA7348C18D889`。
- 新包已覆盖安装 `emulator-5556` 与 realme 真机，均保留账号数据。

## 当前包真实界面

Test Terminal 03 在更新后的群聊提及抽屉中：

- Test Terminal 01 行显示“公司总部”，不再显示账号。
- 搜索框显示“搜索姓名或部门”。
- 抽屉高度、50 px 成员行和移动端底部展开方式保持不变。

[当前提及成员抽屉](../test/evidence/multi-device-sync-20260908/09-mention-picker-account-hidden.png)

## `@我` 样本为何未判定

1. Test Terminal 03 通过成员选择器选中 Test Terminal 01，并发送唯一结构化提及。
2. 真机收到事件后，群摘要显示该消息；当前环境数据库中对应消息的 mention 密文长度明显不同于空 mention，证明结构化元数据已落地，不是仅发送可见文本。
3. 随后约 41 秒内，同一群又出现一条由 Test Terminal 01 同账号发出的消息；会话 `lastReadSequence` 同时推进到新的最后序号，`unread_mention_sequences` 被清空。
4. 桌面 v1.0.105 当前也登录 Test Terminal 01，且本轮无法控制其窗口确认是否打开或发送了该群消息。因此不能区分“另一端真实阅读后跨端清零”和“服务端错误推进已读”。

下一次 `@我` 验收必须使用没有其他客户端登录的独立接收账号，或先取得桌面窗口控制，记录提及事件、显式 `/read` 请求及 `conversation.read` 事件的时间线。当前样本不作为移动端缺陷，也不作为通过证据。

