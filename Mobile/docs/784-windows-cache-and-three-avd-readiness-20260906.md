# 784 · Windows 缓存投递与三 AVD 压测准备

> 日期：2026-09-06，Asia/Shanghai。结论：当前 Windows v1.0.87 样本中，移动端发送后的服务端会话序号与桌面本地缓存均由 261 推进到 262，未复现“移动发送、桌面缓存不更新”；桌面可见 UI 与最新 v1.0.94 仍未验收。三台 Android AVD 已恢复为 test02/test03/test04 独立账号拓扑，准备继续并发、及时性与离线补偿测试。

## Windows 样本

- 实际运行文件版本为 v1.0.87，不是用户此前截图中的 v1.0.94，因此不能代替最新桌面端验收。
- test01 会话的 IM、OA bootstrap 均返回 200。
- test03 通过真实移动端输入框发送一条 `AI-UAT-` 消息后，服务端会话 `lastMessageSequence` 从 261 变为 262，未读从 145 变为 146。
- Windows 本地 `collaboration-cache.sqlite3` 同步推进到 262，`quick_check=ok`，窗口消息数保持 200。
- 当前电脑控制接口没有暴露本机原生“合兴智联”窗口，只能证明服务端与本地缓存投递，不能声称桌面 UI 已显示。

## 三 AVD 准备

- `emulator-5554`：test02；旧会话到期后使用已安全保存的记住登录恢复成功。
- `emulator-5556`：test03；在线。
- `emulator-5558`：test04；在线。
- 三台均为 `1.0.1+2`，安装包 SHA-256 为 `C31AAD0029F83EA55F5010ED073A2319A90ED39B8DFBEE84CCAE436742663ADA`。
- 启动第三台前宿主机仅余约 3.7 GiB；停止构建完成后遗留的 Gradle daemon 后回升到约 5.8 GiB。第三台以 1 GiB、无窗口、宿主 GPU 方式启动，三台运行且 App 拉起后仍保留约 4.3 GiB 可用内存。
- 本轮不会继续盲目增加第四台本地 AVD；GUI 多实例用于端到端行为，不用于外推服务端容量上限。

## 当前判定

- 移动端到 Windows v1.0.87 的服务端/缓存投递：本样本通过。
- Windows 最新版本 UI 可见性、未打开会话后台刷新：未通过，需 v1.0.94 可控窗口复验。
- 三 AVD 并发环境：已就绪；后续继续执行同群并发、单聊及时性、强停离线追赶、去重/顺序、已读和稳定性。
- 自动登录：保存凭据可恢复，但旧会话仍实际到期，本轮不判无感续期通过。
