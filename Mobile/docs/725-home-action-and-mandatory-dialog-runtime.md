# 725 首页操作按钮与强制更新居中弹窗

时间：2026-09-05 00:42–00:49（Asia/Shanghai）。本轮局部回归通过，整体 IM/OA 对齐目标未完成。

## 本轮交付

- 首页“去处理”可见尺寸 60×30dp，字体 12、细浅色边框；保留 60×48dp 点击区域。仍进入原审批详情，不修改业务状态。
- 按用户最新选择，**强制更新改为根导航居中 Dialog**，最大宽 360dp、外边距 24、圆角 16、紧凑下载按钮；不能通过返回键、遮罩关闭，没有关闭或稍后入口。
- 普通可选更新仍使用可关闭底部抽屉；OA 选择器保持既有交互。
- 长版本信息允许换行，长更新说明可滚动；窄屏、横屏、大字体保持下载入口可达。
- 根据“先不做内置”的指示，移除本轮新增且未交付的两个附件预览实验 Dart 文件及 pdfrx/video_player 直接依赖，pub get 移除 16 个不再需要的依赖。保留既有 just_audio 与原附件打开方式。后台/桌面预览协议仍待核验，见 724；未把未验证 Office 路由带入安装包。

## 自动化与构建

- `workbench_action_size_test.dart`：实际 Material 60×30、触控 60×48，点击上方透明区域仍进入目标审批。
- `client_update_sheet_test.dart`：4/4。含默认屏幕居中位置、360 宽、遮罩/返回不可关闭、下载回调、普通更新稍后关闭，以及 320×640/640×320 的 1.5 倍文字和长说明。
- 两张受影响视觉基线 02-workbench、13-mandatory-update 已逐张查看并更新。首次全量只因强制更新背景中的首页按钮差异失败（1276 通过、1 失败）；更新背景基线后 1277/1277。随后用户要求居中弹窗，新增两个尺寸用例后，最终 **1279/1279**。
- 最终 `flutter analyze` 无问题。正常 `lib/main.dart` Profile 双架构包构建成功，46.2 秒、80.7 MB。
- SHA256：`C1FE894B5AEBBB087EF9DDC2AC6EEF417F40BA0C4B8E6259C27533D64B5669F4`。
- secure_tunnel 的未来 Kotlin 插件兼容警告仍存在，本次没有导致构建失败。

## 真机操作证据

realme RMX3366 保留数据 `adb install -r` 成功；00:47:48 更新。未卸载、清数据、切换账号或关闭热点。

1. 安装前首页为 Test Terminal 01，有 3 条待处理；[安装前](../test/evidence/mobile-home-725/01-before.png)。
2. 新包启动后自动恢复 test01，无需再次输入凭据；首页三项原审批仍在，[安装后](../test/evidence/mobile-home-725/03-after.png)。
3. 第一项按钮触控边界由 `[810,995][1008,1139]` 变为 `[828,995][1008,1139]`，宽从 66dp 收到 60dp、高仍 48dp。实际点击 `(918,1001)`（按钮可见背景上方的透明触控区），进入原申请 `OA-20260904-FE90A4` 的审批详情，[详情](../test/evidence/mobile-home-725/04-detail.png)。随后返回首页，没有点击同意、驳回或更改申请。
4. 当前进程 9272 范围内日志检查：FATAL EXCEPTION、Unhandled Exception、RenderFlex overflowed 均 0。
5. 只读 SQLite 快照前后均 integrity=ok；Test03 单聊原 15 条消息的服务端 ID/clientMessageId/sequence 全部保留、Outbox=0、对方已读序号 15。证据：[升级前](../test/evidence/mobile-home-725/02-before-im-metadata.json)、[升级后](../test/evidence/mobile-home-725/05-after-im-metadata.json)。

## 未执行与后续

- 居中强制更新在 widget/视觉测试中验证；本轮没有通过线上强制发布真实触发，也没有下载替换 APK。不能把测试 fixture 当成真实服务器更新验收。
- 仅真机安装本轮包，M3 保持此前 723 包；未把其旧包运行算成本轮弹窗验证。
- 本轮未新增消息发送或注入 ACK 网络故障；相关本机事务测试见 723，桌面缺消息 P1 仍见 720。
- 现有视觉测试首页背景仍存在字体子集缺字方框，实拍首页中文正常；本次只确认弹窗本体与按钮变化，不宣称整个视觉测试字体问题已修复。
- 附件 Office 预览协议、完整多端同步、高级 OA、长时会话及整体性能仍待完成。

## 背景配色补记

用户进一步反馈弹窗背景奇怪。强制更新浅色模式表面明确改为白色，关闭 Material 表面 tint；暗色模式保留深色表面避免白字白底。遮罩明确使用 `0x52000000`（约 32% 黑色），更新说明保留浅灰底。未改变版本判断、下载行为与强制关闭限制。

新增颜色断言；当前更新交互和全部视觉测试 21/21、分析无问题，已查看新 13-mandatory-update 基线。此前 1279/1279 是配色补记前的完整回归，本次配色使用定向回归，不冒充重新跑完全量。

配色包正常 Profile 构建 51.4 秒、80.7 MB；SHA256 `6B9B0CD9512A6D1F6E14519E594BECCCF45C5C44AF582137A56DF4DEC9297129`，真机保留数据安装成功并启动。强制更新仍未线上真实触发，颜色结论来自视觉与组件测试。
