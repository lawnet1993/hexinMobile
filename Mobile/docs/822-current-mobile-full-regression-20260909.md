# 822 · 当前移动端全量可信回归

时间：2026-09-09（Asia/Shanghai）  
项目：SecureAccess Flutter Mobile

## 结论

当前工作树在无多模拟器资源争用条件下完成全量 Flutter 回归：**1438/1438 通过**，最终构建对应代码最近一次完整运行耗时约 1 分 16 秒，相关改动静态检查为 **0 issue**。本次基线已包含 OA 附件 SHA-256 流式校验、同尺寸损坏缓存重取、未知格式端内打开保护、安装包版本单一来源、聊天页紧凑标题栏、普通和路由自持控制器搜索框的无障碍用途语义、通讯录/成员选择器隐藏账号、群成员目录使用面向人员的搜索提示、图片原生编码失败的逐级降级及明确提示、头像裁剪临时文件清理和裁剪前原生采样、Presence 汇总诊断隐私白名单、聊天多图逐张本机压缩并增量写入加密 Outbox、OA 图片压缩后立即写入加密草稿存储、无扩展名图片文件头识别、页面临时附件生命周期清理、IM/OA 强杀残留临时加密文件启动清理、图片压缩强杀原图缓存的非阻塞启动清理、单聊跨设备旧发送者标识的头像视觉聚合、同名登录设备可识别撤销确认，以及 IM 主动重试期间的真实同步状态用例。

这次结果替代 2026-09-06 多模拟器并行运行时被本机资源拥塞污染的超时样本，可作为当前 Android 客户端代码基线证据；它不能替代尚未执行的服务端、桌面可见窗口、厂商推送和 iOS 真机验收。

## 覆盖重点

- 登录、主动续期、心跳、独立安装 ID、`401/409 session_replaced` 和账号切换隔离。
- IM 事件落库后 ACK、崩溃重放、跨端已读投影、Outbox FIFO、单聊/群聊隔离、历史消息自动分页、510/1000 事件窗口和媒体消息。
- 联系人 2000 人虚拟化、组织折叠、真实 Presence 生命周期和联系人进入会话防重复导航。
- OA 动态表单、精确公式、草稿/命令队列、详情刷新、同意/驳回/撤回/转交/加签/退回的会话与任务版本保护。
- 附件分片预览、下载进度、加密暂存、文件流上传、图片本机压缩、自适应采样解码、外部预览缓存回收及同一对象二次打开免下载。
- 工作台、全部应用、通知、待办、通讯录、个人页、强制更新居中弹窗及紧凑按钮/输入框布局。
- Golden 基准 13 个主要页面通过。

## 当前构建

- Profile APK：约 81.9 MiB。
- SHA-256：`EC15C44654FEF019374461ABFEA4ACC280869396A237DD1E9F58065FF704EA81`。
- 当前最终工作树全量回归为 **1438/1438**；新增无扩展名图片识别覆盖后保持全部通过，未通过放宽断言规避问题。
- OA 新建申请已恢复与桌面端一致的可编辑“申请标题”，Android 模拟器安装后原生语义树确认标题、金额、类型、日期、说明、附件与审批流程均来自当前表单页面。
- OA 附件上传现透传真实字节进度；提交按钮按整批附件显示“上传 N%”，更新按整数百分比节流，上传前不伪造进度。
- Android 模拟器已用 19.0 MiB 文件在 2000 Kbit/s 限速下真实提交，捕获“上传 3%”并在恢复全速后进入 `AI-UAT-OA-UPLOAD-20260909-0100` 申请详情；证据见 [861](861-mobile-oa-title-upload-progress-regression-20260909.md)。
- Android 16 模拟器已通过前台断网/恢复：账户与安全页显示“已登录·同步中断”而非隧道或笼统“网络不可用”，网络恢复后自动回到“已登录”，心跳 204 且 IM/OA 健康队列归零；证据见 [862](862-mobile-account-security-offline-recovery-20260909.md)。
- 已覆盖安装 realme 真机并完成图片、联系人、会话、个人页、账户安全、登录设备与审批详情复验。
- 真机单聊标题完整显示，收藏、搜索、更多三个操作保持 44dp 点击区域和 20dp 图标，不再挤压会话名称。
- 真机通讯录默认保持组织折叠；展开后只显示姓名、本人标记和真实在线/最近上线状态，原生布局树不包含登录账号。
- 真机冷启动进入账户安全页显示“已登录·在线”；重试状态不再把正在进行的长轮询误报为“同步中断”。
- 真机单聊中同一远端联系人 5 分钟内的连续消息只在第一条显示头像；群聊仍严格按发送者 ID 分组，不混淆成员。
- Android 模拟器已通过头像相册选择、圆形裁剪、本机压缩、上传回显、恢复原头像及裁剪缓存无残留复验，见 [845](845-mobile-avatar-local-processing-upload-restore-20260909.md)。
- 当前测试服两次通讯录 Presence 响应均为 4/4 状态明确，移动端展开列表与 2 人在线汇总一致且无“状态未知”；诊断仅输出匿名汇总，见 [847](847-mobile-contact-presence-authoritative-runtime-20260909.md)。
- 聊天多图已改为逐张本机压缩并立即加密写入同一消息的 Outbox，避免同时保留最多 9 张压缩结果；失败批次会清理已暂存文件，见 [848](848-mobile-chat-multi-image-incremental-outbox-20260909.md)。
- OA 图片压缩后会立即写入账号隔离的加密草稿存储，页面仅保留小预览和元数据，避免连续添加多图时主体字节累积，见 [849](849-mobile-oa-image-immediate-encrypted-staging-20260909.md)。
- 新建页面拥有但尚未被草稿或 Outbox 引用的加密附件，会在删除、单选替换、退出、提交成功或可靠转入待同步后清理，见 [850](850-mobile-oa-ephemeral-attachment-cleanup-20260909.md)。
- 头像原图在进入裁剪器前先做最长边 1600 px 的原生采样；6000×4000 样本的 PSS 峰值增量由约 104 MB 降至约 24 MB，见 [851](851-mobile-avatar-native-path-memory-20260909.md)。
- OA 动态附件字段已真实达到 20/20：添加按钮按后台 `maxCount` 直接禁用，40 个加密主体/预览文件删除后归零；全量回归仍为 1427/1427，见 [852](852-mobile-oa-20-attachment-limit-runtime-20260909.md)。
- OA 19 MiB 附件在提交后 350 ms 强杀并重启，草稿及附件完整恢复；同时修复了强杀遗留的孤立 `.imq.tmp`，当前账号启动清理实测通过，见 [853](853-mobile-oa-submit-kill-temp-cleanup-20260909.md)。
- 图片压缩过程中被系统杀死后可能遗留的 `mobile-image-source-*` 私有原图目录，现于下次启动首帧后异步清理；同时通过一分钟启动宽限期和活跃压缩目录登记避免并发误删。真实冷启动 `runAppReturned` 为 2.9 ms、首个 Flutter 帧约 249 ms，目标目录删除且无关缓存保留，见 [858](858-mobile-image-process-death-cache-cleanup-20260909.md)。
- JPEG、PNG、WebP、HEIC 和 HEIF 统一走本机照片压缩；GIF、SVG、普通文件、音频和视频保持原文件流，避免动画丢失或矢量图被栅格化。全量回归 1432/1432，见 [859](859-mobile-compressible-image-upload-policy-20260909.md)。
- IM 原始 GIF/SVG 图片改为逐张流式加密并保持图片消息语义；测试服 GIF 发送、回传和动画播放已在 Android 模拟器通过，见 [860](860-mobile-im-original-image-stream-runtime-20260909.md)。
- OA 未知格式附件不再以 `application/octet-stream` 误拉起 Android 错误关联应用；附件行直接标记“请在桌面端查看”并使用桌面图标，真实 `.bin` 点击已在模拟器确认给出完整原因且没有产生外部 Activity 启动，见 [864](864-mobile-oa-attachment-format-open-guard-20260909.md)。
- “我的”和账户安全页保持移动端职责边界；修改密码抽屉键盘弹出后仍完整可见，通知同步文案改为“应用内实时同步”，与待接入的厂商后台推送明确分离，见 [865](865-mobile-profile-security-notification-truthful-state-20260909.md)。
- Android 16 模拟器已真实关闭系统通知权限、通过移动端进入对应系统设置、重新开启并直接返回；页面无需重启便由“已关闭”刷新为“已开启”，且应用内事件同步保持独立正常，证据并入 [865](865-mobile-profile-security-notification-truthful-state-20260909.md)。
- 通知页前台断网时仅显示“连接恢复后同步”，系统通知权限及登录状态不受影响；恢复网络后自动回到“应用内实时同步”，避免把连接、系统权限和身份状态混为一谈，证据并入 [865](865-mobile-profile-security-notification-truthful-state-20260909.md)。
- 登录设备页遇到多个同名 Windows 设备时，撤销确认会同时显示对应的最近活动时间，并与列表统一使用本地化设备名称且不暴露完整设备 ID；Android 模拟器真实页面只打开并取消确认，未改变现有会话，见 [866](866-mobile-login-device-identifiable-revoke-20260909.md)。
- 同一最终构建已重新巡检工作台、消息、待办、通讯录和我的五个主页面；站点入口保持桌面端边界、单聊/群聊行保持分离、通讯录默认组织折叠且不显示账号，见 [867](867-current-build-five-main-pages-runtime-audit-20260909.md)。
- 当前四台 Android 设备均已安装同一 APK，但只有 `emulator-5556` 保持可交互登录；其余两台模拟器在登录页、realme 处于锁屏 Doze，因此没有伪造多设备及时性结果，见 [868](868-current-multi-device-session-readiness-20260909.md)。
- 无扩展名或未知扩展名的 Provider 图片现在通过最多 512 字节文件头识别；静态照片继续本机压缩，GIF/SVG 保持原始文件流，聊天与 OA 两条选择链路均已接入，见 [869](869-mobile-extensionless-image-local-processing-20260909.md)。
- 当前测试服请假审批 v1 已重新打开核对：动态字段与审批流程来自发布 Schema，原始“自动计算、只读、单位天”等配置没有暴露为业务值，见 [870](870-current-oa-dynamic-form-configuration-text-runtime-20260909.md)。
- 当前目标完成门槛已按最终代码、设备状态和外部契约重新审计；Android 客户端自身为当前可验收基线，但多登录端、Windows 可见窗口、服务端契约、厂商推送和 iOS 仍阻止总体通过，见 [871](871-current-goal-completion-audit-20260909.md)。

## 不包含的通过声明

- 后台/锁屏厂商推送与通知点击定位。
- 服务端错误已读、群事件长尾、OA `return` 权限与节点 `completionMode`。
- 当前 Windows v1.0.105 可见窗口逐项操作对照；Computer Use 初始化在重试及重置后仍因 kernel assets 路径缺失而失败，且运行 shell 的 PE 元数据仍为 1.0.87，版本层次需分开记录，见 [863](863-windows-current-baseline-observation-blocker-20260909.md)。
- iOS 真机键盘、媒体、附件、通知和 ImageIO 大图内存。
- M2 替换 M1、设备撤销和改密后的真实多会话失效。
