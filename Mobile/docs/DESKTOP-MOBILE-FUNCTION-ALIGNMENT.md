# 桌面端与移动端功能对齐清单

更新：2026-09-05

最新界面修正见 [735 强制更新弹窗背景](735-mandatory-update-dialog-background.md)：按用户反馈把居中强制更新弹窗改为纯白扁平表面和轻量中性遮罩，去掉弹窗外边框、阴影以及更新说明的灰底内框；普通更新仍为底部抽屉，强制策略和文案未变。专项4/4、更新页黄金图1/1，含横屏和150%字体。线上触发仍由后台版本配置决定。

当前 IM 双端复验见 [733 移动端双向单聊与桌面入站缺口](733-current-mobile-direct-sync-and-desktop-gap.md)：test01 真机与 test03 模拟器在同一 direct 会话真实互发序号16/17，约3.8秒/3.0秒可见；双方消息身份、已读17、未读0、Outbox0一致，双端冷启动后持久化，614条群聊未混入。服务端也为17且事件2332/2334/2336/2338齐全，但当前Windows缓存仍停15；旧8/10/13虽已被历史路径补齐，实时链仍故障。已保留现场并补给桌面AI的增量修复提示，不以重启/重登/打开会话补齐掩盖。

当前群聊与 @我 复验见 [734 群聊、@我与冷启动持久化](734-current-group-mentions-and-cold-persistence.md)：test01 真机与 test03 模拟器在真实 group 会话双向同步到序号16；有效提及在群组列表产生未读1及 SQLite 提及序列 `[15]`，打开后显示首条未读分隔并持久化清零。双端消息身份、已读16、Outbox0一致，冷启动保持；服务端摘要为16/已读16，test03服务端在线为真。失败的首条ADB录入作为无效记录保留并排除，连续头像实际已只显示在第一条，因此未修改正确逻辑。

最新 OA 真实链路见 [732 跨部门转交、终态与通知实测](732-oa-cross-department-transfer-runtime.md)：M3 发起并真实转交给公司总部的 test01，真机接收后同意；服务端、两端终态和处理轨迹一致。M3 可见详情约 18 秒由事件自动更新，通知点击后未读 39→38、回执发送成功，冷启动仍持久化。新增转交终态的事件/兜底两项回归，文件 14/14、全量 1361/1361、分析 0。发现同一申请在 GMT/UTC+8 设备生成不同编号的 P2 协议缺口，已给桌面/服务端修正提示；本轮未用移动端固定时区掩盖。

最新表单修正见 [731 后台只读配置执行](731-oa-schema-readonly-controls.md)：选择器/勾选框/日期及附件共 10 类补齐只读行为，20 新用例先复现后修正，全量 1359/1359；普通包已保留数据安装真机与 M3，原草稿和可编辑抽屉回归正常。当前线上模板无显式只读选择器，本轮不把受控测试当成线上配置真实触发。

最新桌面复核见 [730 入站同步复核与交接提示词](730-desktop-inbound-sync-recheck-and-prompt.md)：原桌面进程/会话仍在，GET 200；服务端及两个移动端完整 1–15，桌面缺 8、10、13。13 来自 test03，不能仅归因于同账号过滤。已补只读取证脚本和可交给桌面 AI 的具体提示词；P1 未解决，不重登/清库掩盖现场。

最新性能取证见 [729 联系人进入会话与大群历史实测](729-chat-open-and-large-history-runtime.md)：真机六次进入 15 条单聊，首轮布局 122ms、五次缓存命中重开 65–97ms；M3 真实滑动既有 614 条群历史至首条并重新打开最新位置，专项 32/32。仍有少量超预算帧；不外推为整体性能通过，不冒充远程分页或新版桌面同步已完成。

最新 OA 真机修正见 [728 时间范围校验](728-oa-duration-field-validation-runtime.md)：结束时间不晚于开始时间时，具体错误定位到后台配置的结束字段，不再误标只读时长；改正后自动恢复计算。新增 6 项，OA 页面 92/92、全量 1339/1339，真机已保留数据升级并完成草稿恢复/错误定位/改正保存。当前四人会签仅核对了后台预览，尚未真实完整流转；M3 仍为 727。

最新媒体隔离见 [727 媒体缓存账号/环境隔离](727-media-cache-account-environment-guard.md)：旧账号迟到响应、摘要更新、视频封面命名空间及续期保留缓存，新增 16 项，全量 1333/1333、本轮文件分析无问题。真机和 M3 均已保留数据升级，真实图片/群视频封面、冷热重开及原 15 条单聊账本核对通过；点击视频仍为系统播放器，不冒充端内播放。未完成项与非量化性能边界见报告。

最新附件隔离见 [726 旧附件请求与导航隔离](726-oa-attachment-session-and-navigation-guard.md)：原件/缩略图/图片预览迟到会话响应 12 项先失败后修正，新增共 38 项，全量 1317/1317、分析 0。普通包已保留数据安装真机，原 TXT 审批附件真实进入系统打开方式并取消，未上传第三方；重复点击、页面离开与换账号边界有受控测试。内置 Office 方案仍暂停，长期附件缓存回收与全部异步缓存写入仍待审计。

最新局部交付见 [725 首页按钮与强制更新居中弹窗](725-home-action-and-mandatory-dialog-runtime.md)：首页按钮可见 60×30、触控 60×48；强制更新按用户最新选择改居中、不可返回/遮罩关闭，普通更新仍底部抽屉。1279/1279、分析 0，普通包已保留数据安装真机，真实点击透明触控区进入原审批、IM 原 15 条及回执保留。强制更新尚未线上触发。附件内置实验已撤下，先等 [724 桌面预览核验](724-desktop-oa-attachment-preview-handoff.md)。同步提交后发布与 ACK 解耦见 [723](723-im-commit-before-ack-ui-publication.md)，不等于桌面入站缺口已解决。

移动实际接收验证见 [721 可见已读与回执](721-mobile-visible-read-and-receipt-runtime.md)：M3 未读 5→打开后 0，真机收到回执；M3 实际 UI 反向发送序号 13，双方唯一落库、真机可见已读、M3 双勾，冷启动保留原 13 条与回执。未证明所有事件即时推送，桌面缺消息仍未修复。给桌面排查方的实际实现说明见 [722 移动同步说明](722-mobile-im-sync-handoff-for-desktop.md)，明确长轮询主通道及 12 秒可见会话补偿，不宣称已接入新版 Push Gateway。

最新双端阻塞见 [720 桌面入站同步缺口](720-desktop-inbound-sync-gap-20260905.md)：**P1，手机 → 桌面不通过**。同账号 test01 的服务端/手机均有序号 1–12，桌面缓存缺 8、10；原手机消息及事件可由桌面会话只读 GET 200 获取。问题不止 UI 重绘，需核对新版入站事件与补偿；未重登、清缓存或修改旧桌面源码来掩盖现场。桌面 → 手机用户确认可见。

最新单聊运行证据见 [719 移动单聊与新版协议缺口](719-im-runtime-and-push-protocol-gap-20260905.md)：真机 test01 向 Test Terminal 03 实际发送一条测试消息，服务端/SQLite ID 一致，自己的未读为 0、Outbox 0；冷启动后单条消息和 8 条历史顺序保留，交叉检查 8/8。公开接口仍返回有效 IM 同步结果，但新 Push Gateway 契约与 Windows UI 实时收发尚未验证，不能据此宣称新版双端对齐完成。

最新按钮收紧见 [718 紧凑操作按钮](718-compact-action-controls-20260905.md)：审批发起/详情与常用抽屉采用共享样式，可见按钮高 34、触控区高 48，去除装饰图标和横向撑满。全量 1272/1272、分析 0、普通包已保留数据安装真机；真实表单/日期抽屉截图及透明留白点击验证通过。后续页面继续排查大按钮，完整新版同步协议与多端验收仍未完成。

最新基准核对见 [717 只读表单与桌面版本](717-readonly-validation-and-desktop-release-baseline.md)：用户运行窗口已确认 v1.0.91，旧 EXE 元数据不能否定更新。发布说明涉及统一 Push Gateway 和 ACK 断线补偿，协议仍待核对。移动端只读必填缺值提示已修正并随 718 普通包安装真机，真实空表单验证通过；717 当时专项 102/102、全量 1269/1269、分析 0。

最新真实 OA 回归见 [716 真机撤回、再次发起与部分会签](716-phone-oa-withdraw-restart-cosign-20260904.md)：普通 715 包实际创建两条独立测试请假，原申请撤回后再次发起生成新 ID；新申请一人同意仍等待其余 3 人，最终均撤回且历史保留。通知 50→49、服务端已读及 SQLite sent、冷启动已验证，证据检查 8/8。发现 P2 服务端取消通知把“撤回”误写成“其他处理人完成”，因此部分通过；完整高级 OA 与多端仍未完成。

最新上线时间回归见 [715 上线默认时间与真机状态一致性](715-presence-default-time-and-phone-20260904.md)：真机缓存确认 5 人为 epoch-zero，被错误显示为“最近上线 01-01”；成员解析和新旧状态投影过滤默认时间，保留真实历史与在线/离线/未知依据。新增 12 项、专项 44/44、全量 1267/1267、分析 0；普通包已安装真机，通讯录 5 人与单聊状态复验一致。长时续期、桌面完整多端、高级 OA 等仍未完成。

最新心跳回归见 [714 心跳生命周期与真机前台恢复](714-presence-lifecycle-and-phone-resume-20260904.md)：复现并修复旧维护阻塞重启、双定时器、迟到终止及销毁后重启等 6 项问题。新增 13 项、专项 45/45、全量 1255/1255、分析 0；新普通包已保留数据安装真机，三次返回前台、组织折叠到头像进入单聊、冷启动实际验证。真机长时续期、桌面对照、完整多端与高级 OA 未完成；联系人“最近上线 01-01”待核对原值。

最新资料会话回归见 [713 个人资料会话隔离与真机回归](713-profile-session-isolation-20260904.md)：资料读写与 bootstrap 固定原始会话，同账号续期保留编辑草稿，切账号清除旧表单，迟到请求/选图不得影响新会话。新增 49 项，专项 79/79、全量 1242/1242、分析 0；普通包已保留数据安装真机，真实保存并恢复测试签名，冷启动恢复 test01。长时间真机续期、桌面窗口对照、完整多端与高级 OA 未完成。

最新账户状态见 [712 账户状态真实性与真机页面检查](712-account-presence-and-native-pages-20260904.md)：未知/过期在线观察不再误显示“离线”，有效在线与离线仍取真实投影。新增 5 项，全量 1193/1193、分析 0；普通包已保留数据安装真机，账户、个人资料、待办详情及更多操作抽屉实际检查。电脑控制所需 node_repl 当前未提供，Windows 窗口对照未执行；高级 OA、长时续期及完整多端未完成。

最新同步生命周期见 [711 同步任务销毁与重启隔离](711-im-sync-lifecycle-20260904.md)：复现并修复 Provider 销毁后的状态写入、停止后迟到 bootstrap 发布与旧任务重启问题；新增 9 项，全量 1188/1188、分析 0。普通包已保留数据安装真机，单聊/群聊各往返 3 次，冷启动恢复原账号。真机长时间续期、完整桌面多端、复杂 OA 与长期性能仍未验收完成。

真机自动登录排查见 [710 旧包更新与恢复验证](710-phone-session-upgrade-20260904.md)：原真机实际仍为 658 包，缺少 670 的到期前续期修复；已先更新 709，现由 711 普通包替换。短时恢复通过不等于跨实际到期时间或隔夜续期通过。

最新连续输入回归见 [709 草稿竞态与断网发送验收](709-im-composer-race-and-offline-20260903.md)：发送同步截取并清空当前输入，不再让入库回调覆盖下一条文本、引用或提及；本地入库失败按账号/会话保留内存恢复项，稳定ID重试。新增16项，全量1179/1179、专项101/101、分析0。两模拟器正常709包实际在线/断网各发送2条，下一草稿均完整保留，离线2条Outbox恢复后原ID确认；双方614条、未读0、回执614，原IM/OA数据保留，证据核对12/12。同步协调器销毁异常、真机/最新桌面/完整多端/高级OA/媒体500/长期性能等继续；未入库内存项不承诺跨杀进程保存。

最新未读定位回归见 [708 首条未读、双向分页与真实验收](708-im-first-unread-navigation-20260903.md)：新增限定窗口与可见已读保护，修复后台返回停转及异步刷新卸载列表导致跳页。全量1163/1163、分析0。两模拟器真实新增100条，首次原生定位失败并保留read550证据；修正包用剩余60条复验，从551开始只读到可见561，7次手势到610，两端未读0、回执610，610条账本无丢失重复，原IM/OA数据保留。12项证据一致性核对通过不等于首次失败被抹去；快速输入草稿被旧发送回调清空的疑似竞态、真机/桌面/完整多端/OA等继续。

最新读取隔离回归见 [707 历史消息会话隔离与断网](707-im-history-session-isolation-and-offline-20260903.md)：检查首条未读依赖时发现历史缓存/网络链未固定原登录，修复换账号、同账号重登、退出后的迟到读写与错误；79项本地HTTP+SQLite用例（修复前62失败）全部通过，全量1123/1123、分析0。正常707包已安装两模拟器，M3真实断网24次上滑到第1条、离线重开最新510、恢复网络与在线显示；8项数据/运行核对通过，原IM/OA数据保留。首条未读定位尚未实现，不能将累计read510描述为逐条阅读；完整IM/OA/多端/真机/桌面验收继续。

最新解码性能回归见 [706 消息行解码复用](706-im-row-decode-reuse-and-runtime-20260903.md)：新 SQLite 全字段一致才复用，默认 4096 条/2Mi 源字符预算；覆盖已读投影、编辑撤回、密文校验与退出清理，修复 ref.read 持有存储时订阅暂停漏清理。两模拟器正常706包已安装，M3 原生滑到第1条，19次查询2980行中复用2470次、实际解码510次，3次热重开无重复解码；IM/OA数据12项保留核对通过。新增11项、全量1044/1044、分析0。SQL/列表重组与绘制仍有成本，不能宣称整体卡顿消除；完整IM/OA/真机/桌面/多端验收继续。

最新缓存性能回归见 [705 历史窗口与热重开](705-im-window-retention-and-hot-reopen-20260903.md)：旧510条分页保留7份重叠窗口（2190列表条目），改为每会话最近一份、32窗口/4096条目预算与闲置30分钟期限，不截断活动历史。两模拟器正常705包已安装，M3原生滑到第1条、实际仅1份510条，3次热重开无新增窗口加载；原IM/OA数据11项核对通过。新增8项，全量1033/1033、分析0。条目减少不等于整体内存/帧率达标；分页扩展仍重复读取/解密旧窗口，后续继续增量优化。真机仍锁屏、WindowsUI/复杂OA/推送/媒体500等未完成项不变。

最新真实收尾见 [704 顶部上滑与510条积压验收](704-im-history-edge-and-batch-acceptance-20260903.md)：修复Android顶部钳制时继续同向拖动不触发历史加载；离线原生24次手势滑到第1条、后续保持稳定、热重开显示510。两台模拟器正常704包哈希一致，冷启动保留双方510条、read510/unread0、原IM/OA记录；专项101/101、全量1025/1025、分析0，真实积压证据核对15/15。脚本漏算合并姓名的首条消息导致原退出1，保留原日志并对同批XML多行解析/人工截图复核；未重发数据。首条未读定位、真机/WindowsUI、完整性能、媒体500、推送和高级OA等仍开放，完整目标保持活动。

登录隔离见 [702 已读链路会话绑定](702-im-visible-read-session-isolation-20260903.md)：本地HTTP实证旧已读链在切账号后使用新账号调用“@我”，已改为三阶段保留原登录、请求前后核对、短锁提交SQLite；16项新增（原12失败）、专项44/44、全量1023/1023、分析0。正常702包已在两模拟器回装并实测可见已读，现由704替换；不把本地会话竞争测试当线上多端验收。

满页同步见 [701 满500事件补拉与真实积压](701-im-full-batch-catchup-20260903.md)：满页立即补拉、追平恢复长轮询；本地3项新增、全量1007/1007。真实发送任务22983已exit0，实际原生观察500+10页且事务提交先于ACK，两端510个唯一消息身份与顺序完全一致。未入群保留510未读，原生可见后SQLite清零、对端持久回执及双勾恢复；临时观察入口已移除。完整证据与限制见704，不重复运行原发送任务。

最新头像性能回归见 [700 头像尺寸解码](700-avatar-decode-size-and-runtime-20260903.md)：修复小头像按原图解码，保留比例/裁剪/真实来源和缓存复用；Android两轮真实codec内置头像256KiB→36KiB，大PNG16–32MiB→36–72KiB（仅解码位图，非进程内存），但大PNG首次resolve更慢约20–33ms，已记录代价。新增8项，全量1004/1004、分析0，M3/M4正常包同hash、数据23项保留通过；通讯录、单聊、群聊、我的、OA头像真实截图正常，5次新包进入返回通过。**绘制卡顿仍未消除**；真机已授权但锁屏，已询问解锁，未计为真机验收。原完整IM/OA与外部阻塞范围继续。

最新性能实测见 [699 长历史消息窗口与联系人回归](699-im-window-index-and-contact-runtime-20260903.md)：修复最近80条仍全量临时排序，schema14→15增加匹配索引并保留墓碑历史索引；Android真实SQLite合成5万条两轮含解密读取约111/123ms→15ms，结果/已读一致，不能等同整页速度。全量996/996、分析0，M3/M4正常包同hash、迁移与保留/运行31项通过，原数据未清；原单聊前后10次及断网3次原生进入返回通过，**raster高耗时仍在，完整性能未通过**。小数据集耗时有波动；原媒体500、推送、WindowsUI/真机及完整多端、高级OA等未完成项不变。

最新原生安全见 [698 Android Keystore 与环境隔离](698-android-push-keystore-and-environment-isolation-20260903.md)：Android推送原生副本改为Keystore AES-GCM、noBackup密文、明确环境namespace，历史无归属值加密隔离后清除旧明文键；失败不回退明文，密码学任务离开主线程。M3独立原生测试17项+新进程5项共22/22，新增Flutter5项，专项28/28、全量989/989、分析0。两台已恢复同哈希正常Profile包，业务数据12项保留，测试助手已卸载。真实推送仍未注册/待平台配置；iOS原生副本仍需独立Keychain迁移，不能宣称全平台推送验收完成。媒体500、完整多端/桌面UI/真机、高级OA、长期性能等不变。

最新推送修复见 [697 注册生命周期与隔离](697-mobile-push-lifecycle-and-session-isolation-20260903.md)：六项延迟/并发问题先复现全失败，补运行代次、会话绑定及安全存储锁，修复同令牌重登漏注册、隐私模式被前台恢复覆盖、令牌乱序与异步失败；新增18项，专项50/50、全量984/984、分析0。正常包M3/M4同哈希，真实群聊/通知设置正常，安装前后12项数据保留通过。**离线推送未通过**：当前页面未注册，未发现实际SDK接入，已询问平台/配置；另发现原生桥接仍写普通SharedPreferences，需继续迁移到系统安全存储，不能把Flutter层隔离当作全链路安全完成。原有上传500、完整多端/WindowsUI/真机、高级OA、长期性能等继续。

最新会话实测见 [696 移动替换与账号隔离](696-mobile-session-switch-and-account-isolation-20260903.md)：M4原生登录test03替换M3，正常包捕获heartbeat409退出、迟到刷新200被拒绝；旧端缓存停止在296/群seq5，新消息seq6恢复后仅一条、本人未读0。M4切回test04保留独立未读1，可见阅读后落库为0，两台冷启动与OA/待发数据31项核对通过。专项81/81、重新全量966/966、分析0；未改生产代码/未重建包，最终两台仍为正常695同哈希并恢复原账号。此前642已有同账号桌面/移动替换证据，本轮D1是不同账号只读200，不能替代完整WindowsUI/D2/改密矩阵；媒体500、推送、长期性能及高级OA等未完成项继续。

最新媒体排查见 [695 上传 500 安全取证](695-im-upload-failure-diagnostics-20260903.md)：原图片/视频分别原生重试一次，确认 multipart 后收到 JSON 500，取得各自服务端 traceId，非超时；两条仍 pending/seq0，没有重复创建。新增默认关闭、release硬禁用的白名单诊断与安全日志采集，12项测试，专项54/54、全量 **966/966**、分析0；临时诊断仅M3，最终已恢复两台正常包同哈希，17项数据保留核对通过。媒体根因/送达仍不通过，已请求后端日志，不在无新证据时继续手工重试相同500；其余IM/OA与完整多端/真机/WindowsUI/推送/性能矩阵继续。

最新待发隔离见 [694 IM 会话隔离与真实回归](694-im-outbox-session-isolation-and-native-regression-20260903.md)：本地复现旧队列使用切换后账号发出，修复六类发送请求绑定原会话、媒体分步检查、迟到结果隔离及 401/替换处理。新增39项，专项47/47、全量 **954/954**、分析0，正常包两模拟器同哈希。M3原生新发一条群消息，M4首页未读1→实际可见后0，M3回执自动已读；冷启动两端seq1–5唯一、原数据22项核对通过。原图片/视频分别重试一次仍 **HTTP 500**，媒体链路未通过。Windows仅只读会话200，不等于UI验收；完整同账号多端、真机、推送/性能与高级OA仍未完成。

最新群消息断点见 [693 群 @我 的 ACK 前杀进程](693-group-ack-crash-and-filter-empty-state-20260903.md)：M4原生只发1条群@消息，M3真实落库289/确认286时强制结束进程；正常包冷启动补确认到289，唯一消息/read3/unread1/@[4]保留，实际可见阅读后read4/unread0/@[]持久化，发送端回执同步更新。此前658已覆盖单聊，本轮补两人群，不代表批量/所有崩溃窗口已完成。实测另修复“@我读空却显示暂无会话”，未读/提及/群组使用各自空态；新增13项，全量 **915/915**、分析0，最终正常包M3/M4同哈希，14项保留核对通过。高级OA、同账号/WindowsUI/真机完整矩阵、推送及性能等仍未完成。

最新状态时序见 [692 来源对照与离线延迟](692-presence-source-comparison-20260903.md)：两轮真实断网同时采集 API 状态、成员/会话投影、列表和桌面授权资料查询；首次离线截图距断网约140秒/126秒，新离线响应到构建决策约17ms/40ms。缩短采样后未见“资料离线之后新的 presence 仍在线”或“新离线响应被旧在线覆盖”，不能把不同采样时刻直接判为接口冲突；数据与服务端状态窗口叠加30秒轮询相符，具体TTL仍未确认，**P2 离线及时性保持开放**。新增默认关闭/发布禁用的白名单诊断，专项26/26、全量 **902/902**、分析0；两台已回装同哈希正常包，双网络恢复、诊断0、9项数据保留检查通过。其他原目标未完成项不变。

最新列表状态见 [691 可见单聊状态刷新](691-message-list-live-presence-refresh-20260903.md)：修复消息列表观测过期后不补取；只调度视口内 direct 的现有 presence 接口，最多2并发、30秒周期，离页/后台/登录变化取消。新增11项，专项47/47、全量 **899/899**、分析0；正常包安装M3/M4且哈希一致。真实静置约101秒仍在线，本机断网降级未知且保留登录，恢复自动在线；原IM/2待发媒体/2草稿/10回执保留。另实测 **P2 对端断网约164秒才显示离线，资料接口更早返回离线**，原始会话presence响应尚未并行采集，不能断言根因或判及时性通过。包体93.2MB增大待分析；同账号多端、Windows UI/真机、高级OA、推送与性能等仍未完成。

最新群聊与键盘见 [690 独立双端群同步与创建抽屉](690-m3-m4-group-sync-and-keyboard-20260903.md)：M3/test03 与 M4/test04 真实新建两人群，3 条消息含双向 @；接收列表保留未读，实际可见后持久清零，离线冷启动恢复自动补齐 seq1–3 且无重复，已读回执双端更新。修复创建群聊按钮被键盘遮挡，新增3项红转绿，全量 **888/888**、分析0；正常修复包已装两台且哈希一致，键盘上方按钮实测可见可用，原单聊/2待发媒体/2草稿/10回执保留。另实测发现 **P2 消息列表静置后状态未知、进入会话才恢复在线**，仍需补可见成员状态刷新；同账号多端、Windows UI/真机、高级OA/推送/性能及其他服务端问题仍未完成。

最新独立双端见 [689 M4、单聊离线同步与焦点修复](689-independent-m4-direct-sync-and-sheet-focus-20260903.md)：新建独立M4/test04（合盈），不复制旧设备数据、不动M1/M2；与M3/test03真实好友申请/接受及3条单聊完成。M3离线冷启动期间收到2条，联网留在工作台自动落库且保留未读2，实际打开后清零，两端回执自动更新。最终正常修复包已装M3/M4且哈希一致，3条ID/序号/已读重启保持，原2待发媒体/2草稿/10回执保留。真实发现并修复添加好友/部门抽屉关闭重弹键盘，新增2项，专项18/18、全量 **885/885**、分析0、两台实际关闭后键盘false。M4保留后续验收；新测试群、同账号多端替换、Windows UI/真机、高级OA/推送/性能与服务端缺陷仍未完成。

最新通讯录性能见 [688 按可见区域渲染与运行回归](688-contacts-virtualization-and-runtime-20260903.md)：展开部门改为逐行懒构建，保留默认组织收起、头像入口、真实在线源及好友/群聊边界。2000 人本地组件测试连续滚动后挂载头像从240降到14，非实机FPS结论。新增8项，全量 **883/883**；等价SDK缓存API调整后专项56/56、分析0。正常包仅M3且哈希一致，5次头像进入/单次返回及搜索进入已有单聊实测通过，返回保留展开、清词恢复收起；IM账本/2待发媒体/2草稿/10回执均保留。真实目录仅2人，服务端分页、真机大目录帧率、Windows UI和完整多端/高级OA/推送仍未验收。

最新个人与改密见 [687 改密安全与断网交互](687-profile-password-safety-and-offline-20260903.md)：修复成功后旧记住密码残留、请求会话固定与迟到提示隔离、超时误报“未修改”、提交期编辑/返回及校验残留；键盘逐框前进。新增15项，专项23/23、全量 **875/875**、分析0。正常包仅M3且哈希一致，实际点按下一项进入新密码/确认框；断网冷启动保留头像和登录，显示同步中断，原网络恢复后自动移除提示（成员当时离线，不虚报在线）。原2草稿/10回执/IM账本和待发媒体保留。真实改密与其他设备失效、Windows UI、真机和完整多端/高级OA/推送/性能仍未验收。

最新计算精度见 [686 十进制计算与保存往返](686-oa-decimal-calculation-and-roundtrip-20260903.md)：修复中点舍入、floor/ceiling 误差、范围及有效系数漏检和 JSON 大整数丢位；新增31项，专项40/40、全量 **860/860**、分析0。最终正常包仅装 M3，APK 哈希一致；实际重开既有请款 **2C342E**，100.25、已通过、头像和处理记录保留；原2草稿/10回执/IM账本及2条待发媒体不变，未读仍31。线上当前模板无公式，双公式/大金额服务端与桌面窗口兼容性仍未实测，不能据本地测试判完整验收；真机/Windows UI、跨账号、高级OA、多端/推送/性能及服务端问题继续保留。

最新真实请款见 [685 数值校验与错误恢复](685-oa-numeric-validation-and-live-payment-20260903.md)：修复金额/数字接受 NaN、Infinity、指数溢出，以及修正字段后仍保留旧错误；新增20项，OA页面84/84、全量 **829/829**、分析0。最终正常包仅M3且哈希一致，实际100.25请款 **2C342E** 提交→同意→结果通知，未读32→31并落库；断网冷启动保留终态与已读，联网自动恢复，原2草稿/9回执/IM不变。本轮是当前财顺模板单节点自审批，不是复杂分级或跨账号流程通过；真机/Windows UI、完整多端/推送/性能及服务端问题仍未完成。

最新媒体密度见 [684 气泡留白与预览](684-media-bubble-density-and-real-preview-20260903.md)：修复媒体状态行撑满292dp及图片网格重复安全区；单图/音频发出上限229dp、视频219dp，保留预览尺寸/时间/状态，长群成员名单行省略。新增15项，聊天69/69、全量 **809/809**、分析0。M3正常包哈希一致，真实图片放大/长按、群视频播放暂停返回及群历史滚动通过，图片消息行633→570px；消息/两条待发媒体/2草稿/9回执保留。媒体仍500，多图音频真机、完整多端/大群性能/Windows UI和高级OA未完成。

最新联系人进入性能见 [683 分段与断网重开](683-contact-chat-timing-and-offline-20260903.md)：正常Profile仅M3，新增8项、全量 **794/794**、分析0；实际9份进入分段。当前7条消息首次末条布局195.642ms，4次热缓存49.133—79.459ms；断网3次61.899—71.863ms、断网杀进程后83.502ms，本地头像/消息/待发图片恢复且不退出登录。布局检查点不等于过渡/媒体解码完成，大群/真机/长时性能仍未通过。已恢复原网络，消息/队列/2草稿/9回执保留；服务端媒体500、Windows UI、完整多端和高级OA继续未完成。

最新启动与渲染证据见 [682 分段诊断与 GPU 对照](682-startup-phases-and-renderer-20260903.md)：正常 Profile 已有真实 Flutter build/raster 帧数据，新增11项、全量 **786/786**、分析0。独立M3保留原数据切换并确认RTX4070渲染，修正启动助手遗漏AVD目录且验证防重复。普通冷启动软件4.467秒、主机7.571/3.458秒，仍有明显波动和耗时峰值，**不能判性能修复**。5次真实头像进入单聊/一次返回通过，消息、两条待发媒体、2草稿/9回执保留；真机/Windows UI、长时/大群性能、完整多端和高级OA及服务端阻塞继续未完成。

最新通讯录导航见 [681 防重入与当前运行](681-contact-navigation-guard-and-runtime-20260903.md)：复现并修复收键盘期间连续点击导致重复会话入栈，已有/新建入口统一锁定至返回，失败可重试。新增8项，全量 **775/775**、分析0；正常包仅装M3并核对哈希，20次真实头像进入/单次返回全部通过，原消息/待发媒体/OA草稿回执保留。安装后首次模拟器冷启动 **10.6秒偏慢**；PSS回落但Swap增加，尚无有效Flutter帧数据，不能判整体性能通过。真机/Windows UI、多端、高级OA与既有服务端阻塞仍未完成。

最新媒体与断网列表见 [680 真实上传重试与待发摘要](680-im-media-retry-and-pending-preview-20260903.md)：系统选择器真实发送图片/视频仍 HTTP 500、未送达；修复 Outbox 待发内容不显示在会话列表，处理排序、加密冷启动、部分确认和账号隔离。新增17项，全量 **767/767**、分析0，正常包仅装M3并再次核对哈希；离线冷启动保留图片/视频摘要与小等待图标，图片放大和系统视频播放器控制实测通过。联网自动重试原ID无重复，仍500；D1只读确认单聊6/群聊11未增加媒体，原2草稿/9回执保留。媒体跨端、完整OA/多端/推送/性能与真机/桌面UI仍未完成，不把本地预览算发送成功。

最新真实审批见 [679 转交与前后加签](679-oa-transfer-add-sign-real-20260903.md)：M3 页面真实创建 E5F097、8B87B0、5303A3，执行转交、前加签、后加签和原节点同意；D1 当前会话只读确认实际权限、持久通知及任务激活，未代替桌面 UI。三单均已撤回、无可操作任务，但 **P2：前加签原任务仍 waiting，终态不一致**，未伪造取消。已修复历史 `add_signed` 中文显示，新增4项，**750/750**、分析0；正常包仅装M3、哈希核对一致，原2草稿/9回执/IM保留。接收人最终处理、前加签回原节点、真机/桌面UI、完整高级OA/多端/性能及既有服务端缺陷仍未完成。

最新同步性能见 [678 OA本机操作后追赶](678-oa-sync-latency-20260903.md)：分段确认事件请求约20秒、投影0.5–0.8秒、提交约0.06秒；成功本机操作唤醒零等待追赶，保留正常长轮询、原子落库与会话守卫。新增9项，**746/746**、分析0，正常包仅装M3并核对哈希。真实通知已读意图到本地applied_at由两次6.15/9.29秒降到1.73/1.59秒；离线已读杀进程保留，联网自动sent与seq411，原草稿/回执/IM未变。不是跨端延迟全部解决；远端事件唤醒、取消通知错误正文、真机/Windows UI、高级OA及完整多端/性能仍未完成。

最新选人与自然续期见 [677 抽屉空态与到期边界](677-oa-member-picker-and-natural-refresh-20260903.md)：修复无搜索结果的大空态溢出到键盘下方，区分无候选和无匹配；新增5项，全量 **737/737**、分析0，正常包仅装M3并核对哈希。01:46:30自然刷新200并保存，旧确认被拦截；同一进程跨原01:51:13到期边界后真实撤回新单2560AC成功，没有重登或再次刷新，原草稿/回执/IM保留。此次自然续期链路通过；长离线、完整多端/高级OA、真机/Windows UI与性能仍未完成，候选范围也未擅自扩大。

最新审批确认安全见 [676 抽屉会话与任务守卫](676-oa-action-dialog-guard-20260903.md)：捕获首次交互会话并绑定到底层发送，菜单/选人/方式/最终确认复核任务、权限与网络，连点不叠抽屉。新增60项，全量 **732/732**、分析0；正常包仅装M3并核对哈希。真实新单2DDF00在确认前断网被明确拦截，联网无自动补发，重新确认后只有一次ONLINE撤回；冷启动保留终态，原草稿/回执/IM不变。转交与加签仅验证取消，未分配新任务给他人。D1只读IM/OA200；实际高级审批、自然续期、真机/桌面UI、多端、推送与性能仍未完成。

最新真实 OA 链路见 [675 撤回与再次发起](675-oa-withdraw-resubmit-real-20260903.md)：正常M3/test03通过页面新建8928DD（100元）→撤回→再次发起0FE47E（101元）→驳回；原单不被覆盖，实际节点/原因正确，结果通知定位正确，未读14→13→12及seq389/390落库。断网杀进程仍保留，联网未点击自动恢复，原草稿与IM不变。12项专项11过，**P2：撤回后的任务取消通知误写“其他处理人完成”**，未伪造修复。D1当前会话IM/OA200，两条他人申请只读404；不是桌面UI完整验收。仅改测试助手/报告，无新Flutter构建；整体目标继续。

最新统一人员状态见 [674 跨页面来源与时效](674-unified-member-presence-20260903.md)：通讯录、会话列表/头部、群成员、选人和我的共用短期人员状态；旧请求不覆盖新请求，缺字段/过期/断网为未知，按账号设备会话隔离，群总数不映射个人，不增加逐行 HTTP。新增23项，全量 **672/672**、分析0，正常包仅装M3，断网保留成员且无绿点，联网未点击自动恢复；消息/游标/队列/草稿未变。没有更新Golden。真实多端矩阵、自然续期、真机/桌面、高级OA和性能仍未整体通过。

最新聊天交互回归见 [673 可见性与已读](673-chat-visibility-and-read-guard-20260903.md)：修复被详情/抽屉覆盖或后台、Offstage 时页面仍轮询，以及抽屉覆盖下新消息被误标已读；恢复可见才重新计算视口，慢 presence 请求不重入。新增5项，**649/649**、分析0，正常包仅装M3并核对哈希，聊天→抽屉→返回→详情实际截图已存。新消息隐藏已读是组件注入回归，尚非真实双端到达证据；全局状态新鲜度、自然续期、真机/桌面及高级OA未完成。

最新成员状态验收见 [672 群成员与可见刷新](672-member-presence-and-visible-refresh-20260903.md)：修复断网未知仍显示绿点、慢请求清空成员列表、第一页计数冒充全群在线数；通讯录/成员按可见前台刷新，成员响应会话隔离，SQLite 14 保存真实最后在线。新增19项，**644/644**、分析0，正常包仅装M3并核对哈希；实际断网保留行与分页、无绿点，联网约28秒检查点前自动恢复，通讯录默认折叠正常。消息元数据、游标、队列和OA草稿未变。跨来源全局新鲜度、自然续期、真实多端和高级OA仍未完成，M1/M2未操作。

最新在线状态修复见 [671 列表/聊天一致性](671-im-presence-consistency-20260903.md)：同一测试会话三种只读服务端来源均离线，确认列表错误采用旧会话成员缓存。列表与聊天共享有时效、账号隔离的 presence，优先新通讯录状态，不增加每行请求。新增11项，全量 **625/625**、分析0；M3正常包实测列表→聊天→返回均离线，过期/断网变未知或最后在线，恢复后重新离线；消息、游标、Outbox和草稿未变。仅更新已核对的群详情在线标记Golden。通讯录整体新鲜度、群成员状态、连接提示、自然续期和其余验收未完成，M1/M2未操作。

最新会话续期见 [670 到期前主动续期](670-proactive-session-refresh-20260902.md)：修复仅在 401 后刷新导致到期后被拒绝的问题，恢复登录、前台恢复和 30 秒心跳在 exp 前 5 分钟主动刷新；拒绝/断网不凭本地时间退出，账号切换和停止任务仍隔离。新增18项，全量 **614/614**、分析0。M3真实刷新200→心跳204、令牌旋转且重复检查不重刷；恢复正常包后单聊消息由D1只读接口核对同ID/seq唯一，草稿与回执不变。自然到期仍未实测，且新发现列表“在线”与聊天页“离线”冲突，真实在线状态不能判通过。

最新草稿与详情见 [669 保存生命周期与字段排版](669-oa-draft-lifecycle-and-detail-fields-20260902.md)：草稿串行保存、稳定ID、修订号保护，返回等待最新值，提交等待旧写入，失败显示可重试状态；字段自适应列宽、多行事由按后台配置正常换行。新增10项，全量 **596/596**、分析0。仅M3实际编辑后快速返回重开保留最新值、草稿不重复，已通过申请详情前后截图核对业务值不变；最终正常包哈希一致、网络正常。原请假草稿未变，未提交新审批。真机/Windows UI、高级OA、自然续期、推送、性能及服务端P1继续保留。

**669收尾新增：23:38真实心跳401、自动刷新401、会话终止并回到登录页；无感续期未通过，拒绝原因未定。** 当前刷新请求不携带旧Bearer，不能猜测根因。23:40已通过应用安全保存的test03凭据恢复登录，草稿、已读和Outbox核对未丢失。下一轮优先核对最新协议与自然到期恢复；详见669末节证据。

最新表单交互见 [668 自动保存稳定性与抽屉键盘](668-oa-form-stability-and-picker-keyboard-20260902.md)：保存/恢复提示收进申请人栏，消除首次保存38dp跳动；5类选择抽屉清除旧输入焦点，关闭不再重弹键盘。新增13项，全量 **586/586**、分析0；正常包仅装M3，真实填写、取消/确认、断网杀进程恢复及联网自动解析取证，三个字段坐标始终不变。只新增一条本地测试草稿，未提交远端申请，原草稿未变。长字段标签、真机更新与完整桌面/高级OA/多端/性能仍待完成。

最新审批动作见 [667 会话隔离与真实加班审批](667-oa-action-session-and-real-overtime-20260902.md)：审批读取/动作与通知已读隔离旧登录结果，新增43项回归，全量 **573/573**、分析0。正常包仅装M3，实际新建加班申请 FE9B58 并通过，结果通知已读8→7，seq355落库；断网杀进程保留通过状态和已读，网络已恢复。原草稿未变；新发现自动保存推移表单与选择后键盘重开，继续处理。完整高级OA、桌面UI、多端、推送及服务端P1仍未完成。

最新网络恢复见 [666 OA网络唤醒与生命周期](666-oa-network-wakeup-and-lifecycle-20260902.md)：订阅系统网络变化，恢复时取消旧读取、持久刷新目录/通知并零等待追赶事件；修复启动期间停止与快速重启竞争。专项 **46/46**、全量 **530/530**、分析0。正常包仅装M3，真实断网恢复分别在约1.7秒（保持进程）和2.5秒（断网冷启动后）观察到落库与页面恢复，无需重试；原账号/草稿/已读保留。仅两次实际样本，不代表完整性能达标；OA在途写操作停止语义、完整多端/审批/推送与服务端P1继续保留，目标仍进行中。

最新目录状态见 [665 加载、空态与真实断网恢复](665-app-catalog-states-and-real-offline-recovery-20260902.md)：首页与全部应用共享状态处理，修复把加载/失败当搜索空态、切号残留旧目录；真实网络重试并保留当前账号缓存与搜索。**521/521**、分析0；正常包仅装M3，断网杀进程后目录保留，恢复后未再点击，约28秒目录落库并移除提示。状态条压至44dp；完整OA、多端、性能及恢复延迟仍待处理，目标继续。

最新应用目录见 [664 分类密度与当前目录图标](664-app-catalog-density-and-current-icons-20260902.md)：当前桌面会话目录 GET 200（非桌面 UI 证据），补齐钱款/钱包/购物车等图标并统一首页；去掉分类重复安全区。正常包仅装 M3，卡片高度实际减少120dp，请款与借支入口和底部选择抽屉正常，未提交业务。**508/508**、分析0、安装哈希一致；目录加载/错误空态、黄金图字体、其他 OA 隔离和完整多端/性能仍待处理，目标继续。

最新 OA 同步见 [663 事件会话隔离与真实已读恢复](663-oa-event-session-and-live-read-recovery-20260902.md)：25 项切号/重登/退出竞态复现后修复，37 项专项、全量 **503/503**、分析 0。正常包仅装 M3，真实打开考勤通知后未读 5→4，回执 sent，实际 `oa.notification.read` seq344 与缓存同批落库；断网杀进程重开保持，网络已恢复。其他审批动作会话归属、应用目录留白/通用图标及完整桌面/多端/性能仍待处理。

最新断网恢复见 [662 审批流程自动恢复](662-workflow-preview-network-recovery-20260902.md)：修复相同表单失败结果导致联网后仍卡离线状态；10 项新回归，全量 **466/466**、静态检查 0。正常包仅装 M3，真实断网杀进程恢复草稿后，联网不点重试，约 32 秒检查点前自动恢复审批人，表单值保留；日期抽屉取消不改值。恢复延迟、其他 OA 隔离及完整桌面/多端验收仍待完成，目标未完成。

最新 OA 隔离见 [661 待同步会话隔离与草稿恢复](661-oa-outbox-session-isolation-and-draft-recovery-20260902.md)：旧申请跨登录发送、迟到结果及认证失败队列处理已有本地 HTTP/SQLite 红绿证据，**456/456**、分析 0；正常包仅装 M3。真实草稿断网杀进程后内容保留，但联网后审批流程需手动重试，自动恢复待修复。远端跨账号重放、其他 OA 同步路径及完整验收未完成。

最新续期验收见 [660 真实续期与后续消息](660-live-refresh-and-post-refresh-im-20260902.md)：M3/test03 调用应用真实续期方法，`/connect/token` 200、两种令牌均旋转、账号设备不变；恢复正常包后 UI 发出新 seq5，M1 自动落库并保留 unread1，D1 原会话 GET 200、同一消息唯一。增加白名单续期诊断与禁止重定向，**445/445**、静态检查 0。本轮是到期前显式调用，**尚未证明自然到期自动触发**，M1 历史 401 原因仍未确定；新版仅装独立 M3，真机避免打断操作而未更新，完整目标继续。

最新会话核对见 [659 普通刷新隔离与新环境会话](659-bootstrap-session-isolation-and-live-session-health-20260902.md)：IM/OA 普通 bootstrap 刷新旧响应覆写缓存，8 项真实本地 HTTP 测试复现后已修复；缓存提交与会话切换串行，**437/437**、静态检查 0。正常包仅安装独立 M3，实测断网杀进程重开后登录/头像/消息保留，待办与我的状态正确，联网自动恢复。新环境当前 test03 **带刷新令牌，未验证 Token 时间字段相差 2 小时**，三个接口均 200；不能沿用旧 12 小时描述，真实续期与 M1 历史 401 仍待核对。真机疑似用户正在审批而未更新，其他 OA/IM 仓库竞争、服务端 P1 与完整验收继续保留。

## 当前环境切换

最新精准断点验收见 [658 会话隔离与 ACK 前真实杀进程](658-event-session-isolation-and-real-ack-crash-20260902.md)：四项旧事件/摘要响应跨登录竞争修复前失败、修复后通过；新增 ACK 失败/重复事件恢复回归。M3 实际收到 message.created 并落库到 200、ACK 仍 195 时强制杀进程，正常包重启自动确认到 200，原消息唯一、未读 1 保留，可见阅读后清零；M1/M3 四条账本一致，D1 GET 200。**423/423**、分析 0 问题，正常包双端同哈希。中途 M1 被 managed_commands 401 退出，已用安全保存凭据恢复，但原因未定；群读投影 P1、群事件、媒体、完整桌面/OA/推送/性能仍保留。

最新未读来源与重连续验见 [657 服务端读投影与传输重试](657-unread-provenance-and-transport-retry-20260902.md)：真实 M3 未打开会话、未 POST 已读，服务端摘要已 read=7→9/unread=1→0，P1 定位为远端投影来源，服务端内部原因仍待修复。客户端新增传输失败一次性恢复标记，不跳过 HTTP 429/500 退避；正常包双端安装，冷启动离线两条于联网约 10 秒检查点前自动按序发出，早于原调度时间；双库 11 条完全一致，接收端未打开群保持未读 2。**416/416**、分析 0 问题，临时诊断输出关闭。旧媒体失败、群事件缺项、桌面 UI 和高级 OA/性能仍未验收。

最新断网发送验收见 [656 Outbox 时钟、升级与恢复](656-outbox-clock-migration-and-network-recovery-20260902.md)：定位并修复 ISO 时间精度导致的队列漏查/乱序，v12→v13 保留旧离线消息，最终包 M1/M3 实际自动按序发出原 ID 两条，无重复。网络恢复唤醒和零等待首拉已加入，**411/411**、分析 0 问题。但补发后 M3 未打开群却 read=4→7、unread=1→0，新增 P1 待来源核对；已有退避造成约 61 秒发送等待、群事件缺项和完整桌面/OA 验收仍未解决。

最新群聊验收见 [655 群聊、@、离线与角标](655-group-mentions-offline-and-badges-20260902.md)：M1/M3 最终包真实完成双向群消息、@ 成员、阅读后角标与 SQLite 清零、断网冷启动补齐；四条消息双库 ID/序号完全一致，头像合并位于首条。修复本地已读后总角标缺少刷新依赖，新增 3 项回归，全量复跑 **406/406**、静态分析 0 问题；首跑 1 项 Outbox 偶发失败保留待查。**群事件流仍缺 message.created，恢复后约 47 秒检查点才确认补齐，不能判定实时同步通过。** M3 网络已恢复，旧上传 500 与桌面 UI/高级 OA 仍待完成。

最新独立双端验收见 [654 好友与断网单聊](654-independent-device-friend-and-offline-im-20260902.md)：新建独立 M3/test03，不操作原 M2；最终包在 M1/M3 实际完成好友申请接受、双向单聊、可见后已读和断网冷启动补齐。恢复网络后未打开会话即落齐三条，未读保持 2，阅读后清零，最终双库 ID/序号一致无重复。修复好友查询的误报空结果及编辑中卡住，**403/403**、静态分析 0 问题。M3 网络已恢复并保留继续群聊验收；群聊、附件 500、完整桌面 UI/高级 OA 和性能仍未整体通过。

最新会话校对与定位见 [653 轻量摘要与热重开](653-conversation-index-and-hot-reopen-20260902.md)：不再仅靠未读总数发现同账号消息，25 秒限频校对实际可用的会话列表，变化才落库并补正文；修复缓存已就绪时重开聊天跳到旧记录。**401/401**、静态分析 0 问题，最终包已装 M1；真机连续三次重开、历史阅读位置及上滑最早记录均取证。M2 尚未安装，D1 真正发送后的自动接收、媒体服务 500 和完整高级 OA 仍未验收，不能宣称整体通过。

最新消息补齐见 [652 已公布序号的正文补齐](652-announced-im-history-repair-20260902.md)：摘要末序号高于缓存时，后台按页落正文与断点，未读/ACK 不伪造；账号切换、空页、503、重复和清空保护覆盖，**390/390**、静态分析 0 问题。新包已装真机且 85 条缓存正常；M2 尚待确认操作权后安装，不能宣称真实接收缺口已解决。未读总数不变且事件漏发的隐藏会话发现仍待完善。

最新群聊续验见 [651 群历史与会话队列隔离](651-group-history-and-outbox-isolation-20260902.md)：历史 GET 已恢复 200，真机自动上滑加载至首条。修复图片 500 导致整个账号排队阻塞，原群文本 clientMessageId 不变并自动确认成序号 85；同会话顺序保持。**378/378**、静态分析 0 问题，新包已装真机；模拟器因操作来源未确认暂未更新。接收端摘要/@我 已更新但正文未落库，群事件和媒体/附件仍待解决，不能判定全链路通过。

最新会话续验见 [650 请求归属与双向替换](650-session-response-isolation-20260902.md)：旧命令 401 清空新会话在同/异账号两项测试中复现，已改为按请求实际凭据核对、原子条件清理和隔离旧刷新结果。**376/376**、静态分析 0 问题；新包真断网冷启动保留登录，M2/M1 双向替换均取证心跳 409，桌面会话保持 200，原测试账号已恢复。649 的最初退出原因仍未追溯证明，12 小时到期/D2/改密等矩阵继续保留。

最新通知断网验收见 [649 离线已读与补传](649-notification-offline-read-replay-20260902.md)：单条 OA 已读先持久化、离线冷启动保留、联网自动补传，真实 test02 目标通知服务端 isRead=true、本地回执 sent、事件游标 139 对齐。IM 通知点击移除可见前预标整会话已读。**362/362**、静态分析 0 问题；正常入口包已恢复双端同哈希，模拟器网络恢复。首次登录失效原因仍待请求级核对，离线全部已读/系统推送/高级审批不因此判定通过。

最新终态续验见 [648 可见详情校对](648-oa-visible-detail-reconciliation-20260902.md)：确认 test02 服务端事件与本地游标都停在 100，缺少已撤回申请的后续事件。保留事件驱动更新，并对前台可见的未结束详情增加 30 秒校对，后台/终态停止。新申请 523F15 在最终包双端完成同意与撤回，M2 不下拉、不重开即显示已撤回和剩余任务取消；断网冷启动仍读取相同终态。**353/353**、静态分析 0 问题，正常入口包已安装双端并核对哈希，模拟器网络已恢复。服务端事件漏发仍为待修复项，不等同于系统推送或全流程通过。

最新跨人审批验收见 [647 转交与详情定向更新](647-oa-transfer-live-detail-and-notifications-20260902.md)：两条真实申请均完成 test01 → test02 转交、test02 同意、申请人撤回，确认权限迁移和会签中间态。修复事件只更新列表、不更新已打开详情的问题；最终包实测 M1 不退出、不下拉即显示 M2 同意，M2 新待办自动到达、通知已读断网重启保持。**348/348**、静态分析 0 问题，18:16 包已安装双端。但已处理人接收后续撤回状态仍未自动更新，手动刷新才对齐，事件分发需继续核查；全会签仍需独立测试审批人配置。两条本轮申请已撤回并取消剩余任务。

最新媒体验收见 [646 媒体待发送与重试](646-im-media-outbox-retry-and-new-environment-20260902.md)：补齐未确认消息长按底部抽屉、安全阶段说明及原记录重试。M2 视频真实重试定位到上传 HTTP 500；M1 图片发送也返回 500。视频真断网冷启动仍有首帧并可用系统播放器打开，真机图片重启后缩略图和全屏预览保持。**342/342**、静态分析 0 问题，17:58 最终包已安装两端并核对实际 APK 哈希。媒体跨端交付未通过；保留原待发送记录，模拟器网络已恢复。18:03 复核历史 GET 405、目标单聊序号仍为 6，不能把本地预览认作远端发送成功。

最新已读验收见 [645 真实已读与离线持久化](645-im-real-read-status-and-offline-persistence-20260902.md)：修复“有回执权限就画双勾”的错误；真机/模拟器单聊实测 0/1 单勾 → 真正阅读 → 自动双勾与 1/1，SQLite 冷启动断网仍保留证据，对方在线状态显示未知。**339/339** 测试、静态分析 0 问题，同一 17:39 最终包已安装两端。群历史 GET 405 与接收端群事件缺失仍未解决，不能宣称群聊整体通过。

本地历史分页见 [644 缓存与断网](644-im-local-history-pagination-and-offline-20260902.md)：真实测试群 84 条消息，旧包冷启动只显示最近 80 条且上滑失败；新包优先读取紧邻缓存，在线/断网冷启动均能上滑显示最早 4 条。缓存缺口仍请求服务端，不伪装补齐；模拟器网络已恢复。

最新 OA 验收见 [643 真实申请与待同步](643-new-environment-oa-request-and-outbox-20260902.md)：真机完成草稿重启恢复、无附件提交、三人会签仅本人同意后仍审批中、撤回、再次发起新编号、驳回及通知持久已读。附件上传实际返回 HTTP 500，保留一条本地待同步，不能宣称附件通过；已补齐 pending 的安全原因、重试与底部操作抽屉。全量 **325/325**、0 静态问题，17:05 最终 Profile 包覆盖安装真机和模拟器。桌面仅 GET 核对，完整桌面 UI、三人全部同意及高级流程仍待验收。

最新会话验收见 [642 移动会话替换与账号隔离](642-mobile-session-replacement-and-login-notice-20260902.md)：实测 M2 登录 test01 后 M1 退出，反向恢复亦生效，当前桌面 test01 会话始终可读；同设备切换 test01/test02 时群消息缓存隔离，切回原账号可恢复。退出原因已改为登录页持续单条提示，并在真机停留 31.9 秒验证。最终 **323/323** 测试通过，16:49 新包已安装两端；结束时真机 test01、模拟器 test02 均已恢复。D2 替换、改密及完整网络层取证仍未完成。

最新续验见 [641 群聊与离线验收](641-new-environment-group-and-offline-uat-20260902.md)：新环境群历史 GET 返回 405，接收端持久事件流缺少群消息，群聊未通过；单聊应用离线两条消息补齐、顺序、去重与读后服务端清零通过。已修复发送确认后会话摘要不刷新、永久错误自动重试过多和 405 模糊提示；全量 **321/321** 测试通过，16:39 最终包已安装真机及模拟器并复验。不能用单聊或旧环境结果宣称群聊/OA 全部完成。

以下为本日上一轮迁移基线：

旧服务器已删除；当前桌面 1.0.87 的实际控制面为 `http://api.sfhkh.com`。移动端已切换并加入按服务地址隔离的安全存储、数据库与媒体目录，旧账号和待发送业务不迁移。真机 test01 / 模拟器 test02 已完成登录、单聊双向收发、已读清零和重启恢复；桌面现有会话的只读接口核对到同一单聊序号 2、已读序号 2、未读 0。审批申请页漏传系统预设头像的问题已修复并在真机复验。最终 314/314 测试通过，新包已覆盖安装双端；新环境群聊、完整 OA 流程与桌面窗口交互仍待验收。详细证据及产物见 [640 新环境记录](640-new-test-environment-migration-20260902.md)，下方旧服务器测试条目仅作历史记录。

## 对齐基准边界

- 产品功能、页面信息、交互状态与视觉完成度，只以当前已登录、正在运行且已更新到最新版本的 Windows 桌面窗口为准。
- `Desktop/Windows` 源码不是最新版本，不再用于判定桌面端“应该有什么”或移动端是否完成；仅可辅助查找接口路径、字段和历史实现线索，且所有结论必须由最新运行窗口或真实服务端响应复核。
- 已留存的 v1.0.79 桌面截图是当时的运行证据，不等同于 v1.0.80 当前产品规范；受版本影响的结论在最新登录窗口复核前标记为待复验。
- 2026-08-31 当前 Windows 窗口已更新到 v1.0.80 并保持登录，工作台已重新取证；此前 v1.0.79 的更新阻断不再作为当前结论。

| 模块 | 桌面端能力 | 移动端状态 | 后续动作 |
|---|---|---|---|
| 登录与会话 | 终端登录、记住登录、令牌续期、自动重登、退出 | 已按桌面端补齐刷新令牌与记住凭据自动重登，并修复续期只写存储、不更新运行态的问题 | 测试环境站点接口恢复后补弱网正向回归 |
| 工作台 | 授权站点、主备登录地址、应用、审批摘要、消息、通知、公告、日程 | 已配置真实授权站点；首页把“全部站点（数量）”与刷新拆为独立操作，只预览前三项，完整站点页支持搜索、下拉刷新和紧凑打开；今日日程已接入 OA todos，并完成新增、编辑、完成及重启恢复；“全部应用”已按当前桌面 v1.0.80 的数据边界改为只投影服务端工作台目录，保持服务端名称、顺序、图标、路由和账号权限，不再按本地分类重排或混入日程、公告、工作群组、审批中心、设备与诊断快捷入口；空目录显示真实空态；主导航校正为“工作台、消息、待办、通讯录、我的”，待办不再隐藏在二级页；授权站点保留服务端 `backupLoginUrls`，按主地址到备用地址逐一探测并失败关闭 | 补服务端实际配置备用地址后的真机切换和多站点搜索正向取证；同账号桌面/移动端授权逐项对照仍需安全登录条件 |
| IM 会话 | 单聊、群聊、文本、图片、视频、音频、文件、名片、回复、@、表情、搜索、已读回执、收藏、消息助手 | 单聊/群聊严格按服务端类型隔离；真实群聊创建、单聊删除与重建、图片/视频/音频发送、鉴权下载及重启持久化已通过；普通文件和联系人名片也已核对为真实上传/联系人 ID 接口，元数据进入账号隔离的本地加密缓存；会话检索现可通过单聊对端的姓名、终端账号或部门命中已有单聊，群名与最近消息继续独立匹配；已读回执按桌面对象契约展示已读数/总接收人数和成员；回复入口遵循服务端消息配置；收藏和消息助手任务记录保留桌面端页码、页容量及总数，移动端在接近底部时自动读取后续页；消息助手同源任务接口和权限门控已实现 | 为测试账号授予 `batchSend` 后补助手正向任务取证；补收藏和助手超过 50 条的真实数据分页取证；补有已读成员的数据分支；依赖服务端历史消息全文检索能力后再补非最近消息命中 |
| 会话管理 | 置顶、免打扰、清空、成员分页/搜索、复制群聊、管理员、禁言、入群审核、操作记录、退群、解散、群历史全量删除 | 群成员详情已改为 10 人紧凑预览和服务端 50 人分页列表，支持账号/姓名搜索及真实在线状态；管理员、禁言名单、入群审核和操作记录也已按桌面端改为服务端分页并跨页去重，四类列表接近底部自动加载，五分类在移动端同屏紧凑展示；群历史任务已真实提交但后端持续 `Pending` | 后端任务执行器恢复后补 `Completed` 正向取证；补真实大群翻页和待审核入群申请 |
| OA 附件 | 上传、删除、缩略图、安全图片预览、原文件下载、附件字段绑定、桌面草稿附件租约 | 已对齐：移动端提交时上传本地文件，将完整附件对象写入动态表单字段并发送 `attachmentBindings`；详情接入 `/thumbnail`、`/preview` 和原文件下载，图片可缩放预览，其他文件交给系统应用；原始附件元数据不再重复显示；缩略图离屏自动释放并复用账号隔离的 48 项 / 32 MB LRU，按显示尺寸解码 | 已用真实申请 `OA-20260901-C5084D` 完成新附件上传、提交、详情预览、强制重启后再次预览和撤回清理；移动端草稿保存本地文件并在提交时上传，因此不需要桌面端租约 |
| 通讯录 | 组织树、好友、好友申请、搜索账号、单条/批量接受、拒绝、真实在线状态、导航角标 | 已接入同源 `/api/im/departments` 部门树；添加联系人按桌面端改为完整终端账号显式搜索；进入组织/好友/群聊页与手动下拉均主动刷新服务端在线状态，并合并并发请求；realme 真机真实显示 3 名组织成员及在线/离线状态，群聊与联系人保持隔离 | 补多条真实好友申请批量接受正向取证 |
| OA 审批 | 待处理、已发起、抄送、参与、发起、详情、处理 | 分页、状态/时间筛选及服务端允许的高级动作已实现；待办已成为底部主入口，底栏角标直接投影 OA bootstrap 中带 `operableTask` 的真实待处理审批数；六个分类压缩后完整显示，草稿和待同步数量使用 14dp 内联角标且包含读屏数量语义；动态表单的部门字段优先使用真实部门树路径和服务端排序，不再只从联系人反推，旧缓存仍安全回退；详情采用 5 分钟热重开并在到期后释放，主动刷新和业务动作仍精确重取 | 继续真机覆盖非零待处理角标、无数据及真实多级部门选择分支；最新版桌面窗口解除更新阻断后补双端状态对照 |
| 通知 | OA 通知、IM 会话通知、好友申请、考勤异常、全部/未读、公告、跳转、游标分页 | 主通知中心按桌面端统一时间流合并本地真实会话投影与 OA 游标通知；单聊、群聊、好友申请、审批和考勤使用独立图标及类型。`targetKind/targetId` 安全跳转审批、真实会话或通讯录；考勤 `category/type` 跳到补卡异常页，存在异常 ID 时置顶高亮，没有 ID 时安全回退列表；会话已读调用 IM 已读接口而不是误写 OA 通知。通知内容、未读状态和首屏游标元数据按账号持久化 | 验证正式推送到达、超过 100 条、相同事件去重；服务端考勤通知补异常 ID 后再做精确定位正向取证 |
| 考勤 | 打卡、记录、异常、补卡、岗位行为 | 已完成真实上下班打卡、记录刷新、完成态和冷启动持久化；岗位定义、活动状态、开始/结束/返回岗位及近 30 天历史均已对齐桌面端真实接口 | 补真实岗位定义、岗位历史和缺卡异常正向数据 |
| 个人与设置 | 个人资料、账号、安全、通知、设备、外观、帮助、关于 | 昵称/签名真实读写、账号改密、真实设备授权、真实推送状态、账号语言读写、浅色/深色/跟随系统主题、手动版本检查及诊断信息复制已接入；个人页在线、隧道、设备和账号安全状态均改为真实数据；改密成功退出时同步清除旧记住密码，防止下次启动继续使用失效凭据 | 服务端提供反馈工单接口后补线上提交 |
| 安全接入 | 站点、隧道状态、设备安全 | 站点和策略接口已实现；Android Flutter 插件、VpnService 权限边界、内核探测与 SHA-256 校验已接入 | 提供受信 mihomo Android 内核和 TUN 桥接后完成真实连接回归 |
| 管理安全 | 签名策略、心跳策略版本、远程终端指令、强制下线 | 已按桌面端 `HMAC-SHA256-HEX` 规则验证 `/api/client/policies/current`；心跳上报真实策略版本与 TUN 状态；每 25 秒及前台恢复轮询 `/api/client/commands`，Android 执行强制下线并清除记住凭据，截屏/录屏明确回传平台不支持 | 需后台下发专用强制下线指令后补真机正向验收；网关节点健康等待 Android 原生核心可用 |

## 本轮已确认问题

- 移动端“添加联系人”误用了 `/api/im/search`，桌面端实际使用 `/api/im/members/search`。
- 好友申请只可发送，原移动端没有查看、接受和拒绝入口。
- 弹层连续关闭并刷新数据会触发 Flutter `_dependents.isEmpty` 红屏。

## 已修复并真机验证

- 移除应用网格“只有图标没有功能”的状态：静态“日程”和“公司公告”分别压栈进入真实日程待办、通知中心；服务端应用目录为空时仅展示已有真实路由的内置能力，不再放出缺少模板的审批占位图标。同步把工作台和全部应用的二级入口由替换路由改为压栈导航，修复真机从日程返回直接退出应用；公司公告使用 `?tab=announcements` 直接选中公告分类，并支持同页参数变化。真机连续完成“全部应用 → 日程 → 返回 → 公司公告 → 返回”，日程读取已有真实待办、公告显示服务端真实空态；12 张视觉基线和 105 项自动化通过，关键崩溃日志 0 条。
- 对齐桌面端群管理分页：移动端原先遗漏独立管理员列表，并把禁言名单、待审核入群和操作记录固定为 `page=1&pageSize=100`，超过 100 条会静默遗漏。现管理员、禁言、入群、记录各自保留服务端 `page/pageSize/total`，跨页按成员或业务 ID 去重，并在接近底部时自动加载；五分类同屏、不使用横向滚动。真机真实群返回禁言 0、入群 0、管理员 1、记录 5，管理员页显示真实账号与群主身份，记录完整且不足一页时不出现多余提示；后续自动分页边界与模拟器证据见 604 报告。
- 底部主导航从错误的四项“消息、工作台、通讯录、我的”校正为五项“工作台、消息、待办、通讯录、我的”，未添加无真实能力的扫码、搜索或悬浮加号。待办底栏角标使用 OA bootstrap 的真实可处理审批数量，当前测试账号为 0，真机按真实零值隐藏；自动化覆盖非零 `5` 的显示。待办六分类由固定宽度改为紧凑自适应宽度，`待处理/我发起的/抄送/已完成/草稿/待同步` 在 realme 真机同屏完整显示，真实待同步数以浮动角标呈现。五个入口逐项切换、强制停止和冷启动均通过，关键崩溃日志 0 条。
- 待同步队列补齐失败记录处置闭环：原页面对永久失败项只有重复“重试”，无法清理且直接暴露英文 `Approval form validation failed.`。现改为中文“申请表单校验失败，请检查必填项”，每项使用紧凑更多菜单提供重试与放弃记录；放弃前明确说明只删除本机失败记录、服务端不会收到申请，并要求二次确认。真机验证菜单和确认弹窗后选择取消，7 条现有记录全部保留，未擅自删除测试数据，关键崩溃日志 0 条。
- 抄送已读同步从待办列表入口下沉到审批详情页：无论从待办、通知还是其他深链打开，详情加载到当前成员未读抄送后都会异步调用 `/api/oa/approval-requests/{id}/cc/read`，不阻塞详情展示；成功后统一刷新详情、OA bootstrap 和游标通知页。新增页面行为测试证明未读抄送只触发目标申请的已读动作；真机从真实通知进入审批详情正常，当前数据不是未读抄送分支，因此未伪造服务端正向证据。
- 修复通知目标契约丢失：移动端 `OaNotification` 原先没有解析/缓存桌面端已有的 `targetKind/targetId`，点击任何业务通知都只能尝试打开审批。现完整保留两字段，`oa_approval` 打开审批、`im_conversation` 使用经过裁剪和路径编码的真实 conversationId 打开聊天、`im_friend_requests` 直达通讯录“新朋友”；空会话 ID 失败关闭，未知旧通知继续按 `requestId` 兼容。模型往返、目标路由、非法目标及新朋友直达共新增 3 项自动化测试。真机验证“新朋友”真实空态及旧审批通知回退入口，关键崩溃日志 0 条；当前服务端数据未下发 IM 目标通知，未伪造正向通知。
- 修复 Android 通知点击的启动时序：推送通道改为先于在线状态、IM 和 OA 同步初始化；`onNewIntent` 在 Flutter EventChannel 尚未监听时不再静默丢弃路由，而是暂存并由 `getInitialNotification` 消费。真机冷启动受控好友申请 Intent 在 5 秒内进入“新朋友”；普通启动后 0.4 秒追加的 single-top Intent 同样正确选中通讯录并完成真实接口空态加载，关键崩溃日志 0 条。
- 考勤岗位状态已接入桌面端同源真实接口：定义、活动状态、开始、结束、一键返回岗位及近 30 天历史；历史以紧凑列表合并到考勤页，展示起始时间、时长、进行/结束和超时状态。测试账号当前无服务端定义及历史，真机显示真实空态。
- 真机在服务端休息日规则下完成真实上班和下班打卡，两条 `terminal` 记录在覆盖安装、强制停止和冷启动后仍存在。同步修复完成态仍显示“上班打卡”及页面时钟静止的问题，完成态改为“今日打卡已完成”，时钟按秒刷新。
- 2026-08-31 真机继续压缩考勤完成态：班次与实时钟同排，不再保留大号禁用按钮，服务端 `punchMessage` 只显示一次；顶部状态卡由 687px 降至 342px，最近打卡行同步压缩且不改变真实记录。
- 补齐桌面端群主专属“删除所有人的聊天记录”：调用 `POST /api/im/groups/{id}/history-deletion-jobs` 创建异步任务，最长轮询 60 秒读取状态，完成后清理本地加密消息缓存；入口严格受 `canDeleteAllHistory` 控制，要求填写原因并输入完整群名。真机独立测试群已真实提交，但后端任务持续 `Pending`，移动端现区分失败与后台处理中状态；独立群已解散，主验收群未受影响。
- 底部导航角标改为桌面端同源 `/api/im/badges`：消息显示服务端未读总数，通讯录显示待处理好友申请数；同步事件到达时自动刷新，接口短暂不可用时消息角标回退到会话未读汇总，避免显示假数字。
- 通讯录补齐桌面端真实部门目录：新增 `/api/im/departments`、父子层级/排序模型和账号隔离缓存；移动端以紧凑路径分组展示，保护循环及孤儿部门数据，接口或旧缓存不可用时继续按联系人自带部门显示，不阻断通讯录。当前真机旧令牌失效，已验证缓存回退和重启稳定，正向部门树等待有效账号。
- 添加联系人搜索不再随输入逐字符并发请求，改为与桌面端一致的“完整终端账号 + 显式搜索”；404 显示真实空结果，其他接口错误留在弹层内且不会形成未处理异常。好友申请接口成功后，即使 bootstrap 刷新暂时失败也保持成功结果，防止用户被误导再次提交。realme 真机已用真实账号 `qa.term.testingdepartment.member` 搜索并提交申请，服务端重复提交按幂等成功处理。
- 通讯录联系人在线状态继续直接使用服务端 bootstrap 的 `isOnline`，页面停留期间每 30 秒主动刷新真实投影；组织联系人与好友申请列表的分割线已移除。真机缓存中的两个测试终端当前均由服务端标记为离线，没有伪造在线状态。
- 2026-08-31 通讯录组织、好友、群聊三视图继续按钉钉式平面列表收敛：移除占满剩余屏幕的大圆角空卡和组织头部分割线，搜索、四个 Tab、头像、真实在线/离线、好友操作与群聊入口保持不变；组织、好友、群聊仍严格隔离。
- 2026-08-31 聊天资源栏继续按移动端密度收敛：选中线定位到 38dp Tab 的真实底边，文字独立居中；输入栏单行态压缩到 40dp，表情并入输入框，附件和发送保持独立触控入口，多行仍可向上增长。
- 2026-08-31 待办主入口移除占满剩余屏幕的大圆角外壳，六分类、38dp 搜索与筛选、草稿和待同步真实角标保持不变；真机未操作现有失败记录。
- 待同步失败记录现以 77dp 紧凑行同时展示处理方式、真实尝试次数、更新时间和具体错误；同名记录可以按时间区分。校验失败仍只允许修改后重提或放弃记录，不暴露无效的普通重试。
- 消息通知页将 FCM/厂商远程到达能力明确命名为“离线推送”，与“应用内同步”和 Android 系统通知权限分开表达；无运行时令牌时不再用服务端旧记录误报已启用，状态卡压缩为 60dp。
- 移动首页公告已上移到问候区与常用应用之间，只显示最新一条标题，行高不超过 48dp；无公告与无今日日程均不渲染空卡片，工作台不暴露常用站点。真机当前无公告数据，已验证不占位；有公告顺序与密度由受控数据和 Golden 覆盖。
- 通知中心从旧的一次性 `/api/oa/notifications?take=100` 改为桌面端同源 `/api/oa/notifications/page`，并进一步按桌面端投影规则把 OA 通知、真实 IM 会话和待处理好友申请合并成一个时间流；顶部只保留“全部/未读”，单聊、群聊、审批使用独立图标和标签。列表行压缩到 64dp 级别，去除外围大卡片和三组胶囊筛选；打开会话通知时同步 IM 已读状态，不再调用 OA 通知已读接口。
- 通知中心顶部“全部/未读”已分离计数投影，不再用当前筛选页数量覆盖另一类数量。realme 真机修复前切到未读后从“全部 28”错变为“全部 3”，修复后稳定保持“全部 28 / 未读 0”，单聊、群聊与 OA 边界未改变。
- 通知首屏补齐进程级离线恢复：按账号持久化内容、未读状态、`nextCursor` 和 `hasMore`，OA 事件刷新时与事件游标同一事务落库；标记已读后直接更新三个本地投影，不再删除缓存。realme 真机关闭唯一可用的蜂窝数据并确认网络不可达，强制结束、冷启动、再次进入通知中心后仍完整显示 28 条 / 21 条未读；在线打开一条真实未读后降为 20，再次断网杀进程仍保持 28 / 20，证明已读状态不是页面临时值。
- 授权站点补齐桌面端主备地址解析：移动端不再丢弃 `backupDomains/backupLoginUrls`，使用无终端鉴权头的独立客户端按主地址、备用地址顺序探测；接受 2xx/3xx 和需要业务自身鉴权的 401/403/405，全部不可用时失败关闭，不盲目打开无效 URL。当前真机会话已失效，不能把缓存外的旧站点作为正向证据；主失败、备用 401 可用及不泄露终端 `Authorization` 已由本地 HTTP 服务测试覆盖。
- 修复 IM 已读回执契约错误：桌面端 `/api/im/messages/{id}/read-receipts` 返回包含 `readCount/totalRecipientCount/readers` 的对象，旧移动端却只读取数组或 `items`，因此会把真实回执误报为 0 人。现改为强类型对象，紧凑弹窗展示“已读 x/y”、成员和读取时间；真机对单聊消息真实返回 `0/1`，没有再把总接收人数丢失。服务端 `config.message.reply=false` 时长按菜单也不再显示回复。
- 修复改密后的旧凭据残留：`POST /api/client/change-password` 成功后，移动端退出会话时现在同时清除 refresh token 和记住密码；否则冷启动静默重登会继续使用旧密码。出于不破坏当前共享测试账号，未在真机实际改密；凭据清除和不可恢复由安全存储自动化覆盖。
- 修复通知分页后的投影刷新遗漏：审批提交/处理、高级动作、个人待办、打卡、工作台刷新和 OA 同步事件现在同时失效旧通知缓存与新游标页。真机点击 1 条真实未读“审批已提交”通知，进入详情后返回，未读数从 1 消失、蓝色未读点清除，证明服务端读状态和分页页面联动完成。
- “外观与语言”不再只读显示账号语言：接入桌面端同源 `PUT /api/im/preferences/language`，提供简体中文、繁體中文、English 紧凑选择器及保存状态。真机完成简体→English→强制停止重启→简体→再次重启完整闭环，最终账号设置已恢复。
- “主题模式”已由固定浅色文案改为真实的浅色、深色、跟随系统三态，使用本地偏好持久化并驱动 Material 明暗主题；共享容器、底部导航、输入框及工作台、IM、通讯录、OA、设置主路径均改用主题色。真机切换深色并强制停止重启后保持，随后已恢复浅色并再次冷启动确认。
- 网络与安全页补充真实管理策略状态：读取 `/api/client/policies/current` 的桌面端同源 envelope，以登录响应密钥执行 HMAC-SHA256 恒定时间比对，通过后才展示启用状态和版本。真机成功校验 `prod-2026.08.20.001`；令牌失效时显示紧凑提示和重试，不暴露网络异常堆栈。
- 2026-08-31 网络与安全页继续按移动端信息密度收敛：安全通道不可用时不再误报“设备不支持”或暴露 `mihomo` 实现名，而是明确显示安装包缺少“移动端安全组件”；不可用态隐藏空策略版本、0B 流量等无意义诊断行，只保留安全组件与真实运行环境。realme 真机状态卡由 756px 压缩到 480px，管理策略 `prod-2026.08.20.001` 仍真实显示，未伪造安全连接。
- 补齐远程终端指令轮询与结果回传。移动端只执行平台适用的强制下线（类型 3），先三次重试回传成功，再清除会话和记住密码；桌面截屏/录屏类型回传平台不支持。同期修复认证刷新重建 GoRouter、深层页面跳回工作台的问题，真机在安全页停留超过一个轮询周期仍保持当前路由。
- “关于”页补齐桌面端可见的手动版本检查：用户主动点击后请求 `/api/client/versions/latest`，区分无发布版本、已是最新、发现新版本和失败重试，发现版本时可查看发布说明并交给系统下载；手动检查限制为 12 秒反馈。当前真机旧会话下真实接口不可用，已验证 12 秒内进入可点击重试状态，未伪造“已是最新”。
- 群详情不再一次渲染全部成员：首屏最多展示 10 人，新增紧凑“查看全部”底部页，调用桌面端同源 `/api/im/conversations/{id}/members/page`，每页 50 人，支持服务端姓名/账号搜索、页码、总数、群主/管理员身份及真实在线状态；群主仍可从分页列表进入成员管理。
- 在岗巡检已接入桌面端同源真实接口：应用启动/回前台检查、标记打开、提交在岗或无法响应；当前账号没有活动巡检任务。
- 个人头像已接入桌面端同源 `PUT /api/im/profile/avatar`：移动端可选择本地图片、校验服务端 400 KB Data URL 上限，头像随 bootstrap 刷新并在重新安装覆盖后继续显示。
- `@ 我` 不再只依赖 bootstrap 序列展示：进入会话并读到对应消息后，会调用 `/api/im/mentions/unread` 定位可见记录，再调用 `/api/im/mentions/read` 同步服务端已读状态。
- 修复工作台“今日日程”长期固定空态且错误跳转审批页：现在直接投影 OA bootstrap 的真实 `todos`，独立紧凑日程页接入 `POST /api/oa/todos` 和 `PATCH /api/oa/todos/{id}`，支持新增、编辑、完成与恢复。
- 真机新增 `MOBILE_TODO_20260825`、标记完成、编辑为 `MOBILE_TODO_EDITED_20260825`，强制停止重启后仍保持完成状态；同时修复底部表单关闭后刷新触发 `_dependents.isEmpty` 红屏。
- 移除个人与账号安全页的固定状态：在线来自 IM bootstrap，安全连接来自 `TunnelPhase`，设备来自 `/api/im/devices/authorizations`，账号状态同时校验当前会话与真实在线状态；真机显示 `安全连接不可用`、`realme RMX3366 · android` 和 `已验证·在线`。
- 2026-08-31 账户与安全页将密码可见性控制收回到三个 42dp 输入框尾部，每个字段独立切换，移除额外“显示密码”行；确认按钮从 924×144px 通栏收敛为右对齐 396×120px，表单内容高度从 828px 降到 696px。真机只点按空字段的可见性图标并恢复隐藏，未输入、未提交、未更改密码。
- 补齐桌面端单聊“删除会话”语义，严格调用 `DELETE /api/im/direct-conversations/{id}`，不再以清空本地记录或群聊删除接口代替。真机删除“测试-成员测试”后会话从列表消失，重新发起单聊并发送 `DIRECT_RECREATED_20260825`，强制停止重启后会话和消息均恢复。
- 补齐桌面端“复制群聊”：仅群主/管理员显示，调用 `POST /api/im/groups/{id}/copy` 并携带 `includeMembers/copyProfile=true`。服务端要求群至少 3 人；真机补足成员后复制成功，副本包含 3 名成员与原群资料，重命名为 `MOBILE_COPY_REAL_20260825`，覆盖安装和强制重启后保持，随后已解散副本。
- 修复群名称编辑弹窗关闭动画期间提前销毁控制器导致的 `_dependents.isEmpty` 红屏，并将复制/移出成员的服务端错误提取为可读信息。进一步确认移出接口存在“服务端已提交、响应仍为 400”的情况：移动端现会在异常后回查成员列表，并将成员读取改为服务端优先、缓存兜底。真机冷启动和 SQLite 均确认原群已恢复为 2 人。

- 精确搜索真实终端账号。
- 发送好友申请。
- 接收方“新朋友”显示数量和申请记录。
- 接受、拒绝操作。
- “新朋友”已补桌面端同类“全部接受”，采用紧凑文字按钮和二次确认，真实调用 `POST /api/im/friends/applications/pending/accept-all` 并显示处理数与剩余数。
- 接受后联系人出现在“好友”列表。
- 移动端每 25 秒发送真实终端心跳，联系人在线状态来自服务端 IM bootstrap；已用第二终端会话验证当前 Android 账号为在线。
- 单聊标题栏已将真实在线状态提升为首要副文案：在线显示“在线”，离线显示“离线 · MM-dd HH:mm”。会话 Presence 未到达时回退到 bootstrap 的 `isOnline/lastSeenAt`；完整账号和部门放在个人资料页，不再把状态挤到省略区。群聊继续显示成员与在线人数，不套用单聊状态。
- 单聊与群聊操作已按会话类型隔离；真机长按单聊消息仅显示单聊适用操作，不再出现“设为群置顶”。
- 图片消息已对齐桌面端独立模型：一次选择 1–9 张，调用 `/api/im/conversations/{id}/images`，本地加密缓存 `images[]` 元数据，并通过鉴权下载、聊天气泡预览和全屏缩放；真实群聊发送及强制重启恢复已通过。
- 消息助手已对齐桌面端 `batchSend` 权限：只有服务端授权时显示入口，可选择多个真实联系人、创建文本批量发送任务、查看接收/成功/失败进度并取消运行中任务；任务记录与收藏列表均保留服务端分页元数据并支持移动端加载更多。当前真机账号服务端返回无该权限，因此入口保持隐藏，没有伪造正向结果。
- 视频/音频已对齐桌面端通用媒体接口：先上传 `/api/im/upload/{video|audio}`，再创建 `/api/im/conversations/{id}/media-messages`；附件元数据加密写入 SQLite，点击气泡经鉴权下载后交给系统播放器。真实 WAV、MP4 发送和强制重启恢复均已通过。
- 会话类型改为 `direct/group/unsupported` 三态；未知类型不会再落入单聊分支，单聊与群聊创建接口若返回错误类型会直接拒绝打开。
- 单聊列表不再直接显示服务端“自己、对方”组合标题，改为从真实会话成员中排除当前账号，只显示对方姓名；群组筛选仅包含 `type=group` 会话。
- 消息页“@ 我”原只有标签但没有筛选逻辑；现已按桌面端 `unreadMentionSequences` 真实字段过滤，并将序列持久化到 SQLite schema v7，防止重启后退化。
- 管理后台已为测试终端配置“创建群组”账号权限；真机创建 `Mobile-Group-20260825`、加入真实成员、发送 `GROUP_REAL_DEVICE_20260825` 和重启后发送 `GROUP_AFTER_RESTART_20260825` 均成功，群组筛选只显示真实群会话。
- 修复 IM bootstrap 本地缓存遗漏 `permissions/config`：群聊权限和消息能力现在随缓存写入并在进程重启后恢复，不再退回默认权限。
- 历史消息按桌面端接口使用 `beforeSequence + take=80` 向前分页；聊天列表靠近顶部 72dp 时自动请求，不再显示“加载更早消息”或失败后的手动重试按钮；加载失败保留现有历史，再次上滑即重试。真机已验证无更早数据时进入明确终态。
- 修复 IM 同步的全局重建：任意事件不再失效所有会话的消息、成员和群资料，改为按事件类型和真实 `conversationId` 精确刷新；会话消息窗口退出后保留 5 分钟，重进不重新读取，向前分页使用稳定消息 Key 保留已有元素。realme 真机单聊/群聊各连续重进 8 次，P95 分别为 6.62 ms / 7.87 ms，超过 16.7 ms 的渲染帧均为 0，关键异常 0 条。
- 2026-08-31 消息主列表与通讯录统一为整页平面列表，移除数据少时占满屏幕的大圆角空卡；会话内缩分割线、群聊标签、未读、时间、真实单聊头像状态及四个筛选保持。新增热重开回归直接证明立即重进不会再次调用消息窗口 Loader。
- 图片与媒体二进制缓存改为按账号隔离的 48 项 / 32 MB LRU，Provider 离屏自动释放；气泡缩略图按显示宽度与设备 DPR 解码，全屏预览保留原图，全局解码缓存限制为 120 项 / 48 MB。修复前群聊重复进入第 21–40 次 Graphics 仍增加 60,344 KB；修复后同阶段只增加 24 KB，PSS 只波动 4,187 KB，证明进入稳态而非继续线性增长。
- 消息收藏已完成新增、列表、进程重启持久化与取消收藏真机闭环；列表已去除多余分割线，并支持超过 50 条继续加载。
- 失败消息支持立即点按重试，SQLite 出站队列状态恢复与立即到期已有自动化覆盖。
- 当前会话补充同账号遗漏事件的有界历史校准：12 秒读取一次最新 50 条元数据，只在服务端窗口真实变化时合并缓存并精确刷新当前会话；真机旧缓存已在重新登录后恢复模拟器发送并由服务端持久化的群消息。相同账号第二端登录会使第一端真实接口失效，因此并发双端结论必须改用两个不同员工账号，当前仍等待第二账号安全凭据。
- “登录设备”已由固定文案替换为桌面端同源 `/api/im/devices/authorizations` 数据；真机自动登记为 `realme RMX3366 / android`，并显示其他授权设备、最近活动和撤销入口。
- 通知中心已增加独立好友申请分类，复用真实待处理申请接口并支持直接接受/拒绝；真机空态与自动化有覆盖。
- 消息通知页已移除未被运行时消费的本地假开关，改为读取真实推送设备及隐私模式；当前真机明确显示尚未注册推送服务。
- 外观与语言页主题和账号语言均为真实选择：主题保存在设备本地并支持浅色/深色/跟随系统，账号语言读取并写回 `/api/im/preferences/language`。
- “我的”页头部资料卡片已接入 `/api/im/members/{id}/profile` 与 `PUT /api/im/profile`，昵称和签名可真实读写，保存后刷新 bootstrap。
- 好友操作菜单已接入成员资料中的现有备注与 `PUT /api/im/friends/{memberId}/remark`，保存后刷新真实通讯录。
- 群详情新增紧凑“群管理”入口，接入管理能力、禁言成员、入群审核和群操作记录接口；真机将成员禁言 24 小时后返回“禁言 1”，解除后恢复“禁言 0”，服务端记录同步新增两条。
- 工作台“常用站点”已移除固定空数据，改为读取 `/api/client/sites?deviceId=...`；服务端直测 169ms 返回且当前账号数量为 0，真机定位为旧会话 401。页面已停止无限重试，并明确提示重新登录。
- 已在真机退出并重新登录，授权站点正常返回 0 条并显示真实空态。
- 管理后台已为测试终端分配真实“数据中台”站点；真机首页显示真实域名并成功打开系统浏览器，连续重启后仍可自动恢复站点列表。
- 补齐桌面端已有的会话续期语义：登录响应持久化 refresh token，401 时按场景执行刷新或使用已记住凭据静默重登；修复重启后站点 401、自动退出以及一次刷新状态自触发循环，真机 CPU 回落至空闲且关键异常为 0。
- 顶部安全连接图标已从固定绿色假状态改为直接投影 `TunnelPhase`；当前真机安装包未包含原生隧道，如实显示橙色断开状态。
- 帮助与反馈已移除“真机验收日志可用”的内部占位文案，改为真实复制应用版本、设备、系统和隧道状态的诊断信息。
- 修复 `MobileShell` 在初始化异步链路中已卸载后继续读取 Riverpod `ref` 导致的真机异常；协调器改为初始化时缓存，每个异步阶段检查 `mounted`。
- `Packages/secure_tunnel` 已从纯 Dart 不可用占位升级为 Android Flutter 插件：注册 Method/Event Channel、`VpnService` 权限边界、运行时 ABI/内核路径/版本/SHA-256 身份、策略文件哈希校验和状态事件。未携带受信内核时仍失败关闭，不建立空 TUN 或伪连接。
- 授权站点请求收到 401 时不再把异常留在 Riverpod 错误流中，而是清理旧会话并回到登录页；真机已验证保留账号密码填充且无 `E/flutter`。
- 整个流程日志无 Flutter 异常。

## 2026-08-31 审批发起页运行窗口复核

- 当前 Windows v1.0.80 请假审批抽屉已确认不再提供“申请标题”编辑框；移动端同步移除该冗余字段，系统默认标题以及旧草稿/再次发起标题仍在提交载荷中保留。
- 动态表单必填标识直接来自服务端 `required` 配置；附件、日期、选择、只读计算和文本字段统一显示 `*`，不额外增加说明文案。
- 移动端继续采用单列字段、内联附件和直接展开的审批流程；底部保存/提交操作限制为 116dp、140dp 宽和 42dp 高并右对齐，不复制桌面双列布局。
- realme 真机已完成覆盖安装、冷启动、工作台进入请假审批和截图复核；没有提交申请、保存新草稿、删除附件或修改线上审批数据。
- 2026-08-31 OA 移动交互统一为底部抽屉：单选、多选、人员/部门、筛选、操作菜单、确认、日期、日期范围和时间不再使用居中弹窗或悬浮下拉；图片附件改为全屏页面。动态表单按服务端 schema 解释标签、值、公式、只读与单位，不再把内部配置名拼成字段正文。realme 真机打开并取消请假类型、日期、时间和待办新建抽屉后，现有草稿字段及附件均保持不变；静态检查 0 问题，185 项自动化全部通过，关键异常 0 条。
- 2026-08-31 动态表单进一步补齐当前后台可视化设计器契约：数字/金额字段解析 `calculation/readOnly/unit/scale/roundingMode`，支持链式公式、精度和四种舍入方式，除零、缺失引用、循环依赖及溢出失败关闭；金额不再硬编码 `¥`，单位只取 schema 尾缀，页面不把“自动计算/只读”等内部配置作为独立文本。realme 真机线上“分级请款审批”v1 真实读取到“请款金额”单位 `CNY`，当前下发版本仍允许编辑，因此移动端没有按字段名臆测公式；0 静态问题、189 项自动化通过、关键异常 0 条，未输入、保存或提交业务数据。
- 2026-08-31 动态表单继续对齐后台默认值、文本校验和附件配置：支持 `requester/requester_department/today/now/fixed` 默认来源、长度与 TRON 地址校验、`multiple/maxCount/imagePreview` 附件策略，并在提交前遍历完整 schema，避免长表单离屏字段漏校验。realme 真机确认“请假天数”不暴露“自动计算/只读/单位天”等原始配置；线上分级请款真实带出部门和请款人，申请日期未收到默认配置时仍保持“请选择”。0 静态问题、191 项自动化通过、关键异常 0 条，全程未修改线上表单数据。
- 2026-08-31 以当前已登录桌面 v1.0.80 再次核对请假发起页：桌面端时长字段显示完整用户说明“根据起止时间自动计算自然日”，而不是隐藏全部说明或暴露原始配置。移动端已按 `durationUnit` 同步自然日/小时两种说明，单位仍只作数值尾缀、字段保持不可聚焦；realme 真机页面与 UI 树确认目标说明存在且“自动计算”“只读”“单位天”均没有作为独立配置文本出现。Profile APK 覆盖安装成功，0 静态问题、191 项自动化通过、关键异常 0 条，未修改线上业务数据。
- 2026-08-31 IM 会话重开性能继续收敛：上滑加载后的消息窗口大小和“历史已到底”状态在应用会话内按最近 32 个会话有界保存，退出再进入不再退回固定 80 条或重复请求已耗尽分页；10 秒内同会话的最新消息核对合并为一次请求，12 秒双端补偿同步周期保持不变。列表继续使用 `ListView.builder`、稳定消息 Key 和滚动锚点，只构建可见区域并在插入旧消息后保持阅读位置。realme 真机真实群聊连续重开 10 次后群/单聊边界、1 人在线、视频预览和输入区均正常，Activity 始终 1、View 始终 7，空闲后 PSS 从首次打开的 256211 KB 回落到 242378 KB；0 静态问题、194 项自动化通过、关键异常 0 条。当前真实群仅 6 条缓存消息，超过 80 条的服务端正向分页仍需有足量历史的数据账号。
- 2026-08-31 通讯录在线状态不再只等待 25/30 秒周期刷新：进入组织、好友或群聊页及手动下拉时都会主动请求服务端 IM bootstrap，并合并同一时刻的重复刷新；离线时保留最后一次服务端投影。realme 真机确认组织页真实显示当前账号在线、另外两名账号离线，群聊页仅显示真实群会话，联系人没有混入；无黑色分隔线。Profile APK SHA-256 为 `F8173A01EDACFCDB91BE00682AECA0CCB19EF63A499C898A4B66425B7C5A1204`，0 静态问题、195 项自动化通过、关键异常 0 条。
- 2026-08-31 OA 永久失败记录补齐具体校验原因：服务端顶层 `errors[]` 的每条字段消息会与校验摘要一起写入本地 outbox，不再只留下 `Approval form validation failed`；待同步记录的向上操作抽屉新增“查看失败原因”，长消息可完整阅读，永久校验失败仍只允许修改后重提或放弃，不显示无效重试。realme 真机只读打开现有 7 条旧失败记录和原因抽屉，未重试、修改或删除；旧记录在修复前已丢失字段明细，不能追溯恢复。Profile APK SHA-256 为 `B43A34A92687E8D1445DD4EBB81267222D80F0F3FBBA201EB44E98B43F6253CB`，0 静态问题、196 项自动化通过、关键异常 0 条。
- 2026-09-01 通知中心补齐考勤异常跳转：只按服务端 `category/type` 识别考勤，不解析标题或中文正文；列表显示“考勤”标签和异常日历图标，点击进入真实补卡页。若后续服务端下发异常 `targetId/requestId`，对应异常会置顶并轻量高亮；当前线上通知不带可匹配 ID，因此安全回退到 19 条待处理异常列表。realme 真机从 28 条混合通知分别打开考勤异常和 `Mobile-Group-20260825`，群聊仍显示 2 位成员、1 人在线及视频预览，两类通知没有混线。Profile APK SHA-256 为 `ACC2FB86196A1885B70C6D7247B6B032A115AC6A2456759AFFD5D22B597FEDB4`，0 静态问题、197 项自动化通过、关键异常 0 条。
- 2026-09-01 动态表单补齐旧数据隔离：正常字段继续直接读取后台 `formSchemaJson`，`calculation/readOnly/unit` 仅控制行为和单位，不进入表单值；旧草稿、再次发起或旧接口若误把配置对象放进字段值，移动端会在渲染前剔除，避免把“自动计算/只读/单位天”通过 `toString()` 暴露。当前桌面 v1.0.80 与 realme 真机均只显示“根据起止时间自动计算自然日”，Profile APK SHA-256 为 `B59FC33A17D88E5EFA626AC56A945ADF5A2BFC3D134EC976C4205C3750A5485A`，0 静态问题、198 项自动化通过、关键异常 0 条，未修改线上业务数据。
- 2026-09-01 在岗巡检通知不再被空 `targetId` 和归一化后的 `oa_approval` 目标误导：移动端只按服务端 `inspection` 类型识别，显示独立“巡检”标签，点击进入考勤页并实时查询活动巡检；旧通知没有活动目标时从底部明确提示“当前没有待确认的在岗巡检”，不再无响应。Shell 自动巡检的居中对话框同步替换为底部选择抽屉，并加入并发互斥。realme 真机完成真实旧通知只读验证，当前无活动巡检，未调用打开或响应写接口；Profile APK SHA-256 为 `E541B10A7253D170F1990280E8013C5A6FD286D5EEC30F68FAD3B6A5A6D9DB3E`，0 静态问题、199 项自动化通过、关键异常 0 条。
- 2026-09-01 通知统一时间流增加同通知 ID 精确去重，不使用标题、正文、日期或空 `requestId` 猜测事件身份。真机两条相同文案的“在岗确认”经只读诊断确认是 `23:00:42` 与 `23:32:44` 生成的两个不同 ID，属于两次巡检，Profile 中正确保留两条；同 ID 重复投影由测试证明只保留一条。Profile APK SHA-256 为 `8857CBAFD84FDFBC95A4B86FBC3E4FD3D227441844EF757C9F2ACF0D1827609E`，0 静态问题、200 项自动化通过、诊断日志和关键异常均为 0，未修改线上数据。
- 2026-09-01 登录页修复记住密码语义泄露：安全存储中的密码不再写入输入框控制器或 UI 树，只在提交时于内存使用；修改账号/密码会释放页面引用，明确 401 会删除失效凭据。“忘记密码”同步改为底部消息抽屉。模拟器 Profile UI 树确认密码字段 `text` 为空，真机既有会话正常进入工作台；Profile APK SHA-256 为 `3578DC86CEAC0BDFA3C93C87F84337D8C08A9334A480B33673F8F9976FFE3524`，0 静态问题、202 项自动化通过、双设备关键异常 0。双端 IM 发送因模拟器无可用会话且没有环境变量凭据而未执行。
- 2026-09-01 后台可视化表单的数据边界补齐到发起页与审批详情：配置对象及序列化的“自动计算，只读，单位…”均不会作为业务值显示，真实单位、只读行为和“根据起止时间自动计算自然日”说明仍由 schema 驱动。待办六分类改为自然宽度横滑，搜索类输入统一 34dp、普通输入约 36dp，角标固定 16dp 且不随字体缩放撑高，空状态同步上移。realme 真机只读打开既有请假草稿并横滑核对“草稿箱 1 / 待同步 7”，未填写、保存、提交或处理本地记录；Profile APK SHA-256 为 `E9E6597ADB244F7ADDAB6C34214A8EF1243A29B76E9702FAF1A176EAB3B53C77`，0 静态问题、204 项自动化通过、关键异常 0 条。
- 2026-09-01 通用移动端单行编辑框固定为 36dp，搜索框保持 34dp；好友操作、备注/问候语编辑、消息编辑、已读回执、群名称/公告编辑与确认操作统一为底部抽屉，去除居中对话框和悬浮菜单。编辑抽屉按真机复看继续去除强蓝色描边，改为浅灰输入区、主副标题和紧凑右对齐操作。realme 真机 UI 树实测通讯录搜索框 34dp、备注输入框 36dp，只打开后取消，未保存备注或发送消息；Profile APK SHA-256 为 `8EBF4A8F329623A90D097F0DB7CD44931EF12B63C64587534BA93EF915675A0B`，0 静态问题、204/204 自动化通过。
- 2026-09-01 “添加好友”按当前登录桌面端与 realme 真机双端实测重做：完整终端账号精确查找后只显示一条紧凑成员卡，输入值保持可见，当前账号过滤为未找到；操作严格读取服务端 `canStartDirect`，桌面“老王”对 `test03` 显示“发消息”，移动 Codex 账号因权限不同显示“申请好友”，不再用 `isFriend` 猜测或点击整行立即触发。同步补齐 `person/work/badge/support/security` 五种桌面预设头像键并接入 IM/OA 人员头像主路径。真机只读查找确认 Test Terminal 03 的头像、集团总部和离线状态正确，未发送申请或消息；Profile APK SHA-256 为 `218F0CD076B62B5DEA7E091C42C64D930394233219B3F4CBF5D3BE2066107D51`，0 静态问题、207/207 自动化通过。
- 2026-09-01 完成真机 Codex 测试终端到当前桌面老王的真实单聊发送：测试消息经服务端持久化，真机重启后仍恢复，桌面打开会话后可从历史记录读取；移动端发送和历史链路通过。桌面会话列表 15 秒内未更新旧预览，打开消息后未写已读回执（真机查询仍为 0/1），运行中的桌面账号也被服务端投影为离线且最近时间停在 01:45，因此双端实时体验暂不通过。补充自动化固定移动端按最新可见 `sequence` 上报会话已读，0 静态问题、208/208 自动化通过。反向桌面发送需操作前确认，尚未执行；完整证据见 `docs/578-dual-end-im-sync-audit-20260901.md`。
- 2026-09-01 工作台继续保持移动端紧凑结构：公告仅在真实存在时于常用应用上方显示一条，空公告、空日程和常用站点均不占首页空间。修复全部应用把服务端目录压成单块九宫格的结构回退，按服务端首次出现顺序恢复考勤、费用、财务、采购、行政分类，分类内保持应用顺序，搜索只保留命中分类；32dp 图标、五列布局和无单项边框不变。realme 真机打开分组页及请假审批入口正常，未修改或提交草稿；Profile APK SHA-256 为 `396DD6ED4BECA9B3110F874E71405466080D12270E2DD5E5BB8CFC5038F23484`，0 静态问题、208/208 自动化通过、关键异常 0 条。完整证据见 `docs/579-workbench-all-apps-grouping-audit-20260901.md`。
- 2026-09-01 待办六分类保留横滑但增加左右 22dp 渐隐边缘，360dp 真机滑动后不再露出孤立残字，名称和 16dp 数量角标保持完整。共享选择、确认、文本、日期、日期范围、时间抽屉及审批详情直接抽屉统一挂到根导航，真实“同步操作”抽屉已覆盖底部主 Tab 和安全区，不再形成 Shell 内半截弹层。realme 真机只查看 7 条旧待同步记录及操作抽屉，未修改、重试或放弃；Profile APK SHA-256 为 `D10920B3310EB5A6285B253F30803B518BDB200412093CA0984742E2CEA506AC`，0 静态问题、209/209 自动化通过、关键异常 0 条。完整证据见 `docs/580-todos-tabs-and-root-sheets-audit-20260901.md`。
- 2026-09-01 通讯录真机回归确认组织、好友、群聊、新朋友四分类边界清晰：在线/离线继续取服务端投影，好友仅显示真实好友，群聊仅显示真实群会话，列表无黑色分隔线，成员头像正常。修复“添加好友”将非好友的 `canStartDirect` 误当成好友关系的问题：现在已是好友才显示“发消息”，非好友统一显示“申请好友”；真机对测试成员完成精确查找并收到“好友申请已发送”服务端成功反馈。添加好友、联系人操作和修改备注都使用覆盖底部 Tab 的紧凑上拉抽屉，软键盘不再遮挡内容。Profile APK SHA-256 为 `1B418CBA9C4BC5212F5BC8945B394038829F569DD2B65EA62AF0C32875DB412C`，0 静态问题、209/209 自动化通过，清空旧日志后重放关键链路异常 0 条。完整证据见 `docs/581-contacts-friend-chain-audit-20260901.md`。
- 2026-09-01 OA 动态表单完成真实创建、附件提交、进程重启回查和撤回清理闭环。修复可编辑字段把实时值放入 Widget Key 导致每输入一个字符就重建并丢焦点的问题；后台 schema 驱动的选择、日期、自动计算和展开审批流程保持不变。AI-UAT 申请 `OA-20260901-C5084D` 提交后为审批中，重启后仍可查看同一表单与服务端附件，随后已撤回，审批任务变为已取消。审批意见、驳回与撤回原因统一为紧凑上拉文本抽屉。Profile APK SHA-256 为 `A56120053F1C1E5171C02B2AE75602C0FAD1366F611B08461A38B23C2FA3AD4F`，0 静态问题、209/209 自动化通过、关键异常 0 条。完整证据见 `docs/582-oa-editing-attachment-withdraw-audit-20260901.md`。
- 2026-09-01 清理移动端残留的破坏性居中弹窗：退出登录、撤销设备授权和删除所有人的群聊天记录全部改为覆盖底部 Tab 的紧凑抽屉。群历史删除要求原因非空且完整群名精确匹配才启用 36dp 确认按钮，键盘弹起时标题、风险说明、输入区和按钮均保持可见；强制更新的阻断弹窗与图片全屏预览按语义保留。realme 真机三处均只打开后取消，未退出、未撤销设备、未删除群消息。Profile APK SHA-256 为 `2F3CADBB40E406C4D5463D82DD3C6F8DFE9ADCC533B51118C1D3E766DDEC0E3C`，0 静态问题、210/210 自动化通过、应用进程关键异常 0 条。完整证据见 `docs/583-mobile-destructive-confirmation-sheets-audit-20260901.md`。
- 2026-09-01 外观/语言和考勤选择交互继续统一：主题模式、账号语言、通知锁屏内容均复用根导航共享选择抽屉；真实补卡表单移除大号默认描边输入与通栏提交按钮，改为浅灰原因区、36dp 时间/取消/提交操作，并继续串联底部日期与时间选择器。realme 真机从真实通知进入 19 条考勤异常列表，打开 2026-08-31 缺上班卡，依次检查日期和时间后全部取消，未填写原因或提交补卡。Profile APK SHA-256 为 `E3D9F2E8D945B34E738AEACBA625257D15D753B8817328C3E449FCC462B4B7DC`，0 静态问题、210/210 自动化通过、应用进程关键异常 0 条。完整证据见 `docs/584-mobile-choice-and-correction-sheet-audit-20260901.md`。
- 2026-09-01 消息发起会话补齐根导航与安全区，保留已有 34dp 搜索、36dp 群名及单聊/群聊严格分流；OA 转交/加签成员选择改为 34dp 搜索、50dp 人员行和紧凑标题，人员账号、部门、头像与服务端动作参数不变。0 静态问题、210/210 自动化通过，Profile APK SHA-256 为 `138857854E25A2F99DBD4E9DE7FF64423568C7876A588F2437521B31C79D0E03` 并已覆盖安装。真机随后进入系统锁屏，权威状态为 `Keyguard showing=true / InputRestricted=true`，未绕过锁屏，视觉复验待解锁；完整边界见 `docs/585-im-oa-picker-density-implementation-20260901.md`。
- 2026-09-01 “新建待办”从默认大号表单收敛为覆盖底部 Tab 的紧凑抽屉：待办内容、优先级和操作区均为 36dp，输入框去描边并使用浅灰底，取消/创建按钮右对齐且创建按钮宽度小于 120dp。定向测试曾真实发现视觉密度把按钮二次压缩到 28dp，修正后加入实际尺寸断言；0 静态问题、210/210 自动化通过，Profile APK SHA-256 为 `8C0B64BEAB5849CB22776B47E8DC0989D42B6C85FD839F0C63F708E4392B236F` 并已覆盖安装。真机仍为 `Keyguard showing=true / InputRestricted=true`，未绕过锁屏、未创建线上待办，视觉复验待正常解锁；完整边界见 `docs/586-todo-create-sheet-density-implementation-20260901.md`。
- 2026-09-01 真实打开当前已登录桌面 v1.0.80 的通讯录与“添加好友”面板后，确认桌面链路按服务端 `canStartDirect` 决定“发消息/申请好友”，移动端原先按 `isFriend` 判断会把可直接会话成员误导为重复申请。移动端已改为能力字段驱动，精确终端账号搜索、当前成员排除、单聊/好友申请接口和群聊隔离保持不变；0 静态问题、210/210 自动化通过，Profile APK SHA-256 为 `70F51002C77049D483EEEFF63D23BA21E6011302A70DD152F9169A66B4B7748E` 并已覆盖安装。桌面端只打开/关闭面板，未输入账号或发送申请；真机仍锁屏，结果页触控复验待解锁。完整证据见 `docs/587-add-friend-routing-alignment-20260901.md`。
- 2026-09-01 IM 继续清理默认大控件：单聊“新建共同任务”改为根导航紧凑抽屉，任务名称、四段优先级和创建按钮均为 36dp，按钮右对齐且宽度小于 120dp；“分享联系人”改为 34dp 搜索、50dp 人员行和 17dp 头像，并保留真实部门、账号与在线状态。共同任务仍严格只在单聊显示，群聊没有混入；19/19 IM 定向回归、210/210 全量自动化与 0 静态问题通过，Profile APK SHA-256 为 `0BE2D6EE4D2FC9E07AFF48B3011A5D24EB8AF2F70FC0D596752561E152B21413` 并已覆盖安装。真机仍锁屏，未创建任务或发送联系人名片；完整边界见 `docs/588-im-task-contact-picker-density-20260901.md`。
- 2026-09-01 群成员交互继续压缩：“添加群成员”采用 34dp 搜索、50dp 复选人员行和右对齐 36dp 操作；“从单聊发起群聊”采用 36dp 群名称、50dp 成员行和 36dp 创建按钮；“查看全部群成员”采用 34dp 搜索、54dp 人员行并移除逐行分隔线。真实姓名、账号、部门、群角色和在线/最近上线状态保持，创建群聊和添加成员仍分别受服务端权限与群管理身份限制。两个定向用例、210/210 全量自动化与 0 静态问题通过，Profile APK SHA-256 为 `CC13D261ED4CA9223D8CCD374B8B554F818FDF684B6ED1CD28C016A3C9153A81` 并已覆盖安装。真机仍锁屏，未添加成员或创建群聊；完整证据见 `docs/589-group-member-sheets-density-20260901.md`。
- 2026-09-01 独立日程应用的新增/编辑表单改为根导航紧凑抽屉：标题 36dp、备注 64dp、优先级与截止时间 36dp，日期/时间继续使用向上展开选择器，取消/保存右对齐且保存按钮为 36dp、宽度小于 120dp。首页仍不展示空日程模块，服务端个人待办新增/编辑/完成语义不变。日程定向用例、210/210 全量自动化和 0 静态问题通过，Profile APK SHA-256 为 `267D2FB5D67BC69F2EAC547C6B76B861899C91D50C7362ECFBEAA670C44F80DC` 并已覆盖安装。真机仍锁屏，未写入线上日程；完整边界见 `docs/590-schedule-editor-density-20260901.md`。
- 2026-09-01 会话内搜索改为 34dp 搜索框与 34×34dp 关闭按钮；群聊 `@成员` 改为根导航抽屉、34dp 搜索和 50dp 成员行，并从 `members.map` 全量组件创建改为 `ListView.builder` 视口懒构建。新增 2001 人群自动化证明首屏不创建第 1999 名成员、可见 `ListTile` 少于 40，精确搜索后才构建目标；`@全体` 仍受服务端群资料与消息配置双重权限控制。211/211 全量自动化和 0 静态问题通过，Profile APK SHA-256 为 `82B6E63610F5269E9B5AE917BA5CB1198443C395B0751A378CAB51208B8F6F97` 并已覆盖安装。真机仍锁屏，未发送搜索内容或提及消息；完整证据见 `docs/591-chat-search-mention-virtualization-20260901.md`。
- 2026-09-01 通讯录“添加好友”和“选择部门”移除系统默认拖拽条，统一为覆盖底部 Tab 的紧凑根导航抽屉；添加好友继续使用 34dp 完整账号搜索与真实会话权限，部门选择保留真实部门树、人数及 44dp 行。17/17 定向、211/211 全量和 0 静态问题通过，Profile APK SHA-256 为 `905ABD7E415421BF83E7FF4D9E6FB258C0BD3CF6C7CE42709CF997A6D02EFA67` 并已覆盖安装。真机仍锁屏，未发送好友申请；完整证据见 `docs/592-contacts-sheet-density-implementation-20260901.md`。
- 2026-09-01 群成员管理、消息长按操作和转发目标选择统一为根导航移动端选择抽屉。消息操作继续按服务端配置和实际权限生成，单聊不暴露群置顶，群聊才显示；转发目标明确标识单聊/群聊并在目标较多时提供搜索；危险操作保持二次确认。20/20 IM 定向、211/211 全量和 0 静态问题通过，Profile APK SHA-256 为 `BCA056301AB0A2E691361072DEAC44871E8B2483AB32A9E1279F2D8B32040BFA` 并已覆盖安装。真机仍锁屏，未执行转发、禁言或成员变更；完整证据见 `docs/593-im-action-sheet-alignment-20260901.md`。
- 2026-09-01 在 1080×2400 Android 模拟器上使用隔离 Demo Profile 包真实点击工作台、消息、群聊、单聊、提及成员、消息操作、转发、通讯录、部门选择、添加好友、待办与新建待办，共留存 16 组 PNG/XML。运行中发现底部待办角标仅统计审批而显示 1、待办页总数为 4；现统一为“未完成任务 + 可处理审批”，模拟器复验两处均为 4。群聊/单聊操作边界、媒体渲染、真实在线展示和抽屉密度均通过只读复验，日志无 Flutter/Android 崩溃。212/212 自动化、12/12 Golden 和 0 静态问题通过；生产 Profile APK SHA-256 为 `873151EE4FED0CD80E5EF8779FD96D2E4494366DCAA7FBAEE7CD83F2302646B1` 并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/594-emulator-runtime-and-todo-badge-audit-20260901.md`。
- 2026-09-01 在同一 Android 模拟器继续真实点击 OA 待办、审批详情、筛选、新建请假表单、类型下拉与日期选择，共保留 11 组关键 PNG/XML。审批详情仅有一至两个次要操作时改用紧凑选择抽屉，多操作仍保留图标宫格；加签方式同步复用统一移动端选择抽屉。新建页保持后台 schema 字段和流程直接展开，没有把“自动计算、只读、单位天”等可视化配置串显示为业务值，全程未保存、提交或审批。213/213 自动化、12/12 Golden 和 0 静态问题通过；生产 Profile APK SHA-256 为 `803A4990D898A83E7E2297DF7C7D623ECE5F892890A8A0897867DEE0843076F1` 并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/595-emulator-oa-runtime-and-action-density-20260901.md`。
- 2026-09-01 直接操作当前已登录桌面 `v1.0.80`，真实核对工作台、六类待办、已撤回审批详情以及单聊；发现最新版新增“纯表情放大、混合文本保持普通字号”，移动端原先未覆盖。现只对完全由 Unicode Emoji 组成的文本消息使用 30sp，支持 ZWJ 组合、肤色、旗帜、变体和数字键帽，表情加文字不放大；模拟器单聊已实际渲染复验，群/单聊边界、媒体与输入区不受影响。21/21 聊天定向、214/214 全量、12/12 Golden 和 0 静态问题通过；生产 Profile APK SHA-256 为 `D4415F7BBA8692B29B94713FEDF3A19B90A30A9B99ABEDBA18A644EDFD5733FF`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/596-desktop-v1080-emoji-alignment-20260901.md`。
- 2026-09-01 继续只读操作当前已登录桌面 `v1.0.80`：真实群“2000人工作群”首次加载快照显示 `2 位成员 · 2 人在线` 且发送工具禁用，完成加载后权威状态为 `2018 位成员 · 2 人在线`，输入和媒体工具恢复可用，证明首次状态是权限未知时的失败关闭而非真实禁言。移动端现将群资料加载时序及 `muted/status/currentUserRole` 接入输入区：资料未确认时禁止发送，全员禁言时普通成员禁用，群主/管理员保留权限，关闭或解散群统一禁止发送，单聊不受影响。原报告中“真实群已禁言”的推断已经纠正；本轮历史产物为 22/22 IM、215/215 全量、12/12 Golden 和 0 静态问题，后续大群分页优化见 598 报告。
- 2026-09-01 以当前运行桌面 `v1.0.80` 的真实 `2018 位成员 · 2 人在线` 大群为基准，移动端群聊首屏从完整成员加载改为 50 人服务端分页，标题和详情使用分页真实总数，在线数继续读取 presence；会话类型未解析时不再误触发完整请求。提及抽屉对大群使用 250 ms 防抖与服务端关键词分页；群资料未确认时失败关闭，SQLite v10 持久化 `currentUserRole` 后再按群主、管理员、普通成员和群状态开放发送。2001 人自动化证明首屏完整成员接口 0 次且能搜索到第 2000 名成员；23/23 IM、216/216 全量、13/13 SQLite、12/12 Golden 和 0 静态问题通过。模拟器已真实点击 6 组页面且关键异常 0 条；生产 Profile APK SHA-256 为 `B9742D61E10D5DCA2B283A56958680040CC4F0215E7003FCEDA21CE6341D20D5`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/598-large-group-paging-and-permission-20260901.md`。
- 2026-09-01 直接操作当前已登录桌面 `v1.0.80` 的真实通知中心，确认它是 IM、审批、考勤和巡检等应用内事件的统一时间流，而不是手机系统通知设置。移动端保留“全部 / 未读”入口，补齐群聊、单聊、好友、审批、考勤、巡检、公告、安全和兜底应用标签，并停止把所有空目标 OA 事件强制归为审批。模拟器真实点击通知中心、未读筛选及审批跳转均通过；44/44 定向、217/217 全量和 0 静态问题，关键异常 0 条。生产 Profile APK SHA-256 为 `53350D46BF35C0D1B6D57F3451F8DFE748D1760D29E14176FFDE14DFDE41C7B1`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/599-notification-source-boundary-20260901.md`。
- 2026-09-01 通知中心移除手动“加载更多”，改为距底部 240dp 自动取下一游标；第一页不足一屏时自动补齐，临时失败保留既有列表并只在下一次真实上滑时清除该游标错误后重试，避免立即重试循环。“全部 / 未读”游标互相隔离且切换后回到顶部。最终模拟器真实上滑后 UI 树确认没有“加载更多”，来源标签与列表稳定，关键异常 0 条；220/220 全量和 0 静态问题通过。生产 Profile APK SHA-256 为 `4E5108CD6CEFC2DD53DAC7F691360861B1F1ECD0B274A080533CFF3BB05F1A34`，设备端哈希一致并已覆盖安装 realme。真实账号当前只有 34 条通知，无法只读触发第二页；真机仍锁屏。完整证据见 `docs/600-notification-auto-paging-20260901.md`。
- 2026-09-01 IM“我的收藏”和“群发助手”任务记录移除手动“加载更多”，改为距底部 240dp 自动分页；收藏短首屏自动补齐，群发任务只由外层时间流滚动触发，嵌套接收人列表不会误发请求。两页按消息/任务真实 ID 去重，下一页为空结束分页，失败保留既有内容并等待新上滑手势重试。模拟器真实打开两页后 UI 树确认不再显示“加载更多”，关键异常 0 条；4/4 定向、221/221 全量和 0 静态问题通过。生产 Profile APK SHA-256 为 `4AE1EDA6F0611EB016C33D1DFDA4461FFECD34CD29A3FC67AA54189189870A64`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/601-im-favorites-assistant-auto-paging-20260901.md`。
- 2026-09-01 OA 待办审批列表移除“加载更多/加载更多记录”，改为距底部 240dp 自动取下一游标；首屏不足或筛选结果当前页为空时自动继续查找，按申请 ID 去重，游标不前进时停止，失败后等待新上滑或明确“重新加载”。切换分类、搜索和筛选会隔离旧分页状态。模拟器真实打开待办与筛选底部抽屉，UI 树确认没有手动分页按钮，关键异常 0 条；45/45 OA 定向、223/223 全量和 0 静态问题通过。生产 Profile APK SHA-256 为 `B5A57ED304F7C6E6CBB8CDD8B9E94985A72846FC3FA06C4BA7257F5ED14B2EAF`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/602-todos-auto-paging-20260901.md`。
- 2026-09-01 通讯录组织/好友列表移除手动“加载更多”，保留每批 30 人的窗口化渲染并在距底部 240dp 时自动扩展；搜索、分类和部门切换会重置窗口。在线点、在线文字和最近上线时间仍只取服务端成员投影，群聊与直接联系人继续严格分流。65 人自动化证明末位成员仅在上滑后构建；模拟器真实切换组织、好友、群聊，UI 树确认没有手动分页按钮且分类内容不混用，关键异常 0 条。18/18 冒烟、224/224 全量和 0 静态问题通过。生产 Profile APK SHA-256 为 `4147886702CDFBEC580A6E36641740ABE9DFC45A39C8980CB82B6C487AC5DC7F`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/603-contacts-auto-window-20260901.md`。
- 2026-09-01 群管理的禁言、入群申请、管理员和操作记录四个列表移除手动“加载更多”，统一为距底部 240dp 自动分页；空首屏自动补页，标签切换隔离旧请求，成员/记录 ID 去重，页码不前进或没有新增数据时停止，失败保留内容并提供明确重试。在线状态、头像、群身份和服务端管理权限不变。4/4 定向、228/228 全量和 0 静态问题通过；模拟器真实沿群聊详情进入群管理并切换全部标签，UI 树没有手动分页按钮，关键异常 0 条。生产 Profile APK SHA-256 为 `7D3CB222D12EEF3539C5240695961C3DF1C947AFB864BB2912213A98B1256C36`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/604-group-management-auto-paging-20260901.md`。
- 2026-09-01 真实操作当前已登录桌面 `v1.0.80` 的“通讯录 → 添加好友”，确认完整账号查找后继续按服务端 `canStartDirect` 区分发消息与申请好友，“新的朋友”和群聊边界不变。模拟器同路径输入 `laowang` 时发现加载中空列表被提前当作结果读取，稳定触发 `Bad state: No element` 红屏；现先处理加载、失败和查无结果，只有非空结果才读取成员。新增异步等待/失败回归后通讯录 19/19、全量 229/229、0 静态问题；模拟器重放关键异常 0 条。生产 Profile APK SHA-256 为 `CBFC3FE04EBDB11DDFE13C8E20F872977DFA4F0BBDA644C30BC91CCFD20CD4B6`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏，未发送消息或好友申请；完整证据见 `docs/605-add-friend-search-stability-20260901.md`。
- 2026-09-01 当前运行桌面 `v1.0.80` 的“新的朋友 0”仅显示小号“暂无好友申请”，移动端据此移除内容区大号添加好友图标和粗体空标题，改为顶部 44dp、13sp 弱化文本；顶部添加好友入口不变，空列表继续支持直接下拉刷新。模拟器真实打开后 UI 树确认提示边界 `[32,543][1049,593]`，关键异常 0 条；全量 229/229、0 静态问题。生产 Profile APK SHA-256 为 `433586A2B1B06605BE58073C8227A964A24FBCFD811B192A356B7E677DA86CBA`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏，本轮未创建或处理线上好友申请；完整证据见 `docs/606-new-friends-empty-density-20260901.md`。
- 2026-09-01 真实操作当前运行桌面 `v1.0.80` 的 Codex 测试终端单聊，确认自己发送的消息直接提供“已读”入口，详情由服务端返回 `1 / 1 已读`、具体成员和时间。移动端据此为自己发送、已落库且账号具备 `readReceipt` 权限的消息增加双勾“回执”；点击时才请求真实明细，不在列表构建时逐条预取，也不把未知状态伪装成已读。入口复用既有消息间距，自动加载更早消息和热重开测试不回退；底部详情抽屉挂到根导航。25/25 IM、231/231 全量、视觉基线和 0 静态问题通过；模拟器真实单聊关键异常 0 条。生产 Profile APK SHA-256 为 `5DCBE76222776DAE208C75D273D1CCF2C06920A3B154E140161E83CBB9844CBB`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏，桌面只读查看已有回执，未发送消息；完整证据见 `docs/607-im-read-receipt-alignment-20260901.md`。
- 2026-09-01 使用独立 x86_64 Profile Demo 包核查启动和会话重开性能：模拟器三次冷启动为 6829/6221/3783ms，并各有一次 Choreographer 跳帧，当前不能判定冷启动通过；三次热返回为 1074/730/372ms 且跳帧 0。连续 20 次返回消息列表再打开唐泽单聊后跳帧和关键异常均为 0，PSS 从 171180KB 回落至 111861KB，没有持续内存增长。首帧前代码没有等待网络、数据库或协作同步，因此未为模拟器数字删减真实初始化；冷启动仍需解锁 realme 后复测。测量后已恢复生产 Profile APK，SHA-256 仍为 `5DCBE76222776DAE208C75D273D1CCF2C06920A3B154E140161E83CBB9844CBB`；完整证据见 `docs/608-profile-startup-and-chat-reopen-20260901.md`。
- 2026-09-01 当前已登录 Windows 桌面端真实出现 `v1.0.81` 强制更新提示；移动端据此把启动自动检查和“关于”页手动检查统一为根导航底部抽屉。强制更新不可拖拽、遮罩关闭或返回，且没有关闭/稍后入口；可选更新可以稍后处理。版本、包大小、发布说明和 40dp 下载按钮使用紧凑布局，不再使用居中大弹窗。模拟器真实进入“关于 → 检查更新”，服务端对移动端返回“暂无发布版本”，未伪造桌面发布结果；234/234 自动化、13/13 Golden 和 0 静态问题通过。Production Profile APK SHA-256 为 `FE0057E2C8C4691FC1E5F32DAE5DFB79B5F81815DD315FE7A046FCDC6B30E1A7`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏，桌面更新下载/安装待用户明确确认；完整证据见 `docs/609-mobile-update-sheet-20260901.md`。
- 2026-09-01 在 1080×2400 模拟器重新截图检查工作台、全部应用、消息、群聊、待办、通讯录和我的。主路径整体密度、五项底部导航、群/单聊边界、在线/最近上线状态及扁平列表保持稳定；“全部应用”在后台仅下发两个应用时仍被分类与网格留白撑高，现保持服务端分类、顺序、图标、权限和路由，仅把双分类目录表面压缩到 172dp 以内，图标仍为 32dp。19/19 冒烟、234/234 全量、13/13 Golden 和 0 静态问题通过，模拟器复拍关键异常 0 条。构建时同时发现增量 APK 遗留旧 `libapp.so` 内容导致 79,660,326 bytes 膨胀；清理生成目录后最终 Production Profile 为 65,819,655 bytes，SHA-256 `4953D2C2EC068CCC62C9C9C9A5E873685321927196DF40F1DF8D7D01632CF2F5`，设备端哈希一致并已覆盖安装 realme。真机仍锁屏；完整证据见 `docs/610-mobile-visual-audit-and-all-app-density-20260901.md`。
- 2026-09-01 realme 解锁后完成生产会话真机回归：三次冷启动 TotalTime 710/650/652ms，后两次跳帧和关键异常均为 0；工作台、五项底导、消息列表和老王单聊真实数据恢复。最新已登录桌面 v1.0.81 与移动端均显示 `Codex 测试终端` 当前服务端自定义 Flutter 头像，不是移动端漏载。已读入口移除重复“回执”文字，双勾改为与气泡同行并保留真实懒加载详情；真机已读数据为 1/1，单成员抽屉按 52dp 内容高度收紧。25/25 IM、234/234 全量、13/13 Golden 和 0 静态问题通过；本轮未发送新消息或修改业务数据。完整证据见 `docs/611-real-device-core-regression-and-receipt-density-20260901.md`。
- 2026-09-01 待办页六个分类进一步压缩为 56dp 等分单行同屏，数量徽标保留同行；realme 真机真实显示 `待同步 7`，无需横滑且没有换行、残字或遮挡。筛选、新建、请假类型和日期选择均以底部抽屉展开；线上请假 v1 表单继续按后台 schema 渲染，流程直接展开，已通过详情未暴露 `自动计算/只读/单位天` 等配置串。44/44 OA 定向、234/234 全量、13/13 Golden 和 0 静态问题通过；Profile APK SHA-256 为 `02198EB27A3B0E633FE5993E48B8856FD3BB25462CACC91FC6B08C39CBB64B40`，设备端哈希一致。本轮只读浏览，未保存、提交或处理业务数据；完整证据见 `docs/612-real-device-oa-density-and-sheet-regression-20260901.md`。
- 2026-09-01 真机补齐通知中心锁屏后的真实验收：当前 `全部 31 / 未读 2`，审批、单聊、群聊、考勤和巡检标签分流正确，长列表连续上滑没有手动“加载更多”，未读筛选不覆盖全部数量。同步去除“我发起的”个人待办行内重复的复选框与状态图标，只保留单个 40dp 完成控件，审批图标和完成语义不变。45/45 OA 定向、235/235 全量、13/13 Golden 和 0 静态问题通过；Profile APK SHA-256 为 `8F9BC12BB0BCC5264DB5261D7B0EA0CB0FE2BEE9789A48D5C5A4367E0D53996C`，设备端哈希一致。本轮未改变通知已读、待办完成状态或其他线上数据；完整证据见 `docs/613-real-device-notification-and-personal-todo-density-20260901.md`。
- 2026-09-01 “全部应用”名称与首页常用应用统一为单行省略，五列网格不再因长名称折行破坏行高；完整服务端名称仍保留在语义树和目标页面标题中。realme 真机确认 `分级请款审批` 显示为单行 `分级请款…`，点击进入真实 `测试 · 分级请款审批` 动态表单后返回仍停留在全部应用。19/19 冒烟、235/235 全量、13/13 Golden 和 0 静态问题通过；Profile APK SHA-256 为 `EE0FD38F8FB71155235D194B3D10C31A6C3B62695615EF71CE5182C2F8B71FE3`，设备端哈希一致。本轮未输入、保存或提交业务数据；完整证据见 `docs/614-all-app-label-single-line-real-device-20260901.md`。
- 2026-09-01 在当前最终包上逐页复验通讯录四分类：组织真实显示当前账号在线、另外两名成员离线；好友只含 1 位真实好友，群聊只含 `Mobile-Group-20260825`，新朋友为简洁空态。四页无黑色整行分隔线或大空卡，34dp 搜索提示按联系人/群聊类型切换，单聊与群聊没有混用。切换路径无崩溃或 `E/flutter`，本轮未搜索账号、发起会话或修改线上数据；完整证据见 `docs/615-real-device-contacts-presence-and-boundary-20260901.md`。
- 2026-09-01 消息回执继续按移动端密度收敛：移除“回执”文字，仅保留双勾入口并与消息气泡保持同一视觉行；点击后再请求真实 `已读 1/1` 明细。消息列表群聊头像统一为明确群组图标，单聊继续使用真实成员头像和在线点；真机群聊仍显示 `2 位成员 · 1 人在线`，视频首帧、播放按钮及时长正常。新增同行几何与会话类型回归，31/31 IM、235/235 全量、13/13 Golden 和 0 静态问题通过。最终 Profile APK SHA-256 为 `46E5ECAC03BD42AC511312086262970DB5BC95F3AADF1740924121F56EAE3A57`，设备端哈希一致；完整证据见 `docs/616-real-device-im-receipt-single-line-and-group-avatar-20260901.md`。
- 2026-09-01 使用真实测试员工跨账号完成 `OA-20260901-8990C6`：青山提交 4999 USDT 请款，林川、陈拓、苏敏依次同意，许致远单独同意后整单仍审批中，沈清同意后才转为已通过。申请人的中间通知、最终结果和抄送通知真实到达，分别打开结果和抄送后未读数从 14 降至 12。过程中发现详情永久优先本地快照会将已通过误显示为旧待办，现改为联网时服务器优先、仅无响应网络错误时按账号回退缓存；34 位地址也改为保持双栏同一行完整显示。模拟器和 realme 均重放同一最终通知并显示全部最新节点，240/240 全量、13 组 Golden 和 0 静态问题通过，两台设备关键异常均为 0；Profile APK SHA-256 `018E96B340763E3C3E66948F88A9E7C73782F49CA5088967E30150DE9FC1D331` 已安装两台 Android 设备。Windows 桌面终端当前已登录 `laowang` 但不是本申请审批人，未冒充完成桌面同申请证据；详见 `docs/617-oa-cross-account-real-flow-and-detail-freshness-20260901.md`。
- 2026-09-02 继续收敛“我的”设置：账户页隐藏内部设备 ID，只展示账号、可读设备名称和真实同步状态；修改密码改为紧凑底部抽屉，空表单禁用提交，失败保留输入，模拟成功后保留当前操作会话，仅要求其他设备重新登录。“我的”头像卡在成员同步中断时仍可进入个人资料，通知入口补充“消息、审批与公告”摘要；个人资料页移除重复账号，输入区压缩为 40/72dp、保存按钮为 104×36dp，远端资料未返回时使用本地安全会话和缓存资料，用户输入不被迟到响应覆盖。通知页统一入口和标题为“通知设置”，拆分“应用内通知/系统推送”，断网或推送状态加载失败时均保持单行重试状态。补充会话替换提示只产生并消费一次的协议回归。280/280 自动化、0 静态问题和 Profile 构建通过，SHA-256 为 `5E4DC347FBA8040603C2AD23B4238B6E1726C70FCE6E881713EE735EC4014734`，已覆盖安装 realme 与 Android 16 模拟器，关键异常 0。Android 尚未接入真实 FCM/厂商推送服务，页面未伪装成已启用；详见 `docs/622-account-security-mobile-interaction-audit-20260901.md`、`docs/624-notification-settings-boundary-audit-20260902.md` 与 `docs/625-profile-notification-offline-audit-20260902.md`。
- 2026-09-02 OA 待办补齐服务不可用语义：同步失败时保留 SQLite 本机投影并显示“当前显示本机记录”，无缓存时显示“本机暂无审批记录”，不再误报服务端“暂无审批事项”；“重新同步”期间收起离线栏，失败后恢复，不清理会话。realme 真机验证六分类可横向滑到“待同步”，筛选及事项类型选择继续使用根导航上拉抽屉；Android 16 模拟器同步复验离线状态。281/281 自动化、0 静态问题和 Profile 构建通过，SHA-256 为 `AF6D086E0F98F426966E15E8E66D56996F668D6B4F161ECF27D3FC5F3B87FA40`，已覆盖安装两台设备，关键异常 0；详见 `docs/626-todos-offline-state-audit-20260902.md`。
- 2026-09-02 工作台审批摘要与待办继续统一同步三态：冷启动显示“同步中 / 正在同步审批”，服务不可用后显示“本机记录 / 本机暂无审批记录”，仅在线同步完成且确实为空时显示“暂无审批事项”。工作台“本机记录”可进入待办；待办同步中使用 32dp 状态行，避免缓存为空时提前宣称无数据。设备授权后的 Presence、IM、OA 协调器改为并行启动，减少串行网络超时对 OA 状态的阻塞。realme 真机与 Android 16 模拟器完成冷启动、离线降级和跳转取证；284/284 自动化、0 静态问题和 Profile 构建通过，SHA-256 为 `09C7646130BC437C9F3E247FB768306F75960BB7037B2802DF0BC9E108E64904`，两端关键异常 0；详见 `docs/627-workbench-todos-sync-state-audit-20260902.md`。
- 2026-09-02 在同一最终包上重新逐页核对“我的”、消息列表、群聊和单聊：真机与模拟器两个账号的头像、姓名和部门未串用；群聊行与单聊行使用各自标识，连续消息头像和昵称只出现在五分钟消息组首，已读入口保持气泡同行双勾。真机单聊热重开 50 ms 已显示完整本地窗口，150 ms 稳定；`dumpsys gfxinfo` 本次采样 2 帧、Janky 0、P99 12 ms。服务离线时状态明确为“状态未知”，没有用旧 Presence 冒充在线；详见 `docs/628-profile-im-current-runtime-audit-20260902.md`。
- 2026-09-02 账户安全状态补齐实时连接三态：有会话且通道连接中显示“已登录·同步中”，超时后才切换为“已登录·同步中断”，通道可用时继续按真实成员 Presence 显示在线/离线。realme 真机冷启动完成同页状态转换取证，Android 16 模拟器复验最终中断状态；账户安全 3/3 定向、285/285 全量、0 静态问题和 Profile 构建通过，SHA-256 为 `EC9F841657590B4030F82053A43CF7CD65308A98134805B89BE48AD7FD871435`，已覆盖安装两台设备；详见 `docs/629-account-security-current-design-audit-20260902.md`。
- 2026-09-02 “我的”、登录设备和通知设置继续统一连接三态：成员资料未载入时不再闪现登录账号；登录设备冷启动使用紧凑“正在同步设备”，服务超时后切换为“暂时无法同步设备”和重试；通知设置同样区分正在连接、连接恢复后同步与实时同步。真机再次确认账户页不暴露设备 ID，修改密码为紧凑底部抽屉且空表单禁用提交。289/289 自动化、0 静态问题和 Profile 构建通过，SHA-256 为 `9B9408579999A36583613011053A2B913E8257D4CCFBE39BA992196B5DBE47D7`，已覆盖安装 realme 与 Android 16 模拟器；详见 `docs/630-profile-device-sync-state-audit-20260902.md`。
- 2026-09-02 IM 离线发送改为本地优先：文本、普通文件和联系人卡片先写入账号隔离的 SQLite 消息表与 Outbox，立即渲染时钟气泡并清空输入；网络超时、断网和 408/429/5xx 保持 `pending` 自动退避，明确永久错误才显示红色重试。realme 真机在服务完全不可达时发送 `AI-UAT` 消息并强制停止进程，重启后两条排队消息顺序、内容和状态均恢复且没有重复。53/53 定向、292/292 全量和 0 静态问题通过；Profile APK SHA-256 为 `14061AD71F4EEEF30536B71EC216550AEDC235BADA9A43675B30499295D9FF26`，已覆盖安装两台设备，关键异常 0。服务恢复后的真实补发确认及图片/视频/音频离线 Outbox 仍待完成；详见 `docs/631-im-outbox-offline-first-audit-20260902.md`。
- 2026-09-02 补齐 Outbox 严格 FIFO：最早消息处于退避期时，后续消息不会越过它先发送；手动恢复最早消息后按原创建顺序返回待发送队列。真机重新确认账户安全页不暴露内部设备 ID，修改密码保持紧凑底部抽屉且当前会话不提前退出。293/293 全量和 0 静态问题通过；Profile APK SHA-256 为 `BA0D405517BEBA7E3BA6A7E33E3A368F1C111BA55B823F3194650A8E86BCD3BF`，已覆盖安装 realme 与 Android 16 模拟器。媒体离线发送仍需账号隔离、受保护的文件队列，不能把大媒体直接 Base64 写入 SQLite。
- 2026-09-02 图片、视频和音频接入受保护媒体 Outbox：二进制使用按环境和账号隔离的 AES-GCM 文件队列，SQLite 只保存加密引用与元数据；上传前完整验真，再流式解密 multipart，服务端确认后才替换临时消息并删除队列文件。realme 真机在服务不可达时选择图片后立即显示真实缩略图，强制停止并重启后时间、发送中状态和文件计数均保持，未重复插入。298/298 全量、0 静态问题及 Profile 构建通过；APK SHA-256 为 `FCA3A49E4DA9265A2E742C3924AA73D76D3EA8EF368E6D2322E2917334B9C3C1`，已覆盖安装 realme 与 Android 16 模拟器。真实服务端恢复后的补发、桌面同步及真机视频/音频播放仍待取证；详见 `docs/632-im-media-outbox-offline-audit-20260902.md`。
- 2026-09-02 普通文件继续并入同一受保护附件 Outbox，新记录不再把文件 Base64 写入 SQLite；真机在服务不可达时依次排队 MP4、WAV 和 PDF，强停、冷启动和覆盖安装后内容、顺序、时间、发送中状态及文件计数保持且无重复。视频可从受保护文件离线打开；音频首次被 realme 音乐应用误接管，现改为应用内播放器，真机真实显示播放/暂停、进度和 0:01 时长，`AudioTrack` 正常启动停止。42/42 定向、300/300 全量、0 静态问题及干净 Profile 构建通过；APK 为 69,428,260 bytes，SHA-256 为 `EBD214F58B75398C341B55F0A73B222B9344EF3384387793AE1DE243EB63550B`，已覆盖安装两台设备。服务恢复后的真实补发、桌面同步及服务端确认替换仍待验收；详见 `docs/632-im-media-outbox-offline-audit-20260902.md`。
- 2026-09-02 使用同一最新 Production Profile 包补齐 realme 真机性能复核：五次先强停再启动均为 COLD，TotalTime 697/747/712/700/707 ms，中位数 707 ms；连续十次后台热返回 18–44 ms。通讯录仍按 2062 人组织树逐级展开，只在“外站”展开时创建四条成员行；点击林川头像进入已有单聊时本地窗口、头像和输入区直接可用。连续 40 次返回/重开后 PSS 从 255,299 KB 在运行中短暂升至 280,776 KB，空闲 3 秒回落为 254,574 KB，未见持续增长，关键异常 0；详见 `docs/633-real-device-startup-and-chat-reopen-performance-20260902.md`。
- 2026-09-02 真机消息列表修正跨天时间误导：此前 9 月 1 日历史会话在 9 月 2 日仍显示 `20:15` 等时刻，现按今天 `HH:mm`、昨天、本周星期、同年 `MM/dd`、跨年 `yyyy/MM/dd` 分层。realme 真机真实历史会话分别显示“昨天”“周一”“08/27”“08/23”，右侧列未挤压标题、预览或未读角标。9/9 定向、301/301 全量、0 静态问题和 Golden 通过；干净 Profile APK 为 69,493,796 bytes，SHA-256 `685FA098F28DE5D424D9DFE2AF7F665AF80A6D3FF66B547FBA874D221A66FF61`，已覆盖安装两台设备；详见 `docs/634-message-list-date-semantics-real-device-20260902.md`。
- 2026-09-02 通知中心与消息列表改为复用同一个移动端时间格式器：今天显示时刻、昨天显示“昨天”、本周显示星期、同年更早显示日期，正文中的业务日期不受影响。realme 真机 9 月 1 日通知统一显示“昨天”、8 月 31 日显示“周一”，群聊、单聊、审批、考勤标签和未读点保持独立且无挤压；Android 16 模拟器完成同包覆盖安装与启动。9/9 消息定向、1/1 通知定向、301/301 全量和 0 静态问题通过；干净 Profile APK 为 69,428,260 bytes，SHA-256 `88A58101245B4DFFC4E647FD2B7F3015C78CC033B17BB89E2B4D8AB3A5726745`；详见 `docs/636-notification-relative-time-real-device-20260902.md`。
- 2026-09-02 继续复核“我的”、个人资料和登录设备：头像卡只展示姓名与部门，个人资料服务超时后切换为“当前显示本机资料 + 重新同步”。登录设备无任何投影时移除误导性的“已授权设备”标题，连接中只显示同步状态，失败后只显示“暂时无法同步设备 + 重试”；存在真实设备数据时标题仍保留。realme 与 Android 16 模拟器均完成同状态取证，9/9 定向、302/302 全量和 0 静态问题通过；干净 Profile APK 为 69,428,260 bytes，SHA-256 `BAEE66113B49005DCF1F6D72AC4BFEB2B0F3B687410F05B48D031F5A6F419CF8`；详见 `docs/637-profile-and-login-devices-empty-state-20260902.md`。
- 2026-09-02 待办六分类在 realme 真机确认可横向滑动并完整进入草稿箱、待同步；本机草稿在服务不可达时继续按后台表单恢复。审批流程预览等待上限由 12 秒缩短为 6 秒，超时后停止转圈并显示“网络不可用，表单与草稿已保留”，独立重试不会清空表单。真机完成首次超时和重试复验，Android 16 模拟器覆盖安装；OA 55/55、全量 303/303、0 静态问题，干净 Profile APK 为 69,428,260 bytes，SHA-256 `939E081D6D9E036C814FF89445AED71E8BE37ACBA9194EF8CD7125312DEEA1ED`；详见 `docs/638-todos-draft-workflow-offline-timeout-20260902.md`。
- 2026-09-02 审批详情改为账号隔离缓存优先：服务不可达时直接展示 SQLite 快照和“当前显示本机审批快照”，不再先等待 20–30 秒网络超时；离线时隐藏更多、同意、驳回和再次发起等业务动作，手动重试不会清空详情或退出登录。同步修复通知行在导航前等待已读网络请求的问题，最终包单击一次审批通知后 595 ms 已进入详情、1,426 ms 稳定。realme 真机确认离线提示与操作保护；OA 59 项定向、通知导航 1/1、全量 306/306、0 静态问题，干净 Profile APK 为 69,493,796 bytes，SHA-256 `4B906D75C9FA87EDB80E249CD0477475D4ADA7DE6E9FACA87FC9C7F003EB5BF3`，已覆盖安装两台 Android 设备；详见 `docs/639-approval-detail-offline-snapshot-performance-20260902.md`。
- 2026-09-05 强制更新保持不可关闭的居中弹窗，但按真机视觉反馈改为纯白、无外边框、无阴影、无更新说明内层背景，仅保留 16 px 圆角和中性黑 36% 遮罩。模拟器覆盖安装时发现工作台品牌头在设备字体度量下溢出 1 px，已改为最小高度自适应并加入 130% 字体回归；最终模拟器与 realme 真机均保留登录、无布局溢出或 Flutter 异常。全量 1362/1362、Golden 与强制交互测试通过，静态检查 0 问题；APK SHA-256 为 `B07051DC471C189E9F9FC86A7771EC1FD499D55788C0E13ADA47B54CED1C7437`，详见 `docs/735-mandatory-update-dialog-background.md`。
- 2026-09-05 桌面端附件预览链路由另一侧继续修改，移动端登记为等待新版本与准确协议后复核；当前不并行开发独立 Office 预览引擎，也不根据旧源码或命令名猜测后台能力，详见 `docs/724-desktop-oa-attachment-preview-handoff.md`。
- 2026-09-05 用户确认五个主页面已基本达到预期，后续停止无必要的主页面视觉重构，转向会话、跨端、自动登录与 OA 流转。当前真机 test01 与模拟器 test03 均持有刷新凭据，命令/IM/OA 接口为 200；test03 在当前环境真实刷新返回 200并轮换访问与刷新令牌，账号/设备保持不变，恢复普通包后两端均自动进入工作台且关键异常为 0。当前未验签 JWT 时间提示约 2 小时，不再沿用旧 12 小时假设；677 已证明模拟器普通包自然续期并跨原到期点，真机 test01 自然到期与隔夜恢复仍未完成，详见 `docs/736-current-session-health-and-scope-baseline.md`。
- 2026-09-05 使用 test03 模拟器与 test01 真机完成一条请假审批的完整前加签真实流转：test03 提交后将 test01 前加签，原任务进入等待且失去操作权限；test01 收到待办与通知并实际同意后，test03 原任务自然恢复；test03 最终同意后整单通过、操作权限清空且考勤业务回写完成。两端页面、服务端详情、OA 事件游标和 SQLite 缓存一致，定向测试 71/71；同一申请编号日期段跨设备不一致继续按既有 P2 跟踪。详见 `docs/737-oa-front-add-sign-complete-real-flow.md`。
- 2026-09-05 使用同一双设备继续完成一条独立请假审批的完整后加签真实流转：新增 test01 节点在 test03 原节点完成前保持等待且不可操作，原节点同意后才向 test01 激活待办；test01 最终同意后整单通过、双方权限清空，test03 当前详情自然同步并完成考勤业务回写。服务端详情、事件游标与两端 SQLite 缓存一致；没有用同秒处理记录的显示顺序替代任务终态。详见 `docs/738-oa-back-add-sign-complete-real-flow.md`。
- 2026-09-05 使用 test03 与 test01 完成一条独立请假审批的转交后接收人完整处理：test03 转交后原任务进入 `transferred` 且立即失去同意/驳回权限，test01 收到新待办并实际同意后整单通过，test03 当前详情自然同步并完成考勤回写。服务端最终原任务 transferred、新任务 approved、双方均不可操作；与前后加签一起关闭 679 三类接收人未完成终态的历史证据缺口。详见 `docs/739-oa-transfer-complete-real-flow.md`。
- 2026-09-05 当前 Windows v1.0.91 入站 IM P1 再次复现：test03 移动端发送 direct 序号 18，服务端与 test01 真机均在数秒内落地且 test01 未读为 1；test01 真机打开后服务端未读归零、test03 自然收到已读回执。桌面进程正常响应并等待完整同步周期后，SQLite 仍停在 15，消息及 ReaderId=test01 的已读事件都未应用。桌面当前日志持续出现 connecting/retry-wait/transport-error，未观察到 connected/event received/ACK/history reconcile；进一步指向桌面事件传输或其重试链路，不在移动端增加旁路补丁。新的桌面端排查提示词和请求编号见 `docs/740-desktop-inbound-sync-current-reproduction.md`。
- 2026-09-05 移动端补齐可观测协议：登录与心跳显式携带环境隔离的移动安装 ID；心跳上报 `http_long_poll`、已落库/已 ACK 游标、待 ACK 数和健康状态；事件类型、sender+clientMessageId 去重及同账号 `conversation.read` 的单调已读语义统一到无 UI 依赖规则。test01/test03 覆盖安装后会话保持，连续两轮心跳均为 204，游标分别 2363/2363、2364/2364 且健康；1366/1366 全量与 0 静态问题通过。桌面下发配置核对为 IM REST `api.sfhkh.com:80`、IM gRPC `api.sfhkh.com:7273`，同机三轮探测只有 80 可达，运行进程也仅建立 80 连接；Windows gRPC/Gateway 仍是主修复端，详见 `docs/741-mobile-sync-health-and-desktop-gateway.md`。
- 2026-09-05 按“多端同步不止 IM”补齐全业务域口径和可观测性：移动端心跳新增 OA 事件游标、离线业务命令及通知已读待回执计数，日志统一为不含敏感标识的 IM/OA 双域健康；同时明确审批/待办/通知/公告/应用目录/表单流程版本/考勤/日程、联系人/群资料/资料/收藏及控制面三条同步通道。1368/1368 与分析 0；真机重新建立测试会话后心跳 204，IM 2363/2363 与 OA 634 均 healthy。模拟器本轮网络不可用，仅确认保留会话和缓存，不计为第二份在线证据。Windows 入站问题也必须按全部依赖 Gateway 的业务域验收，详见 `docs/742-cross-domain-sync-scope-and-health.md`。
> 2026-09-08：当前真实 Windows 窗口基准为 v1.0.105。站点访问、隧道和网络诊断明确归属桌面端；移动端只显示桌面端使用提醒，并通过通知中心承接授权变化或异常提醒，不再拉取站点列表、打开站点或启动隧道。实现与验证见 [801](801-windows-1105-desktop-only-site-boundary-20260908.md)。
