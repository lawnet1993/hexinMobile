# 852 · OA 20 附件上限与禁用态运行验收

时间：2026-09-09（Asia/Shanghai）  
范围：Flutter Mobile / 后台动态附件字段

## 修复

多附件字段达到后台下发的 `maxCount` 后，“添加文件”按钮现在直接进入禁用态，不再允许一次无效点击后才提示上限。单附件字段已有文件时仍保留“替换文件”能力。

## Android 运行证据

- emulator-5556 真实打开测试服“请款审批”动态表单，字段上限为 `20`。
- 连续选择同一 `AI-UAT` 测试图片 20 次，页面计数依次达到 `5 / 20`、`10 / 20`、`15 / 20` 和 `20 / 20`。
- 达到 `20 / 20` 后原生布局树显示“添加文件” `clickable=false`、`enabled=false`，文件选择器不再打开。
- 每项由主体和预览两份账号隔离加密文件组成，20 项时目录共 40 个 `.imq` 文件；逐项删除后恢复为 0。
- 10 项时 PSS 约 199 MB，20 项时约 211 MB，进程未重启、未崩溃，未观察到随附件数量成倍放大的内存增长。
- 测试过程中只保存本地草稿，没有提交审批；公共目录测试图片已清理。
- 截图：[20 / 20 禁用态](../test/evidence/oa-20-attachment-limit-20260909/01-limit-disabled.png)。

## 自动回归与构建

- 后台 schema 定向用例通过，明确断言达到 `maxCount` 后按钮回调为空。
- 相关静态分析：0 issue。
- 全量 Flutter 测试：1427/1427 通过，约 1 分 19 秒。
- Profile APK：85,792,566 bytes（81.8 MB）。
- SHA-256：`466894E3A4C6E5098459CEE48F9320B0565F52C6C679EF1CEFC03941AB0AE989`。
- 当前构建已保留数据覆盖安装 emulator-5556 和 realme 真机；真机仍锁屏，本轮没有绕过锁屏重复 20 项操作。

## 未完成边界

- 20 张不同大图、选择过程中撤权、上传中断网/杀进程及恢复仍需独立运行验证。
- iOS 文件提供器与内存曲线仍未验证。
