# 710 真机自动登录与旧包排查

时间：2026-09-04 22:57–23:00，Asia/Shanghai。

## 已确认

- 用户反馈真机频繁到期要求重新登录。真机 RMX3366 原安装时间为 2026-09-02 21:18:03，版本 1.0.1+2。
- 实际 base.apk SHA256 为 `18634FA2196C8D4ACEAD465623941C716EB3FA3945994AB7F201C6CD46626104`，精确匹配 [658](658-event-session-isolation-and-real-ack-crash-20260902.md) 的普通包。它早于 [670 到期前续期修复](670-proactive-session-refresh-20260902.md)。版本号相同不能证明已安装新代码。
- 本次未捕获用户此前退出时的请求/刷新响应，不将旧包这一事实等同于所有历史掉线的唯一根因。

## 本次操作

- 校验已有 709 普通入口 APK 后使用 `adb install -r` 覆盖安装；安装成功，未卸载、清数据、导出凭据或调用保存密码重新登录。
- 安装前后均为普通应用入口，不安装诊断探针。新包及设备 base.apk SHA256 一致：`2D32FC79E7635723DB2E4236C59648207C31EAD0E0C8C36E6066EB946731110E`。
- 新包包含到期前 5 分钟续期、恢复/前台心跳、并发刷新合并、网络失败保留会话与旧响应隔离。服务器明确替换/撤销或刷新授权失效仍要求重新认证，未用密码自动登录绕过。
- 此轮未修改业务源码；保留既有工作区改动。未更改服务器配置和任何业务记录。

## 验证

| 项目 | 本轮结果 |
| --- | --- |
| 覆盖安装后首次启动 | 自动恢复 Test Terminal 01，显示工作台、3 条待办，无需输入凭据 |
| Home 后回到前台 | 原账号与工作台保留，未出现登录页 |
| 强停进程后冷启动 | 自动恢复同一账号，待办/消息角标保留，无需输入凭据 |
| 定向测试 | 60/60 通过：主动刷新、诊断脱敏、旧响应隔离、安全存储、bootstrap 隔离 |
| 本轮会话诊断日志 | 未捕获 `MOBILE_SESSION_AUTH` 或 `MOBILE_SESSION_REFRESH` 事件，不能据此断言刷新接口已在本轮调用 |

测试使用 `E:\CodexToolchains\flutter-3.47.0\bin\flutter.bat`，执行 `session_proactive_refresh_test.dart`、`session_refresh_diagnostics_test.dart`、`session_response_isolation_test.dart`、`secure_session_store_test.dart`、`bootstrap_session_isolation_test.dart`。网络/5xx 保留会话及刷新竞争为自动化验证，不伪称本轮真机断网验收。

[真机冷启动恢复截图](../test/evidence/phone-session-20260904/phone-cold-restored.png)

## 未验证与边界

- 本轮短测未跨过真机实际令牌到期时间，也未等待长时间后台/隔夜恢复；不能判定频繁掉线问题已完全解决。
- [677](677-oa-member-picker-and-natural-refresh-20260903.md) 有模拟器正常包自然续期、跨旧到期时间与冷启动证据，但不能替代此次真机验收。
- 若更新后仍出现频繁退出，需要捕获脱敏的刷新 HTTP 状态/白名单错误码和会话终止来源，区分刷新授权过期、服务端撤销/替换及客户端故障。不要延长本地过期时间或静默保存密码重登以掩盖问题。

结论：真机旧包缺少已有续期修复的部署缺口已处理；短时登录恢复通过，真机长时间续期仍未完成验证。

2026-09-05 续查：当前 test01/test03 都有刷新凭据且命令、IM、OA 三条接口为 200；test03 在新环境真实刷新返回 200并同时轮换访问/刷新令牌，普通包恢复后仍自动登录。当前 JWT 时间提示约 2 小时而非旧资料的 12 小时；677 已有模拟器普通包自然跨到期点证据，本轮仍缺真机 test01 自然到期与隔夜恢复。详见 `736-current-session-health-and-scope-baseline.md`。
