# 668 审批表单稳定性与选择抽屉键盘

时间：2026-09-02 23:12–23:24 CN。结论：本轮专项通过，整体对齐目标仍进行中。

## 修复

1. 首次自动保存不再在申请人和表单之间插入 30dp 状态条及 8dp 间距。保存/恢复提示收进申请人栏右下角，预留一行，版本号与原表单位置不变；保存时间由长按提示查看。
2. 日期、时间、日期范围、单选和多选抽屉打开前清除当前输入焦点。确认、取消/返回后不再自动弹出旧输入框的键盘；主动再点输入框仍能正常编辑。可搜索抽屉仍可使用自己的输入焦点与键盘。
3. 本轮不改审批字段配置、计算公式、提交协议、审批权限或业务状态。

## 回归

- 新增 13 项测试：5 类抽屉 × 确认/返回、可搜索部门选择、首次保存/恢复草稿的字段几何稳定性。
- 旧实现复现首次保存字段下移 **38dp**，9 个焦点场景也直接失败；另一个多选测试初次使用了错误按钮文案，已按真实文案修正。
- 初次修复使用 route scope 的 unfocus 仍会恢复内部输入框，测试发现后改为清除实际 primaryFocus。
- 最终 **586/586** 全量通过，`flutter analyze` 0 问题，目标文件 `git diff --check` 通过。
- 初次新测试缺少本地模型 import、搜索用例误匹配输入框和列表项两个同文案节点，均属于测试搭建问题，已修正；过程日志保留，不将其当成产品缺陷证据。
- 正常入口 profile 构建成功，最终 APK 与 M3 安装包 SHA256 一致：`B0CC7B91EF6BE108E62C12E4A12157AE43029A8FDDB9996F03293CE30E559324`。

## 真实运行证据

仅操作独立 M3 / emulator-5556 / Android 16 / test03。未安装或操作 M1、M2、Windows 客户端；不是桌面 UI 验收。

真实进入加班审批，事由输入 `AI-UAT-20260902-232100-FORM-INTERACTION`，自动保存后操作日期抽屉取消，再从事由输入状态打开日期并确认 2026-09-03、时间 15:22。未填写结束时间、未提交申请。

Android UI 树实测坐标，单位为物理像素：

| 字段 | 首次填写前 | 自动保存后 | 确认日期时间后 | 断网冷启动恢复 |
| --- | --- | --- | --- | --- |
| 开始时间 | [63,389][1017,488] | 相同 | 相同 | 相同 |
| 结束时间 | [63,515][1017,614] | 相同 | 相同 | 相同 |
| 事由 | [63,819][1017,982] | 相同 | 相同 | 相同 |

实际输入时 `mInputShown=true`；日期/时间抽屉打开、取消后、确认后、草稿恢复后均为 false，截图无键盘。主动再点事由可再次正常打开键盘。取消未修改字段值，确认后时间正确，事由不丢失。

WiFi/数据均关闭后杀进程重启，随后确认默认网络 none，离线草稿完整恢复，审批流程明确提示网络不可用。重新启用网络后无需点击重试，实际审批人 Test Terminal 03 / 财顺 自动恢复。最后返回首页，WiFi=1、数据=1、默认网络130。

本轮仅新增本地加班草稿 `2672bb18-7c66-4e25-8e89-17c13a08b7fd`；原请假草稿 `61faf8e1-6404-4c34-b2d4-257fd6931949` 的更新时间 `2026-09-02T13:53:30.441508Z` 不变。Outbox 为空，原通知已读回执仍 sent。新草稿保留供后续验收，无新远端审批提交。

证据目录：[oa-form-interaction-20260902](../test/evidence/oa-form-interaction-20260902)。包含日志、field-bounds.json、键盘状态 JSON、SQLite 元数据，以及 04/05/07/10/13/14 页面 XML 和 PNG。冷启动瞬间的首次 UI 导出遇到 null root，未使用过期文件，随后唯一新路径导出成功。

## 未完成项

- 详情页长字段标签换行、其他表单密度和保存中/保存失败的细分反馈仍可继续优化。
- 真机尚未安装此包，桌面 UI 实操、完整高级 OA 分支/权限/并发/推送、自然到期和整体性能矩阵仍未全量验收。
- 既有服务端群读投影、缺失群事件及附件 500 不因本轮 UI 回归通过而关闭。
