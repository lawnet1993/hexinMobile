# 734 当前包群聊、@我与冷启动持久化实测

时间：2026-09-05 02:13–02:20（Asia/Shanghai）。当前移动端群聊双向同步、真实提及未读、可见已读和冷启动持久化通过。

## 实际参与端

- M1：realme 真机，Test Terminal 01 / test01。
- M3：Android 16 模拟器，Test Terminal 03 / test03。
- 会话 `bd15cbb6-ab8e-4cf2-9d62-fdb6f37ce90a`，标题 `AI-UAT-20260902-202100-M1-M3-GROUP`。
- UI、移动端 SQLite 与服务端均确认会话类型为 `group`，没有混用 direct 会话或单聊接口。

## 真实链路结果

1. 两端进入群聊时均显示 `2 位成员 · 2 人在线`。冷启动后又由现有桌面测试会话只读查询 test03 资料接口，HTTP 200、`isOnline=true`，不是客户端按成员数猜测在线。
2. M1 发出有效提及消息，服务端序号 14；M3 打开会话时约 8 秒可见并提交已读。
3. M3 停留在群组列表时，M1 再发一条有效提及，服务端序号 15。约 5.25 秒后列表出现未读角标 1；SQLite 同步为 `lastMessageSequence=15`、`lastReadSequence=14`、`unreadCount=1`、未读提及序列 `[15]`。
4. M3 打开群聊后，序号 15 上方出现“以下为未读消息”，约 1.55 秒内持久化为已读 15、未读 0、未读提及空数组。不是只清 UI 数字。
5. M3 反向发送测试消息，服务端序号 16；M1 约 8.3 秒可见。双方最后都为消息 16、已读 16、未读 0、Outbox 0。
6. 两端序号 13–16 的服务端消息 ID、`clientMessageId`、发送人和顺序逐条一致，各只有一份。服务端摘要同样为 16 条、已读 16、未读 0。
7. 两端强制停止并冷启动后自动恢复原账号；group 类型、16 条消息、已读和提及投影、回执、空 Outbox 全部保留。SQLite 完整性均为 ok；当前进程 FATAL 与 AndroidRuntime 错误均为 0。

## 消息分组与头像

- 发送侧序号 13–15 为同一发送人、同一天且五分钟内的连续组，头像只放在第一条，后续消息保留对齐位；接收侧遵循相同规则。
- 截图复核后确认当前实现已符合“头像放在连续消息第一条”的要求，因此本轮没有改动这段正确逻辑。
- 群聊仍在第一条显示发送人名称；自己的连续消息不重复显示名字，单聊与群聊没有混用展示规则。

## 无效测试记录

- 第一次 ADB 录入把正文插进提及姓名中间，形成服务端序号 13。该记录没有删除或修改，明确排除在有效提及验收之外。
- 序号 14、15 是重新录入并经 UI 可见文本核对的有效提及记录；后续未读角标、提及序列和可见已读均以序号 15 为依据。

## 证据

- `test/evidence/im-group-734/01-m3-mention-unread.png`：M3 群组列表未读角标 1。
- `test/evidence/im-group-734/02-m3-mention-read.png`：有效提及上方首条未读分隔与实际内容。
- `test/evidence/im-group-734/03-phone-group-final.png`、`04-m3-group-final.png`：双端序号 13–16 的最终会话、连续头像合并与反向消息。
- `05-phone-cold-metadata.json`、`06-m3-cold-metadata.json`：冷启动后脱敏 SQLite、消息身份、游标、回执与 Outbox 摘要。
- 本地数据由 `scripts/inspect-device-im-outbox.py` 对账号隔离的 SQLite DB+WAL 一致性快照只读取得；服务端状态由 `scripts/inspect-desktop-im-uat.ps1` 使用现有 test01 测试会话仅 GET 核对。

## 边界

- 本轮证明当前两个移动端之间的小群聊、真实 @我、可见已读和冷启动持久化，不代表 2000 人群、离线 500 条、群媒体播放或 Windows 桌面端入站链路已经重新通过。
- 在线结论是 02:20 的服务端快照；在线状态本身会随设备心跳动态变化。
- 没有修改生产代码、重新构建或安装 APK；当前树最近一次全量自动化仍为 1361/1361。
