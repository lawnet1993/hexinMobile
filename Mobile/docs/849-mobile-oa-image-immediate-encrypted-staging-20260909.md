# 849 · OA 图片压缩后立即加密暂存

时间：2026-09-09（Asia/Shanghai）  
范围：Flutter Mobile / OA 动态表单附件

## 结论

OA 图片维持本机压缩后上传，同时修正了多图片连续添加时的内存驻留方式：每张图片完成原生压缩后立即写入账号隔离的加密附件存储，页面状态不再持有压缩后的主体字节，只保留小尺寸预览和对象元数据。提交、断网重试和草稿恢复均从加密文件按流读取。

普通文件仍保持原始字节流，不执行有损压缩；头像仍按头像用途裁剪和压缩。除小头像协议外，图片和附件均不使用 Base64 上传。

## 验证

- OA 附件存储、页面和草稿生命周期定向测试：106/106 通过。
- 全量 Flutter 测试：1426/1426 通过，约 1 分 18 秒。
- 相关静态分析：0 issue。
- Android Profile APK 构建及 emulator-5556 保留数据覆盖安装通过；`MainActivity` 成功恢复。
- APK：85,792,566 bytes（81.8 MB）。
- SHA-256：`34FA382D298F3EF16B2C98AFD732E5889B8C089049ED801CCB5787615CAC814C`。

## 当前边界

- 真机当前停留在系统锁屏/通知遮罩，本轮未绕过锁屏安装或操作新版；该构建的 OA 多图真机 PSS 曲线仍未补测。
- iOS 图片选择、压缩、权限拒绝和低内存路径仍待真机验证。

