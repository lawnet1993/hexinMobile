# 736 当前双设备会话健康与范围基线

时间：2026-09-05 02:43–02:51（Asia/Shanghai）。本轮不修改业务数据，不修改登录账号、部门、岗位或后台配置。

## 范围调整

- 用户确认当前五个主页面已经基本达到预期，因此本轮不继续做无必要的首页、消息、待办、通讯录或“我的”视觉重构；后续优先处理会话内部、跨端同步、自动登录和 OA 实际流转。
- 桌面端附件预览链路已由另一侧修改，本项目只记录并等待新桌面版本及准确协议后复核，不并行开发移动端独立 Office 引擎。
- 真机重新启动后只读复核工作台，Test Terminal 01、常用应用、三条待办及五个底部 Tab 正常；消息页仍能区分单聊、群聊和真实在线/离线状态。没有进入审批处理、发送消息或修改资料。

## 当前真实会话健康

使用测试专用 Profile 入口读取系统安全存储中的现有会话，并只输出白名单元数据；没有输出密码、完整 Token、设备 ID、Cookie、指纹或业务正文。

| 设备 | 账号 | 刷新凭据 | 命令接口 | IM bootstrap | OA bootstrap | 访问令牌时间提示 |
| --- | --- | --- | --- | --- | --- | --- |
| realme 真机 | test01 | 有 | 200 | 200 | 200 | 18:45:08Z–20:45:08Z |
| Android 16 模拟器 | test03 | 有 | 200 | 200 | 200 | 18:08:19Z–20:08:19Z |

JWT 时间只作为未验签的调度提示；两组 `nbf/exp` 相差约 2 小时，说明当前环境不能继续按旧资料假设 12 小时访问令牌。客户端仍必须依赖刷新协议和服务端 401/409 作为权威结果，不能按本地时间自行退出或延长会话。

## 当前环境真实刷新

- 仅在 test03 模拟器执行一次测试专用真实刷新；真机 test01 未轮换令牌。
- `/connect/token` 返回 200，同时返回新的访问令牌和刷新令牌。
- 刷新前后账号、设备和同步设备归属保持不变；安全存储中的两种令牌均发生轮换，会话未清理、没有出现登录失效提示。
- 新访问令牌时间提示为 18:50:23Z–20:50:23Z，继续约 2 小时。
- 刷新后立即恢复普通 `lib/main.dart` Profile 包；test03 冷启动直接进入工作台，没有登录页、崩溃、Flutter 异常或布局溢出。
- test01 真机也恢复普通包，冷启动直接进入工作台且错误采样为 0。

## 自动化与构建

- 会话定向测试 73/73：主动续期、诊断脱敏、旧响应隔离、安全存储、bootstrap 隔离和心跳生命周期全部通过。
- 上一轮同一源码全量测试 1362/1362、静态检查 0 问题；本轮没有修改业务 Dart 源码。
- 真机普通双架构 Profile APK：83,425,355 bytes，SHA-256 `42ADFC263A03882055862193BDAF0EB445FE0AEFBA07DAEF943090487913446B`。
- 模拟器最终普通 x64 Profile APK：SHA-256 `26C24DD62B12CB2CA624095E972CBC134728A425B9989A6456E92D2D11FD42BA`。
- 结构化结果见 `test/evidence/session-health-736/04-session-health-summary.json`；恢复截图见 `01-phone-restored.png`、`02-emulator-restored.png` 和 `03-emulator-after-refresh-normal.png`。

## 尚未证明

- 本轮真实刷新成功，证明当前接口与客户端轮换逻辑可用；本轮本身没有自然等待到 `exp`。此前 677 已在 test03 普通包上真实观察到期前五分钟自动刷新 200、跨原到期点继续完成 OA 操作并冷启动恢复，因此模拟器自然续期已有证据，不应重新标成未验证。
- 真机 test01 仍没有自然跨过当前访问令牌到期点或隔夜后台恢复的证据；后续应在普通包自然运行到到期前五分钟及到期后继续取证，观察实际自动刷新、心跳、IM/OA 同步和冷启动恢复。不得用测试注入时钟或 test03 结果代替真机验收。
- 桌面入站缺消息仍是桌面 v1.0.91 侧既有 P1；本轮没有修改或重新判定。
