# 717 只读表单校验与桌面发布基准核对

结论：本地表单回归及后续真机缺值提示复验通过；用户截图已确认桌面运行界面为 v1.0.91。完整窗口交互和新协议尚未验收，不能判定移动端已对齐新版桌面端。

## 已复现与修正

- 后台 schema 标记只读、公式或 duration 的必填字段，在值为空时，旧移动端仍生成“请填写字段名”，与不可输入状态矛盾。
- 新增只读必填/非必填测试；修复前必填测试失败、非必填通过。现有 duration 测试增加真实字段配置下的提交校验断言。
- 仅修改通用缺值提示：计算字段提示检查关联字段，其他只读字段提示检查表单配置。字段名来自 schema。
- 必填仍阻止空值提交；可编辑字段仍提示填写/选择；后台字段错误、公式具体错误的优先级未改。
- 未修改后台配置、公式表达式、duration 算法、默认值、单位或业务规则，没有新增写死的请假规则。
- 专项 `oa_mobile_pages_test.dart`、`oa_form_interaction_test.dart`、`oa_calculation_roundtrip_test.dart`：102/102；全量 1269/1269；`flutter analyze` 无问题。
- 后续正常入口 Profile APK 构建成功（33.4 秒，80.7 MB），尚未安装，真机仍是 715 包；此修正的真机复验未执行。

## 线上版本记录

当前运行 `C:\Users\86137\AppData\Local\合兴智联\hexing-zhilian.exe` 的文件/产品版本均为 **1.0.87**。不据此推断页面代码版本。

通过已知公开发布接口只读查询，不是管理后台页面操作：

`GET http://api.sfhkh.com/api/client/versions/public/latest?product=terminal&channel=stable&platform=windows&architecture=x64&currentVersion=1.0.87`

- `hasPublishedVersion`: true
- `latestVersion`: **1.0.91**
- `updateAvailable`: true
- `releaseId`: `da89b121-bd29-4c34-94e3-ec5da77b6a13`
- `publishedAt`: `2026-09-03T05:28:14.024334`（原值不带时区，未自行换算）
- `packageSize`: 252095283
- 发布说明原文：“IM/OA 统一 Push Gateway：消息、好友、建群、审批、在线状态写库后经 Outbox 推送，客户端 ACK 后断线补偿；同时修复受控浏览器访问次数、退出清理和登录页更新检查。”

HTTPS 查询未取得 HTTP 响应；上述 HTTP 公开接口成功返回，无凭据传输。未下载、启动或安装桌面更新。

## 对齐风险与下一步

### 用户报告已运行 1.0.91 后的复核

用户随后确认桌面已运行到 1.0.91。只读进程复核看到新 PID 139028，启动时间为 2026-09-04 23:52:54，但同一路径 EXE 的 FileVersion / ProductVersion 仍均为 1.0.87，文件修改时间仍为 2026-09-02 13:29:36。当前窗口标题为“合兴智联”。这些元数据与用户看到的版本不一致，不能擅自认定升级失败，也不能写成已独立确认 1.0.91 窗口版本。后续需核对运行界面的版本显示与实际新功能；没有重复启动安装。

后续用户截图 `C:/Users/86137/AppData/Local/Temp/codex-clipboard-4ffeb872-60bf-4f11-88dc-f07025e04f7b.png` 右下角明确显示 **版本 v1.0.91**，当前桌面账号为 test01。运行界面版本现已由截图确认，后续对齐以此窗口为准，不再用 EXE 元数据否定更新或重复安装。截图证明运行界面版本，不单独证明所有新协议已验收。

移动端已通过 `adb install -r` 保留数据安装修正包，APK SHA256 为 `C00555B9986D34A5454C56B39EB209269B76B67C86D862B60F8D25F266847C7D`；真机复验另行记录。

### 真机复验补记

2026-09-04 23:56，test01 自动恢复登录。打开请假审批空表单，点击提交后仍停留表单，开始/结束/类型/事由显示各自必填提示；天数显示“请假天数尚未计算，请检查关联字段”，未再要求手填。未填写、保存草稿或完成业务提交。截图已查看：[真机必填提示](../test/evidence/readonly-form-717/02-required.png)。检查当时进程日志 FATAL / Unhandled / RenderFlex overflow 均 0。此后按钮外观继续按用户要求调整，见 718。

- 当前移动端仓库实际仍包含 `/api/im/sync/events`、`/api/im/sync/ack`、`/api/oa/sync/events`。未发现明确的统一 Push Gateway 客户端实现；仅凭发布说明不能断言旧接口已废弃，也不能认定现有补偿机制已经覆盖新协议。
- 下一步须核对新版实际 Push Gateway 协议、订阅/鉴权、事件 ID、游标、ACK、断线恢复，再验证消息、好友、建群、审批和在线状态。不能按名称猜接口或改变持久化顺序。
- 当前工具目录无浏览器控制、node_repl 或插件搜索入口，尚未打开后台版本管理页或操作新版 Windows 窗口。公开最新版本查询不等于查完后台所有发布历史、灰度或平台记录。
- 继续保留真机跨到期会话、完整多端替换、高级 OA 等未完成项；没有缩小整体目标。
